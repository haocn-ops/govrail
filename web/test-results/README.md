# Test Results Workspace

這個目錄保留給 `web` 相關測試在本地生成暫存結果時使用。

目前已知會出現在這裡的內容包括：

- `.last-run.json`

這些文件屬於本地測試輸出，不應作為產品代碼或文檔主體提交。為了讓目錄用途清晰、同時避免生成物持續污染 git 狀態，本目錄保留：

- `README.md`：說明用途
- `.gitignore`：忽略生成物，但保留說明文件可追蹤

如果後續新增新的本地測試輸出，優先沿用這個目錄內的局部忽略規則，不要把暫存結果混入根層文檔或產品代碼提交。
