# Govrail SaaS v1 開發執行文檔

交付對象：Founder / Product / Tech Lead / Full-stack 工程師 / 並行開發 agent  
版本：v0.2  
日期：2026-04-04

## 1. 文檔目的

這份文檔不是新的產品規劃，而是把「接下來 4 週應該怎麼開發」落成可執行的工程文檔。

它主要解決 4 個問題：

- 目前 SaaS 化到底做到哪個成熟度
- 接下來應該先補哪些缺口
- 哪些任務可以並行
- 每條任務線應該主要落在哪些檔案

本文件以目前倉庫真實代碼為準，不以早期規劃文檔中的缺口描述為唯一依據。

## 2. 當前判斷

### 2.1 當前成熟度

目前 Govrail 已經不是「SaaS 概念驗證」，而是：

- 已有 multi-tenant SaaS data model
- 已有 `/api/v1/saas/*` 後端主幹
- 已有 Web Console 與 onboarding / members / api keys / settings / usage / admin 等 SaaS surface
- 已有 service account、workspace invitation、workspace API key、workspace billing session、workspace delivery tracking

但它仍未達到「完整自助式 SaaS v1」。

### 2.2 目前最關鍵的缺口

目前最值得優先補的不是 workflow runtime，而是 SaaS 外層：

- Console session / workspace context 仍保留 `metadata -> env-fallback -> preview-fallback`
- 若 upstream 不可用，部分控制台 API 仍會回 preview/fallback data，容易掩蓋真實問題
- billing 雖已有 checkout/session/subscription 骨架，但 production self-serve path 尚未完全收口
- onboarding 仍是「產品內 + runbook/script 混合模式」，未完全產品化
- SSO / audit export / dedicated environment 仍偏 readiness / staged surface

## 3. 本輪開發目標

本輪 4 週總目標：

把目前的「可演示托管式 SaaS Beta」推進到「可小規模真實試用的 SaaS v1」。

### 3.0 當前執行狀態

截至 2026-04-03，本文件對應的執行狀態如下：

- 第一波並行開發已完成：
  - `A` workspace/session hardening
  - `B` critical route hardening
  - `C` mutation/detail proxy consistency
- 已完成驗證：
  - `npm run check`
  - `npm run web:check`
- 第二波並行開發已完成：
  - `D1` billing backend hardening
  - `D2` billing frontend/productization
  - `E1` onboarding backend/productization
  - `E2` onboarding frontend/productization
- 第二波完成後已通過驗證：
  - `npm run check`
  - `npm run web:check`
- 已新增 `web:test` 最小測試入口：
  - `npm --prefix web run test`（對應 `web/package.json#test`，目前覆蓋 `web/lib/__tests__/*.test.ts` 與 `web/app/api/**/__tests__/*.test.ts`）
  - `npm run web:test`（倉庫根腳本，轉發到 `npm --prefix web run test`）
- 已新增細分測試入口：
  - `npm run web:test:unit`
  - `npm run web:test:contract`
  - `npm run web:test:page`
  - `npm run web:test:e2e`
  - `npm run web:test:e2e:file -- tests/e2e/saas-mainline-smoke.e2e.test.ts`
- 第三波中的 route/source 護欄已補齊一層最小入口：
  - 已納入當前 `web:test` 入口：
    - `web/components/__tests__/handoff-query-surface-a.test.ts`
    - `web/components/__tests__/handoff-query-surface-b.test.ts`
    - `web/lib/__tests__/control-plane-proxy.test.ts`
    - `web/lib/__tests__/workspace-context.test.ts`
    - `web/lib/__tests__/workspace-context-metadata-guard.test.ts`
    - `web/lib/__tests__/enterprise-surface-routes.test.ts`
    - `web/app/api/workspace-context/__tests__/route.test.ts`
    - `web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts`
    - `web/tests/page-level/workspace-context-hardening.page.test.ts`
    - `web/tests/page-level/settings-enterprise-write.page.test.ts`
    - `web/tests/page-level/settings-saved-refresh-coupling.page.test.ts`
    - `web/tests/page-level/verification-go-live-handoff.page.test.ts`
    - `web/tests/page-level/delivery-track-context-handoff.page.test.ts`
    - `web/tests/page-level/handoff-query-continuity-contract.page.test.ts`
    - `web/tests/page-level/console-pages-handoff-contract.page.test.ts`
  - 目前已細分為 `unit` / `contract` / `page` / `e2e`；其中 `e2e` 已落地最小 non-browser smoke，完整瀏覽器 e2e 仍後置。
  - `npm run web:test:e2e` 仍保留給整包 non-browser smoke；若要 targeted 重跑單檔，請改用 `npm run web:test:e2e:file -- tests/e2e/<name>.e2e.test.ts`，不要期待 `npm run web:test:e2e -- <file>` 只執行單一 smoke。

本節用來幫助接手人快速判斷哪些工作流已進入實作階段，避免文檔與實際推進狀態脫節。

### 3.0.1 第三波已落地內容（同步）

截至 2026-04-04，第三波低風險收斂與文檔化已完成以下內容：

- enterprise backend additive readiness contract：
  - enterprise surfaces（`audit export` / `SSO` / `dedicated environment`）延續加法式 readiness contract，不破壞既有 workspace/billing 主流程，並維持 metadata-only guard 與 fallback 語義邊界。
- enterprise frontend contract normalize：
  - 前端對 enterprise/billing 狀態與 provider 語義做 normalize，降低 staged/ready、provider-managed/workspace-managed 表述不一致造成的誤判。
- settings enterprise surface productization：
  - `settings` 已作為三個 enterprise 能力的統一操作入口，具備更明確的 plan gate、狀態判讀與回退導向。
- SSO / dedicated environment controlled live write 同步：
  - 兩個 enterprise surface 已從通用 write-prep 文檔階段前進到 controlled live write 同步階段：目前已具備受控 web `POST` wrapper、metadata guard 與 delivery/readiness contract，可支撐受控 rollout；但仍不等同完全無條件的全面自助開放。
- enterprise fallback helper 與 service 再收口：
  - `web/services/control-plane.ts` 新增 `buildEnterpriseFallbackMeta` / `getEnterpriseFallbackUpgradeHref` / `getEnterpriseFallbackPlanCode` 等共用 helper，使 SSO / dedicated environment 的 fallback contract metadata、upgrade href 與 plan code 在 409 / 503 / generic request failure 場景中保持一致。`saveWorkspaceSsoReadiness` / `saveWorkspaceDedicatedEnvironmentReadiness` 透過這些 helper 進行 controlled live write，`downloadWorkspaceAuditExportViewModel` 也同步回傳 contract meta；相對應的 `web/lib/__tests__/control-plane-service.test.ts` 現在有 dedicated 409 fallback test，確保 contract source / issue / upgrade href / plan code 固定。這也讓 `settings` 頁面的 enterprise submit flow 能直接呼叫這些 service helper、維持原本的 contract 與手動錯誤映射，對應 `web/tests/page-level/settings-enterprise-write.page.test.ts`（含 upgrade card / next-steps / helper import）與 `web/tests/page-level/settings-saved-refresh-coupling.page.test.ts`（含 saved section coupling 與 source badges）進一步鎖定語義，並透過 `web/tests/e2e/saas-mainline-smoke.e2e.test.ts` 的 non-browser smoke 驗證 fallback upgrade href 與 contract source。
- service mutation helper 再收口：
  - `web/services/control-plane.ts` 的 `createRun`、`createWorkspace`、`bootstrapWorkspace`、`createApiKey` / `revokeApiKey` / `rotateApiKey`、`createServiceAccount` / `disableServiceAccount`、`createWorkspaceInvitation` / `revokeWorkspaceInvitation` / `acceptWorkspaceInvitation` 已改為復用可配置 `fallbackMessage/fallbackCode` 的 `postJson(...)`，把非 2xx 情況統一收斂為結構化 `ControlPlaneRequestError`，不再混用裸 `Error(status)`。這讓 service 層的 mutation/error contract 與前面已完成的 route-wrapper/helper 化口徑更一致，也讓後續 `enterprise live-write backend` 與 `settings live wiring` 可以直接站在統一的 service failure semantics 上推進。
  - 這組收斂也讓 `createRun`/`createWorkspace`/`createApiKey` 之外的 workspace-mutating flow（`service_account`/`invitation`/`tool_provider` 的創建與狀態切換）共享同一份 `ControlPlaneRequestError` 反映與 plan-limit metadata，進一步把控制台端的 mutation contract 往 `run/workspace/api-key/service-account/invitation/tool-provider` 的整體一致性靠攏。
- tool-provider service/UI 對齊：
  - `web/services/control-plane.ts` 新增 plan-limit aware 的 `createToolProvider(...)` / `updateToolProviderStatus(...)`，並以 `planLimit` metadata 保留 `plan_limit_exceeded` 語義；`web/components/agents/tool-provider-list.tsx` 現已改為直接復用這些 service helper，而不是在元件內維護另一套 `postJson(...)`。相對應的 `web/lib/__tests__/control-plane-service.test.ts` 已補上 tool-provider mutation 的 request/error contract 與 plan-limit passthrough 護欄。
  - `web/components/api-keys/api-keys-panel.tsx`、`web/components/api-keys/create-api-key-form.tsx`、`web/components/service-accounts/create-service-account-form.tsx`、`web/components/members/create-invitation-form.tsx` 以及 `web/components/agents/tool-provider-list.tsx` 目前都依賴 `ControlPlaneRequestError` 來選出 limit/error copy，並有對應的 page-level contract tests（`web/tests/page-level/api-keys-form-error-contract.page.test.ts`、`web/tests/page-level/access-forms-error-contract.page.test.ts`、`web/tests/page-level/tool-provider-list-contract.page.test.ts`）鎖定這些文字與 helper import，避免改動時錯誤語義漂移。
  - 進一步地，`web/components/service-accounts/service-accounts-panel.tsx` 與 `web/components/members/invitations-panel.tsx` 的 disable/revoke action error 也已改為復用結構化錯誤語義，並由 `web/tests/page-level/service-accounts-panel-error-contract.page.test.ts`、`web/tests/page-level/invitations-panel-error-contract.page.test.ts` 固定 helper/copy/red-banner 契約，避免控制台在 create-action 與 detail-action 之間再次分叉成兩套 generic 文案。
- route wrapper identity hardening：
  - route wrapper 身份與 workspace header forwarding 規則已進一步收斂，核心 metadata-only 路由維持嚴格錯誤返回，不再用高擬真 fallback 掩蓋問題。
  - `W12-C11` 目前又向前收了一層：workspace-scoped detail mutation routes 已抽出 `proxyWorkspaceScopedDetailPost(...)`，`tool-provider` detail routes 也開始透過獨立 `route-helpers.ts` 收斂 path/init 樣板；這一段仍維持 `in_progress` 口徑，表示 helper family 正在繼續收口，而不是已全面完成。
- 測試計劃與 runbook 已新增：
  - 最小測試計劃：`docs/tests_observability_wave1_wave2_min_plan_zh.md`
  - enterprise surface runbook：`docs/enterprise_surface_runbook_zh.md`
  - enterprise route/source 護欄：`web/lib/__tests__/enterprise-surface-routes.test.ts`

這一波以「不擴大風險面、先補可驗收 contract 與運維文檔」為優先，為後續 tests-observability 與 enterprise feature 深化提供穩定基線。

### 3.0.2 下一步建議並行槽位（更新）

註：本段保留當時的拆解建議；其後續承接狀態請以 `3.0.3 當前並行槽位狀態`、`3.0.4 本輪並行拆解狀態` 與下文 execution harness 補記為準。A1/B1/C1 已被後續的 enterprise contract deepen、settings/service 對齊，以及 page-level/tests-observability 收口所吸收，不再視為新的未開工槽位。

前一輪建議的三條槽位中：

- 槽位 A `enterprise backend 深化`：additive readiness contract 已落地，現轉入 controlled live write 契約同步。
- 槽位 B `settings write-prep`：通用 write-prep 已落地，現轉入 controlled live write 導流、提交與回退同步。
- 槽位 C `route-contract-tests`：已補一層最小 source/route 護欄，且目前 `web:test` 已覆蓋 `web/lib` 與 `web/app/api` 兩層最小契約測試。

建議把下一步切成 3 條新的可並行槽位，降低互相阻塞：

1. 槽位 A1：enterprise live-write backend  
  目標：為 `SSO` / `dedicated environment` 等 enterprise surface 定義真實寫入 API 與 payload/錯誤契約，從 controlled live write 同步邁向真正可驗收的受控寫入。
2. 槽位 B1：settings live wiring  
  目標：把目前 settings 的 controlled live write 導流逐步接到真實 mutation，補齊提交後狀態刷新、錯誤映射與回退說明。
3. 槽位 C1：page-level tests / observability  
  目標：補 `Topbar` / `Members` 的頁面級驗證與最小觀測口徑，並逐步把既有 `web/app/api` 契約測試納入統一測試入口。

建議節奏：

1. 先把 C1 的頁面級最小驗收補齊，延續已完成的 P0 route-contract-tests。  
2. 同步推進 A1/B1，避免 settings UI 與 backend live-write 契約再次脫節。  

### 3.0.3 當前並行槽位狀態（同步）

截至 2026-04-04，目前並行槽位狀態建議按以下口徑同步：

- 槽位 A（enterprise backend 深化）：`in_progress`
  - additive readiness contract 已落地，SSO / dedicated environment 已進入 controlled live write 同步口徑；下一步轉入 `enterprise live-write backend`。
- 槽位 B（settings write-prep）：`in_progress`
  - settings enterprise surface 已從通用 write-prep 前進到 controlled live write 導流與提交準備，下一步轉入 `settings live wiring`；當前仍需保持「受控 rollout，不等同全面自助開放」邊界，避免過度承諾。
- 槽位 C（route-contract-tests）：`in_progress`
  - 已有 `web/lib/__tests__/control-plane-proxy.test.ts`、`web/lib/__tests__/workspace-context.test.ts`、`web/lib/__tests__/workspace-context-metadata-guard.test.ts`、`web/lib/__tests__/enterprise-surface-routes.test.ts` 納入當前最小入口；
  - `web/app/api/workspace-context/__tests__/route.test.ts`、`web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts` 目前也已納入當前 `web:test`；
  - 下一步優先補頁面級與 observability，並把當前混合入口進一步細分為 unit/contract/e2e。
- 槽位 E（docs/tests observability sync）：`in_progress`
  - 聚焦把擴大的 shared handoff helper（含 `buildHandoffHref`）覆蓋面、delivery panel continuity contract、`settings -> verification/go-live` 覆蓋口徑、`onboarding/api-keys/access forms/tool-provider` 的新增 page-level contract，以及 `W12-E01` 邊界聲明同步到執行文檔與最小測試計劃，避免測試覆蓋口徑漂移。

### 3.0.4 本輪並行拆解狀態（2026-04-03，同步）

以下狀態是「本輪拆解中的執行狀態」，不是最終完成狀態：

- 槽位 A2（page-level validation）：`completed`
  - 範圍：`Topbar` fallback warning badge、`Members` metadata guard 頁面表現。
  - 已落地 `web/tests/page-level/workspace-context-hardening.page.test.ts` 與 `web:test:page`，先以前置 page-level/source contract 補齊回歸網。
- 槽位 B2（enterprise backend contract deepen）：`completed`
  - 範圍：SSO 多域名原生契約（向後兼容 `email_domain`）、dedicated intake 字段持久化與 readiness 回顯深化。
  - 已完成 `normalize -> save(config_json) -> parse -> readiness` 對齊；維持 controlled live write，不擴大為全面 self-serve GA。
- 槽位 C2（enterprise settings/service 對齊）：`completed`
  - 範圍：settings 提交流與 backend 新契約對齊、錯誤映射與刷新閉環。
  - 已完成 settings/service/type 對齊，並補齊草稿回填與已保存配置回顯。
- 槽位 D2（docs/tests 口徑同步）：`completed`
  - 範圍：開發執行文檔、tests-observability 最小計劃、enterprise runbook 的同輪同步。
  - 口徑：文檔已回寫為本輪真實已落地狀態，不把未合併代碼寫成已完成；`W12-E01` 明確為可執行 non-browser smoke，`W12-P04/P05/P07/P08/P09/P11/P12/P13` 明確作為 settings handoff、verification/go-live continuity、console pages continuity 與 `service-accounts/api-keys/usage/verification` explicit-surface continuity 的 page/source 契約主回歸網。
- 槽位 E2（docs/tests 邊界同步）：`completed`
  - 範圍：共享 handoff helper（含 `buildHandoffHref`）、delivery panel continuity contract、`settings -> verification/go-live` 導流覆蓋面、`W12-E01` 邊界聲明更新。
  - 口徑：`settings` 到 verification/go-live 的導流 affordance、console pages continuity 與 surface query continuity 不歸入 full e2e 完成聲明，而是由 page/source contract（`W12-P04/P05/P07/P08/P09/P11/P12`）承擔主回歸網。

### 3.0.5 W12-E01 邊界同步（2026-04-03）

- `W12-E01` 現口徑為「可執行 non-browser smoke」，不是「完整瀏覽器端到端旅程」。
- smoke 已覆蓋：
  - `workspace-context` metadata mainline（`GET + POST`）
  - `settings` submit path + success affordance + saved-sections 最小 source-assisted contract
  - `settings` 側 verification/go-live handoff affordance 與 audit export execution contract
  - shared handoff-query 在 settings/go-live/delivery surfaces 的 continuity contract（source-assisted+execution）
  - 更深一層 source-assisted+execution continuity：`surface -> verification/go-live -> admin return`、`onboarding/playground/usage` shared helper query-key continuity，以及 `artifacts/logs/members` console-page continuity smoke
- smoke 未覆蓋（由 page/source contract 補位）：
  - verification/go-live 與 delivery panel 的完整跨頁連續旅程（含 UI 交互時序與真實導航）
  - 真實瀏覽器互動細節與完整 verification/go-live 端到端旅程
- 本輪同步補充：
  - `artifacts/logs` 的 verification/go-live 導流已顯式帶 `surface`（`/verification?surface=verification`、`/go-live?surface=go_live`），並由 page/source contract 固定語義。
  - shared handoff continuity 新增主要落在 page/source contract（`web/tests/page-level/handoff-query-continuity-contract.page.test.ts`、`web/tests/page-level/console-pages-handoff-contract.page.test.ts`、`web/components/__tests__/handoff-query-surface-a.test.ts`），覆蓋 `verification/go-live/api-keys/service-accounts`、`onboarding/playground/usage` 與 console pages 的 helper continuity 口徑。
  - `api-keys` 與 `service-accounts` 的 console server pages 也已收斂到 shared console handoff helper：兩頁現在改為用同一套 `parseConsoleHandoffState` / shared handoff href builder 供應 admin follow-up、onboarding hint 與 credential next-step CTA，不再各自維護手寫 query 解析；對應由新增的 page-level contract 與既有 `console-pages-handoff-contract` 一起固定。
  - `service-accounts/api-keys/usage/verification` 的 explicit `surface` 收斂已補齊：`usage`、`service-accounts`、`api-keys` 與 `verification checklist` 上的 handoff 入口現統一顯式帶 `verification?surface=verification`；對應 source/page contract 已同步收緊。
  - audit-export evidence chain 已往下游再補一層顯式提示：`session` 的 trusted-launch relay、`launchpad`、`onboarding`、`playground`、`usage`、`members`、`api-keys`、`service-accounts`、`tasks`、`artifacts`、`logs`、`agents`、`egress`、`settings` 的 relay / continuity 卡片，以及 `verification checklist` 的 evidence note、`delivery panel` 的 shared receipt reminder、`go-live drill` 的 continuity 卡片與 `admin follow-up` 的回流提示，現在共同固定「navigation-only / manual evidence relay」口徑；對應由 session/launchpad/onboarding/playground/usage/members/api-keys/service-accounts/tasks/artifacts/logs/agents/egress/console/delivery/go-live/admin 相關 page/source contract 承擔主回歸網。
  - `workspace launchpad` 與 `accept-invitation` 入口鏈已補一層 continuity 護欄：前者把 recommended/all-surfaces 中的 `verification/go-live` 收斂到顯式 `surface`，後者把 onboarding path builder 改為保留既有 query，避免 `surface` 被 continuity query 吃掉。
  - 最新一輪再補了 shared continuity 的相容層與 guardrail 收口：`session`、`service-accounts`、`members`、`delivery` 目前都已對齊 shared console handoff helper / admin-return helper 的相容語義，`accept-invitation` 的 seat-limit/trusted-session guardrail 文案也已重新鎖回穩定 contract，不把 `upgrade_href` 或 invite redemption copy 漂移成新的 UI 契約。
  - 這一輪並額外用 28 條 page-level continuity contract 與兩條最小 browser smoke（`members-accept-invitation-static`、`session-members-verification-admin-return`）驗證 `members / accept-invitation / session / service-accounts / delivery / admin-return` 這條鏈路；驗收口徑仍是「最小真實 smoke 已補一小段」，不是 full browser e2e 已完成。
  - `web:test:e2e` 本輪已加深為 source-assisted+execution smoke，但仍維持 non-browser 邊界聲明，未宣告 browser e2e 完成。

### 3.0.6 Invitation continuity 合約同步（2026-04-03）

- 前提：invitation continuity 已落地，`members` 的 invitation 入口已作為主鏈路之一。
- 本輪新增同步鏈路：
  - `members -> accept-invitation -> members/playground/verification/go-live`
- 合約重點（對齊 shared handoff continuity）：
  - `members -> accept-invitation` 在適用場景（onboarding handoff）下延續 shared continuity query 語義，不改寫 key 命名。
  - `accept-invitation` 的 next-step builder 在導流到 `members/playground/verification/go-live` 時，不丟失：
    - `week8_focus`、`attention_workspace`、`attention_organization`、`delivery_context`、`recent_track_key`、`recent_update_kind`、`evidence_count`、`recent_owner_label`、`recent_owner_display_name` 與 `recent_owner_email`。
  - `verification/go-live` 導流繼續保留顯式 `surface`：
    - `/verification?surface=verification`
    - `/go-live?surface=go_live`
- 邊界聲明（避免口徑漂移）：
  - 上述覆蓋層級歸入 `W12-E01` 的 source-assisted+execution non-browser smoke 與 page/source contract 主回歸網。
  - 這不是完整 browser e2e，不宣告真實跨頁點擊時序、渲染與導航全覆蓋。

### 3.0.7 Backend slices 同步（2026-04-04）

本輪已完成、且需要回寫到執行口徑的 backend slices 如下：

- session / workspace identity boundary 已收緊：
  - `/api/v1/saas/*` 的 user resolution 現只接受可信 authenticated subject header：`x-authenticated-subject` 或 `cf-access-authenticated-user-email`。
  - 直接依賴 `x-subject-id` 的 SaaS session 解析已被拒絕；web `workspace-context` 也已同步成只有在有可信 subject 時才會打 `/api/v1/saas/me`，否則誠實回落到 `env-fallback` / `preview-fallback`，不再用 env 預設 subject 假裝 live metadata session。
- workspace / organization access gate 已收緊：
  - SaaS workspace 存取現在同時要求 active workspace、active organization、active workspace membership 與 active organization membership。
  - disabled workspace、disabled organization membership 與不再 active 的 access path，現已從 `/api/v1/saas/me` 與 workspace detail 主鏈路中被排除。
- invitation / seat-limit guard 已收緊：
  - invitation 建立與接受都會套用 `member_seats` 限額，pending invitation 會一併計入 seat reservation。
  - disabled workspace / organization、disabled membership 與非 pending invitation 不再可被接受，避免 invitation path 繞過 workspace 狀態與 seat gate。
  - web `accept-invitation` page 也已把這批保護邊界轉成可見敘事：trusted session 缺失、seat-limit、revoked/expired token，以及 disabled workspace / organization 都有獨立文案，不再只把 backend 原始錯誤直接丟給使用者。
- usage / billing period 對齊已完成：
  - `src/app.ts` 的 usage ledger 寫入與 plan-limit 判定現已統一跟隨 subscription billing period，不再混用與訂閱週期脫節的時間窗。
  - `run`、`replay`、`tool-provider` 等會消耗配額的路徑，現在都會把 subscription period 一起帶入 usage 記錄與 limit 判斷，避免 usage 顯示、billing 週期與限流報錯三者各說各話。
  - `web/components/usage/workspace-usage-dashboard.tsx` 也會把 `period_start` / `period_end` 顯式展示成 `Current usage window`，並在 over-limit 時保留回到 `/settings?intent=manage-plan` 升級 lane 的可見 CTA，方便把 period boundary 帶進 verification evidence。
- plan-limit detail contract 已標準化：
  - workspace / run / provider / seat-limit 等 `plan_limit_exceeded` 類錯誤，現已對齊為同一組 detail 欄位：`scope`、`used`、`limit`、`remaining`、`workspace_id`、`plan_id`、`plan_code`、`upgrade_href`、`period_start`、`period_end`。
  - `web/services/control-plane.ts` 已同步擴充 `PlanLimitState` 與 `parsePlanLimitError(...)`，控制台不再只拿到局部 limit 訊息，而能顯示剩餘額度、升級導流與計費週期邊界。
- `workspace_onboarding_states` 持久化已落地：
  - 已新增 `migrations/0007_workspace_onboarding_states.sql`，作為 onboarding 狀態持久化的最小表結構。
  - workspace create 會持久化 `workspace_created` 初始狀態；bootstrap 完成後會把 bootstrap summary 一起寫回；`buildWorkspaceOnboardingState(...)` 也已改為合併持久化狀態，而不是僅依賴即時計算。
  - 這代表 onboarding 已從「僅靠當下 API 現算」前進到「可回看、可恢復、可延續」的 state persistence 基線，但目前持久化主體仍聚焦在 create/bootstrap summary，尚未擴展到所有 demo/evidence milestone。
- root smoke / web service test 覆蓋已同步補齊：
  - 倉庫根層 `npm run smoke` 已新增並固定以下 backend mainline：workspace create idempotency（含 plan 回傳）、disabled workspace / inactive org membership guard、invitation seat limit、bootstrap provider limit、usage/billing period 對齊、plan-limit detail contract、bootstrap onboarding persistence、trusted subject identity requirement、disabled workspace invitation accept guard。
  - web service 單元契約已由 `web/lib/__tests__/control-plane-service.test.ts` 補上 plan-limit 擴展欄位斷言，固定 `remaining`、`planId`、`planCode`、`upgradeHref`、`periodStart`、`periodEnd` 等解析結果，避免 backend detail contract 與 web service parsing 再次漂移。
  - 本輪對應驗證口徑已包含：
    - `npm run check`
    - `npm run smoke`
    - `npm --prefix web run check`
    - `npm run web:test:unit`（若只在 `web/` 目錄做低階/局部調試，才改用 raw `node --import tsx --test ...`）

### 3.1 成功標準

至少達成以下結果：

- 核心控制台頁面不再依賴 preview/fallback 假資料
- workspace / organization / membership 由真實 session 與 metadata 決定
- billing 具備可試運行的正式自助升級主路徑
- onboarding 關鍵步驟可在產品內完成並可回看狀態
- 至少一項 enterprise 能力從 staged/readiness 推到可用版本

### 3.2 本輪不做

- 不追求完整 PLG 註冊體驗
- 不做複雜多套餐、多幣種、多市場計費
- 不做完整 marketplace 或通用 workflow builder
- 不重寫 Worker runtime 與 northbound API 主幹

## 4. 開發原則

- 保留既有 `tenant_id` runtime 隔離鍵，不重寫 control plane 核心
- SaaS 元資料層與 console session 層優先補齊
- 先讓真實資料可見、錯誤顯性，再做更多 fallback 美化
- 優先做能直接影響可售賣性與可運營性的能力
- 並行開發時，盡量以不重疊的檔案集切分 ownership

## 5. 4 週里程碑

### Week 1：Session / Context Hardening

目標：

- 收口 workspace context
- 減少 preview / env fallback 對正式控制台的干擾
- 統一 Web proxy 的身份與 workspace header 注入

完成定義：

- 核心 SaaS 頁面在沒有 metadata 時回明確錯誤或顯示 local/dev mode，而不是默默回 preview data
- workspace context 能清楚區分 production-like metadata 模式與 local fallback 模式
- 主要 proxy route 的 headers 行為一致

### Week 2：Billing Productionization

目標：

- 明確 self-serve billing 主路徑
- 跑通 checkout -> completion/webhook -> subscription state update
- 完成基本 cancel / resume / portal flow

完成定義：

- Pro 升級可從 settings 發起
- subscription 狀態可正確回寫 workspace
- mock provider 降級為測試/開發用途，非正式主路徑

### Week 3：Onboarding Productization

目標：

- 把 workspace 建立後的核心初始化流程產品化
- 把 bootstrap、service account、API key、first demo run 串成統一流程

完成定義：

- onboarding 可展示真實狀態、下一步與失敗原因
- provisioning / evidence / rollback 至少有產品內狀態承載點

### Week 4：Enterprise Readiness

目標：

- 至少完成一項 enterprise feature
- 補齊監控與 e2e 覆蓋

完成定義：

- `audit export` 或 `SSO` 或 `dedicated environment` 至少一項可交付
- SaaS 關鍵路徑有測試與監控

## 6. 工作流拆解

### 工作流 A：Workspace Session / Context

範圍：

- `web/lib/workspace-context.ts`
- `web/app/api/workspace-context/route.ts`
- `web/components/topbar.tsx`
- `web/components/onboarding/workspace-onboarding-wizard.tsx`

目標：

- 讓 metadata-backed context 與 fallback mode 有明確邊界
- local/dev mode 可以保留，但不能偽裝成真實 SaaS session

待辦：

- 統一 context source label 與語意
- 明確標記哪些情況屬於 local/dev fallback
- 優化 workspace context API 回應，方便前端顯示真實狀態
- 讓 onboarding 與 topbar 對 fallback 狀態給出更明確提示

驗收：

- 當 source 不是 `metadata` 時，UI 會清楚標示為 fallback
- metadata 可用時，不再退回 preview 模式
- onboarding wizard 透過 `selectWorkspaceContext` helper 保持 workspace selection/fallback 語義，讓 page 端的 context badge 與 handoff copy 與共享 service helper 進度同步。

### 工作流 B：Critical Route Hardening

範圍：

- `web/lib/control-plane-proxy.ts`
- `web/app/api/control-plane/workspace/route.ts`
- `web/app/api/control-plane/me/route.ts`
- `web/app/api/control-plane/members/route.ts`

目標：

- 核心身份與 workspace surface 不再以 preview 資料掩蓋 upstream 問題

待辦：

- 區分「可接受 fallback」與「必須嚴格失敗」的 route
- 對 `workspace`、`me`、`members` 等核心 surface 返回結構化錯誤
- 減少富假資料 fallback

驗收：

- upstream 缺失時，核心 route 會回明確錯誤
- 不再對 `workspace` 與 `me` 返回高擬真 preview 資料

### 工作流 C：Mutation / Detail Proxy Consistency

範圍：

- `web/app/api/control-plane/workspaces/route.ts`
- `web/app/api/control-plane/workspaces/[workspaceId]/bootstrap/route.ts`
- `web/app/api/control-plane/workspace/billing/checkout-sessions/route.ts`
- `web/app/api/control-plane/workspace/billing/checkout-sessions/[sessionId]/route.ts`
- `web/app/api/control-plane/workspace/billing/checkout-sessions/[sessionId]/complete/route.ts`
- `web/app/api/control-plane/workspace/billing/subscription/cancel/route.ts`
- `web/app/api/control-plane/workspace/billing/subscription/resume/route.ts`
- `web/app/api/control-plane/workspace/billing/providers/route.ts`
- `web/app/api/control-plane/workspace/billing/portal-sessions/route.ts`
- `web/app/api/control-plane/workspace/sso/route.ts`
- `web/app/api/control-plane/workspace/dedicated-environment/route.ts`
- `web/app/api/control-plane/workspace/audit-events/export/route.ts`

目標：

- 減少 route wrapper 重複邏輯
- 統一 workspace / subject / roles / tenant headers 的轉發行為

待辦：

- 收斂 `getBaseUrl` / `getAuthenticatedSubject` / `getAuthenticatedRoles` 重複邏輯
- 讓 mutation 與 detail route 行為一致
- 補齊錯誤返回與 no-store 行為一致性

驗收：

- route wrapper 之間 header forwarding 規則一致
- 減少重複樣板碼

### 3.0.7 Route wrapper consistency 進度（2026-04-??）

- 本輪聚焦把 mutation/detail route 內「`getBaseUrl` + `fetch` + `cache: \"no-store\"` + workspace header」的樣板抽往共用 helper，再把該 helper 的 `x-authenticated-*`/`x-workspace-*`/`x-tenant-id` 透傳語義統一。
- 目前 `workspace` / `me` / `members` 等 metadata-only 路由已把 `workspace_context_not_metadata` 412 guard 與 `controlPlaneErrorResponse` contract 鎖定；下一步是讓剩餘 mutation/detail route 也復用這套 helper、並維持 `no-store`/content-type/`idempotency-key`/header 轉發一致後才算真正完成。
- 先行 contract 護欄會在 `web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts` 以及 `web/lib/__tests__/control-plane-proxy.test.ts` 中加入 `workspace_context_not_metadata`、`cache: \"no-store\"` 與 header 一致性的斷言，文檔在 helper 替換完成後再標記為「已完成」，目前以「本輪同步 route wrapper consistency 护栏」口徑描述。
### 3.0.7 Route wrapper consistency 進度（2026-04-*/未完成）

- 本輪聚焦進一步把 `getBaseUrl` + `fetch` + `no-store` + workspace header 的樣板從各個 route 內抽出，直接復用 `proxyControlPlane` / `proxyControlPlaneOrFallback`，並統一 `x-authenticated-*`/`x-workspace-*`/`x-tenant-id` 的轉發行為。
- 目前 metadata-only 路由（例如 `workspace`/`me`/`members`）已把 `workspace_context_not_metadata` 412 guard 和 `controlPlaneErrorResponse` 香度口徑寫死，還需要讓其余 mutation/detail route 也共享 helper 才算完成。
- 先行 contract 护栏會在 `web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts` 及全局 proxy test 中加強 `workspace_context_not_metadata` 412 以及 `cache: "no-store"` / header 一致性驗證，並於後續再把文檔中這段進展標記為“已完成”。
- 最新一輪已把 `web/tests/route-wrapper-consistency.test.ts` 再往前推到 `workspaces` 與 `workspaces/[workspaceId]/bootstrap`：除了既有 mutation/detail route 的 `proxyControlPlane` / `POST` / `idempotency-key` contract 外，現在也會驗證 workspace create 的 `includeTenant:false + content-type/body passthrough`，以及 bootstrap route 的 forwarded auth headers（`x-authenticated-*` 優先、`cf-access-*` 回退）、`x-workspace-id` / 條件式 `x-workspace-slug` / `x-tenant-id` 注入與空 `workspaceId` 400 guard。這讓 WorkFlow C 在 helper 化尚未完全完成前，至少已有一層更接近真實 wrapper 邊界的 source-contract 回歸網。
- 本輪又把這條線再前推一步：`web/app/api/control-plane/workspaces/route.ts` 與 `web/app/api/control-plane/workspaces/[workspaceId]/bootstrap/route.ts` 已開始共用新抽出的 `web/app/api/control-plane/workspaces/route-helpers.ts`，其中 `buildWorkspaceCreateProxyInit` / `buildWorkspaceBootstrapProxyInit` / `buildForwardedAuthHeaders` 把 POST body、`content-type`、`idempotency-key`、forwarded auth headers 與條件式 workspace/tenant header 注入收斂為可直接執行的純 helper；對應的 `web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts` 已把這些 helper 的 runtime 語義鎖進 `web:test:contract`。
- 同時，`web/tests/route-wrapper-consistency.test.ts` 也開始把尚未 helper 化的 legacy direct-fetch POST wrapper 納入 `W12-C11`：`invitations`、`invitations:accept`、`service-accounts`、`api-keys`、`tool-providers`、`runs` 現在都會被檢查 `getBaseUrl` 503 guard、`await request.text()` body passthrough、`accept/content-type/idempotency-key`、workspace/auth headers 與 `cache:"no-store"` / upstream `content-type` passthrough，減少這批 route 在尚未重構前悄悄漂移的風險。
- `W12-C11` 的 fallback wrapper 也補進來了：`workspace/delivery` 現在會被固定 `GET` 的 `404/503 -> buildFallbackTrack` 預覽回退語義與 `POST` 的 `body.length === 0 ? undefined : body` passthrough，`admin/overview` 也會被固定 `includeTenant:false` 與 `404/503 -> preview summary` 的 fallback contract。這讓 route wrapper consistency 不再只盯 mutation path，也把兩個關鍵 fallback surface 一併拉進回歸網。
- 本輪再往前推了一層真正的 helper 化：新增 `web/app/api/control-plane/post-route-helpers.ts`，把 `CONTROL_PLANE_BASE_URL` 解析、`control_plane_base_missing` 503 回應、workspace-scoped/authenticated POST headers（含 `accept` / `content-type` / `idempotency-key` / auth/workspace/tenant headers）與 upstream response passthrough 收斂為共用 helper；`invitations`、`service-accounts`、`api-keys`、`tool-providers`、`runs`、`invitations:accept` 的 POST wrapper 已改為復用這層 helper，讓 `W12-C11` 開始從「只靠 contract 守住」前進到「實際減少 route 內樣板碼」。
- 最新一輪又補上第二層 helper 收斂：`post-route-helpers.ts` 新增 `buildProxyControlPlanePostInit(...)`，把 `proxyControlPlane(...)` 路由常見的 `POST + application/json + idempotency-key + body passthrough` init 組裝收成純 helper；`invitations/[invitationId]:revoke`、`api-keys/[apiKeyId]:revoke|rotate`、`service-accounts/[serviceAccountId]:disable`、`tool-providers/[toolProviderId]`、`tool-providers/[toolProviderId]:disable` 已切到這套 helper，`web/tests/route-wrapper-consistency.test.ts` 也同步改成固定「helper import/usage + 無 direct fetch/headers 樣板」契約。
- 本輪再把 metadata-guarded enterprise/write 與 fallback POST wrapper 也一起收進來：`workspace/sso`、`workspace/dedicated-environment` 目前都改為在保留 `requireMetadataWorkspaceContext(...)` guard、upstream path 與 request `accept/content-type` 來源語義的前提下，直接復用 `buildProxyControlPlanePostInit(...)`；`workspace/delivery POST` 也改為復用同一套 helper，保留 `includeTenant:true`、`content-type: application/json` 與空 body -> `undefined` 的語義。對應的 `web/lib/__tests__/enterprise-surface-routes.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這批 enterprise/delivery wrapper 的 helper import/usage 契約。這讓 `W12-C11` 從 collection/detail mutation wrapper 再擴到 metadata-guarded enterprise POST wrapper 與 delivery POST；整體狀態仍保守維持 `in_progress`，因為 route family 雖已大幅收斂，但尚未宣告所有 wrapper 完全歸一。
- 本輪又把 collection POST wrapper 再往上收了一層：`post-route-helpers.ts` 新增 `proxyWorkspaceScopedPostRequest(...)` 與 `proxyAuthenticatedPostRequest(...)` 兩個高階代理 helper，把 `getControlPlaneBaseUrl` 503 guard、`await request.text()`、workspace/authenticated headers 組裝與 `proxyControlPlanePost(...)` 串接一起內聚；`invitations`、`service-accounts`、`api-keys`、`tool-providers`、`runs`、`invitations:accept` 現在都直接復用這層 helper。`runs` 仍保留 request-derived `content-type ?? "application/json"`，`invitations:accept` 仍保留 `headers()` 推導 subject/roles 的 fallback 語義，但 route 內的 baseUrl/body/header 樣板已被移除。對應的 `web/app/api/control-plane/__tests__/post-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這批 collection helper 的 runtime/source contract。這讓 `W12-C11` 的 collection POST wrapper 從「共用 headers helper」進一步提升為「直接共用 request -> upstream 代理 helper」。
- 最新一輪也順手把 billing POST init 再收了一層：`web/app/api/control-plane/workspace/billing/route-helpers.ts` 的 `buildBillingPostProxyInit(...)` 已改為直接復用共享 `buildProxyControlPlanePostInit(...)`，不再維護另一套 billing 專屬的 `content-type` / `idempotency-key` / body 樣板；對應的 `web/app/api/control-plane/__tests__/billing-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/billing-routes.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper 委派契約。這一段仍維持「局部 helper family 收口」口徑，不把整體 `W12-C11` 提前寫成完成。
- 最新一輪也把 run detail GET wrapper 往前收了一層：`web/app/api/control-plane/runs/route-helpers.ts` 新增 `buildRunPath(...)` 與 `proxyRunDetailRequest(...)`，把 `runs/[runId]`、`runs/[runId]/graph`、`runs/[runId]/events`、`runs/[runId]/artifacts` 這四條 detail route 的 upstream path 與 query passthrough 收斂為共享 helper；對應的 `web/app/api/control-plane/__tests__/run-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper import/usage 契約。這讓 `W12-C11` 從 POST/detail mutation 再向 GET detail family 推進，但整體狀態仍保守維持 `in_progress`。
- 本輪再把 metadata-only GET family 收了一層：`web/app/api/control-plane/get-route-helpers.ts` 新增 `proxyMetadataGet(...)`，把 `workspace`、`me`、`members` 三條 metadata-backed GET route 的 `resolveWorkspaceContextForServer(...)`、`requireMetadataWorkspaceContext(...)` 與 `proxyControlPlane(...)` 樣板收進共享 helper；對應的 `web/app/api/control-plane/__tests__/metadata-get-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts`、`web/lib/__tests__/workspace-context-metadata-guard.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步切到 helper 契約。這讓 `W12-C11` 不再只覆蓋 mutation/detail，也開始把 metadata-only GET family 納入實際 helper 化範圍。
- 本輪也把 fallback GET family 往前收了一層：`web/app/api/control-plane/fallback-route-helpers.ts` 新增 `proxyFallbackGet(...)`，把 `admin/overview` 與 `workspace/delivery GET` 的 `404/503 -> preview` 語義、`includeTenant` 邊界與 fallback meta 收進共享 helper，同時保留既有 preview payload 形狀不變；對應的 `web/app/api/control-plane/__tests__/fallback-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已把這層 helper import/usage 鎖進 contract 回歸網。
- 最新一輪又把 `workspace/delivery` 的 route proxy wiring 再往前收一層：新增 `web/app/api/control-plane/workspace/delivery/route-helpers.ts`，把 `buildDeliveryPath(...)`、preview fallback track/meta 組裝、`proxyFallbackGet(...)` 的 `includeTenant:true` GET wiring，以及 `buildProxyControlPlanePostInit(...)` 的 JSON POST passthrough 一起收進共享 helper；`web/app/api/control-plane/workspace/delivery/route.ts` 因此只保留 route 轉接，不再內嵌 workspace-context 與 proxy init 樣板。對應的 `web/app/api/control-plane/__tests__/delivery-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper 的 runtime/source 契約。這一段仍屬 `W12-C11` 的局部 helper family 收口，不把整體狀態提前寫成完成。
- 最新一輪也把 `workspace/billing` helper family 的可注入 contract 補齊：`web/app/api/control-plane/workspace/billing/route-helpers.ts` 的 `proxyWorkspaceBillingGet(...)` / `proxyWorkspaceBillingPost(...)` 現在都可顯式注入 `resolveWorkspaceContext`、`proxy`，而 POST 也可注入 `initBuilder`，使 billing family 與 `delivery`、`workspaces bootstrap`、`tool-providers` 這批較新 helper 的測試/依賴注入形狀保持一致。對應的 `web/app/api/control-plane/__tests__/billing-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步把 workspace-scoped path、GET/POST init wiring 與 shared helper source contract 固定下來。這讓 `W12-C11` 在 billing family 上也少一塊舊式直接 context/proxy 調用的例外，但整體口徑仍保守維持 `in_progress`。
- 最新一輪也把 enterprise route helper family 的 runtime 注入 contract 補齊：`web/app/api/control-plane/workspace/route-helpers.ts` 的 `proxyWorkspaceEnterpriseGet(...)` / `proxyWorkspaceEnterprisePost(...)` 現在都可顯式注入 `resolveWorkspaceContext`、`proxy`，而 POST 也可注入 `initBuilder`，同時保留 `requireMetadataWorkspaceContext(...)` 這條 controlled live-write guard。對應的 `web/app/api/control-plane/__tests__/workspace-enterprise-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步把 query passthrough、metadata guard 412、workspace-scoped path 與 injected GET/POST init wiring 固定下來。這讓 `W12-C11` 在 enterprise helper family 上也少一塊只能靠 source-contract 驗證的舊式閉包 resolver 區域，但整體口徑仍保守維持 `in_progress`。
- `workspaces` 這條線也再收了一層：`web/app/api/control-plane/workspaces/route-helpers.ts` 內的 `buildWorkspaceCreateProxyInit(...)` / `buildWorkspaceBootstrapProxyInit(...)` 現在都改為站在共享 `buildProxyControlPlanePostInit(...)` 之上，再疊加 forwarded auth 與條件式 workspace/tenant header 注入，而不是維護另一套獨立的 POST body/idempotency 組裝。對應的 `web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts` 已同步固定這層 runtime contract。整體口徑仍維持 `W12-C11 in_progress`，不把整個 wrapper family 提前寫成完成。
- 最新一輪再把 `workspaces` family 的 proxy wiring 往前收了一層：`web/app/api/control-plane/workspaces/route-helpers.ts` 新增 `proxyWorkspaceCreatePost(...)`、`proxyWorkspaceBootstrapPost(...)` 與 `buildWorkspaceBootstrapPath(...)`，把 `workspaces` create / bootstrap 兩條 route 內重複的 `proxyControlPlane(... includeTenant:false + init builder)` 樣板進一步內聚到 helper；對應的 `web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 也同步固定 route 端只保留參數驗證與 context 取得、helper 端維持 `includeTenant:false` / bootstrap path wiring 的 source contract。這讓 `W12-C11` 從「共享 POST init builder」再向「共享 route proxy wiring」前進一小步，但整體狀態仍保守維持 `in_progress`。
- 最新一輪也把 `workspaces/[workspaceId]/bootstrap` route 端最後殘留的 workspace-context 解析收回 helper：`proxyWorkspaceBootstrapPost(...)` 現在可在未顯式提供 `currentWorkspace` 時自行透過 `resolveWorkspaceContextForServer(...)` 取得當前 workspace，再組裝 bootstrap forwarding headers，因此 route 本身只保留 `workspaceId` 驗證與 helper 委派。對應的 `web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這個「route 只驗證參數、helper 內聚 context 解析 + includeTenant:false proxy wiring」的新 contract。這讓 `W12-C11` 在 `workspaces` family 上又少了一個 route-level context exception，整體口徑仍保守維持 `in_progress`。
- 最新一輪也把 workspace-scoped collection GET fallback family 收了一層：新增 `web/app/api/control-plane/collection-route-helpers.ts`，把 `buildWorkspaceCollectionPath(...)` 與 `proxyWorkspaceScopedCollectionGet(...)` 抽成共享 helper，讓 `invitations`、`service-accounts`、`api-keys` 三條 collection route 不再各自維護 `resolveWorkspaceContextForServer(...) + proxyControlPlaneOrFallback(...)` 樣板；對應的 `web/app/api/control-plane/__tests__/collection-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 也同步固定這層 helper 的 path normalization、fallback passthrough 與 route import/usage 契約。這讓 `W12-C11` 再從 POST/detail/metadata helper 延伸到 workspace-scoped collection GET fallback family，但整體口徑仍維持 `in_progress`。
- 最新一輪也把 workspace-scoped collection POST family 收了一層：`web/app/api/control-plane/collection-route-helpers.ts` 新增 `proxyWorkspaceScopedCollectionPost(...)`，讓 `invitations`、`service-accounts`、`api-keys` 三條 route 不再各自維護 `resolveWorkspaceContextForServer(...) + build workspace-scoped path + proxyWorkspaceScopedPostRequest(...)` 樣板，而是直接委派共享 helper；對應的 `web/app/api/control-plane/__tests__/collection-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper 的 workspace-context 解析、path 組裝與 route import/usage 契約。這讓 `W12-C11` 從 collection GET 再往前收一小層到 collection POST family，但整體狀態仍保守維持 `in_progress`。
- 最新一輪也把 `tool-providers / runs` 的 path-based collection POST family 收了一層：`web/app/api/control-plane/collection-route-helpers.ts` 新增 `proxyWorkspaceContextCollectionPost(...)`，讓 `tool-providers` 與 `runs` 這兩條 route 不再各自維護 `resolveWorkspaceContextForServer(...) + proxyWorkspaceScopedPostRequest(...)` 樣板，而是直接委派共享 helper，由 helper 統一處理 workspace-context 解析與 path-based POST 代理；對應的 `web/app/api/control-plane/__tests__/collection-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper 的 runtime/source contract。這讓 `W12-C11` 從 workspace-scoped collection POST 再往前延伸到相鄰的 path-based collection POST family，但整體口徑仍保守維持 `in_progress`。
- 最新一輪也把 non-workspace-path collection GET fallback family 收了一層：`web/app/api/control-plane/collection-route-helpers.ts` 新增 `proxyPathCollectionGet(...)`，讓 `tool-providers` 與 `policies` 兩條 route 不再各自維護 `proxyControlPlaneOrFallback(...)` 樣板，而是改由 helper 統一處理 path-based fallback GET 代理；對應的 `web/app/api/control-plane/__tests__/collection-route-helpers.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 helper 的 runtime/source contract。這讓 `W12-C11` 從 workspace-scoped collection family 再往旁邊收一小層到 system collection GET fallback，但整體口徑仍保守維持 `in_progress`。
- 最新一輪再把最小 system GET proxy 也收進 shared helper family：新增 `web/app/api/control-plane/system-route-helpers.ts`，把 `health` route 的 `/api/v1/health + includeTenant:false` 代理收斂成 `proxyHealthGet(...)`，讓 route 端只保留轉接、不再直接內嵌 `proxyControlPlane(...)`。對應的 `web/app/api/control-plane/__tests__/system-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/system-routes.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已同步固定這層 system helper 的 path / includeTenant / route import contract。這讓 `W12-C11` 再把最小 system GET proxy 也拉進同一個 helper family，但整體狀態仍保守維持 `in_progress`。
- 本輪也補了一層 system wrapper source-contract：`web/app/api/control-plane/__tests__/system-routes.test.ts` 現在固定 `health` route 的 `/api/v1/health + includeTenant:false` 與 `policies` route 的 preview fallback payload（`previewPolicies` + `page_info.next_cursor:null`），讓非 workspace wrapper 不再完全游離於主要回歸網之外；這一段偏向獨立 contract 加固，不單獨把更大 SaaS 里程碑寫成完成。
- 本輪也把 service 層與 settings 頁面再收一層：`web/services/control-plane.ts` 引進 `buildEnterpriseFallbackMeta` / `getEnterpriseFallbackUpgradeHref` / `getEnterpriseFallbackPlanCode` 等 helper，用以標準化 SSO / dedicated environment 在 `409 + workspace_feature_unavailable`、`503 + control_plane_base_missing` 與 `generic request failure` 的 contract metadata、upgrade href 與 plan code，並讓 `saveWorkspaceSsoReadiness` / `saveWorkspaceDedicatedEnvironmentReadiness` 回傳 normalized readiness、拋出結構化 `ControlPlaneRequestError` 失敗，再加上 `downloadWorkspaceAuditExportViewModel` 對 `contract_meta.source` 與 `issue` 的欄位補齊，使 `settings` 頁面能直接呼叫這些 service helper 而不再自己寫 raw `fetch`。對應的 page-level 與 non-browser smoke 測試（`web/tests/page-level/settings-enterprise-write.page.test.ts`、`web/tests/page-level/settings-saved-refresh-coupling.page.test.ts`、`web/tests/e2e/saas-mainline-smoke.e2e.test.ts`）同步加入 `saveWorkspace...` 的 import/usage、upgrade card 文字與 dedicated upgrade href 的斷言，進一步把 enterprise live-write contract 與 fallback guidance 鎖進 W12-C11 的 helper化進度。
- `admin overview` 也補進了明確的 `contract_meta` 呈現：`web/services/control-plane.ts` 現在會把 live/fallback admin summary 正規化為 `contract_meta.source + issue`，`web/components/admin/admin-overview-panel.tsx` 會顯式展示 live contract 與 preview fallback badge/說明，避免 admin snapshot 在 fallback 情況下仍被誤讀為真實 readiness；對應的 `web/tests/page-level/admin-overview-handoff.page.test.ts` 已把這層 source/page contract 固定下來。
- `delivery track` 這個獨立 surface 也補進了同一套 live vs preview/fallback source contract：`web/app/api/control-plane/workspace/delivery/route.ts` 的 fallback preview 現會明確帶出 `contract_meta`，`web/services/control-plane.ts` 的 `fetch/saveWorkspaceDeliveryTrack(...)` 會在 live/fallback 兩路都正規化 `contract_meta.source`，`web/components/delivery/workspace-delivery-track-panel.tsx` 也會顯式展示 source badge 與 guidance copy，避免 delivery evidence panel 在 fallback 情況下仍被誤讀為 live 狀態；對應的 `web/app/api/control-plane/__tests__/fallback-route-helpers.test.ts`、`web/lib/__tests__/control-plane-service.test.ts` 與 `web/tests/page-level/delivery-track-context-handoff.page.test.ts` 已補齊這層 contract 回歸網。
- 本輪再往前收了一步：`admin / delivery / settings` 三個 surface 的 fallback source 語義已從單一的 `preview/fallback` 進一步細分為可區分的 `404 / 503 / 409` UI contract，讓頁面能把 `route unavailable`、`control plane unavailable` 與 plan-gated 情境拆開呈現；這仍是收口中的前端 contract 進展，用來降低誤讀與誤導，不代表這三個 surface 已經可被寫成 fully GA。
- `web/tests/e2e/saas-mainline-smoke.e2e.test.ts` 本輪也再補了一條 settings enterprise live-write error smoke：現在會顯式用 non-browser execution 驗證 `saveWorkspaceSsoReadiness(...)` 的 `idempotency_conflict` 與 `saveWorkspaceDedicatedEnvironmentReadiness(...)` 的 `401/403` 類 owner/admin access failure，並以 source-assisted 方式固定 `formatEnterpriseWriteError(...)` 的對應 copy。這讓 settings 的 enterprise submit flow 不只守 success/fallback/handoff，也把兩條最容易在 rollout 過程中漂移的 failure semantics 拉進主 smoke 網，但仍維持 non-browser 邊界，不宣告完整 browser e2e。

驗收：

- route wrapper 之間 header forwarding 規則一致
- 減少重複樣板碼

### 工作流 D：Billing Productionization

範圍：

- `src/lib/billing-providers.ts`
- `src/app.ts` billing routes / webhook handling
- `web/services/control-plane.ts`
- `web/components/settings/workspace-settings-panel.tsx`
- `web/components/usage/workspace-usage-dashboard.tsx`

目標：

- 把 billing 從 scaffold 推到可試運行主路徑

本輪已完成：

- `resolveWorkspaceCheckoutProvider` 依照自助 provider 優先級（Stripe > 其他），只有在 `allowMockCheckout` 顯式打開時才回退到 `mock_checkout`，預設無 Stripe 時直接回傳 `null` 以觸發 `billing_self_serve_not_configured`。這讓 production 自助升級明確依靠 Stripe，而 mock checkout 保留在 local/staging/test 或實驗性測試場景。
- `src/app.ts` 的 billing summary 與 checkout session 建立流程同步讀取 `BILLING_SELF_SERVE_PROVIDER`、產生 `configuredSelfServeProvider` / `allowMockCheckout`，並在沒有正式 provider 時直接回錯與文案區分 ready/staged 兩種狀態。
- 設定與 usage 頁面文案已把 mock checkout 明確標示為 test-only fallback、同時推薦生產環境以 `stripe` 為 self-serve provider，並由新的 page-level tests（`web/tests/page-level/billing-self-serve-provider.page.test.ts`）把這個語義鎖死在 source/page contract 層，避免 UI 內容或文檔回退。
  - billing summary 的 `upgrade / manage-plan / resolve-billing` 關鍵 action 文案與 CTA 已由 page/source contract 補一層護欄，確保 Stripe-first、past-due warning、trial/cancelled fallback 等語義不會悄悄漂移。
- billing route wrappers 與 service 請求層已新增 source-contract + non-browser smoke：`checkout-sessions` / `providers` / `portal-sessions` / `subscription:cancel|resume` 的 method/path/idempotency semantics、以及 `billing_self_serve_not_configured` / `billing_provider_portal_unavailable` 這類結構化錯誤，現在都會在 `web:test:contract` / `web:test:e2e` 內被驗證；`web/lib/__tests__/control-plane-service.test.ts` 補的 portal-unavailable 錯誤 guard 會把 `status/code/message/details` 串成 `ControlPlaneRequestError` 往上報，降低錯誤語義回退風險。
- `web/tests/e2e/billing-mainline-smoke.e2e.test.ts` 也進一步把 `billing_subscription_plan_unavailable` / `billing_subscription_not_paid` 拉進 non-browser smoke：現在 portal session、cancel 與 resume 在 plan 不可用或非付費訂閱場景下，會顯式核對 `status/code/message`，避免 settings 端已經依賴的錯誤分支只剩 page/source contract 在守。
- 同一個 `billing-mainline-smoke` 再補了一條 portal healthy-path smoke：以 mock Stripe portal response 驅動 `billing_provider / portal_url / return_url` 的正向 payload，讓 billing portal 在非錯誤通路也有 non-browser smoke 的健康性證據；這仍然基於 `mock_checkout` 的 test-only fallback，並不暗示 Stripe portal 已全面 GA。
- 同一個 smoke 檔也再補了 `billing_subscription_not_cancellable`：現在 cancel action 在 non-browser smoke 層會顯式驗證 `status=409`、`code=billing_subscription_not_cancellable` 與空 `details` 的結構化錯誤行為，讓 settings 的 cancel/resume error narrative 不只依賴 page/source contract。
- billing route wrapper contract 已再補一層 runtime guard：`billing-routes.test.ts` 確認所有 GET route 只透過 `proxyControlPlane` 且不帶 body/idempotency，而 POST route 則只能呼叫 proxy helper、不得自行 `fetch`/`getBaseUrl`，確保 header/`cache:no-store`/body passthrough 這些 runtime 行為不被繞過。新抽出的 `buildBillingGetProxyInit` / `buildBillingPostProxyInit` 已把 POST 的 `content-type`、`idempotency-key` 與 `await request.text()` 封成共用 helper，便於讓 `test:contract` 能直接驗證這些通用選項。
  - `proxyControlPlane` 的 header merge 也已收斂為可測的純函數：`buildProxyControlPlaneHeaders` 現在會在缺省時注入 `x-authenticated-*` / `x-workspace-*` / `x-tenant-id`，同時保留 caller 顯式傳入的身份/workspace/tenant header，不再默默覆寫上游已明確設定的 access-control 語義；對應單元測試已納入 `web:test:unit`。
- Stripe checkout completion / webhook / portal 的邊界已再補一層 page/source contract：非 `mock_checkout` session 會固定回 `billing_checkout_completion_deferred`、Stripe webhook 的 payload normalization 與缺失 checkout session error 不會漂移、`settings` 也會持續維持 `resolve-billing`、portal return 與 local renewal fallback 的分流文案；新增的 page-level tests `web/tests/page-level/billing-summary-actions.page.test.ts`、`web/tests/page-level/settings-past-due-flow.page.test.ts`、`web/tests/page-level/billing-webhook-portal-contract.page.test.ts`、`web/tests/page-level/billing-portal-return-webhook-normalization.page.test.ts`、`web/tests/page-level/settings-billing-portal-return-evidence.page.test.ts`、`web/tests/page-level/settings-billing-action-error.page.test.ts` 會把 billing summary CTA（upgrade/manage-plan/resolve-billing）、past-due notice+refresh/intent、portal-return notice -> evidence handoff、webhook payload、portal return fallback（explicit -> env `STRIPE_CUSTOMER_PORTAL_RETURN_URL` -> `buildAbsoluteBillingManagementUrl`）、action error narratives，以及 `normalizeIncomingBillingWebhookRequest` 的 `cancel_at_period_end` spread normalization 一起鎖死；`web/tests/page-level/settings-checkout-refresh.page.test.ts` 也確保 refresh notice/flag 的 reset 由 page/source contract 保護，`web/lib/__tests__/control-plane-service.test.ts` 的 portal session payload test 也確保 `createBillingPortalSession` 會把 `return_url` 直接作為 JSON payload 送到 `/api/control-plane/workspace/billing/portal-sessions`，避免 manage-plan/resolve-billing return intent 在 service 層跑掉。
  - 同一檔案又補上了 `billing_provider_portal_unimplemented`、`billing_subscription_managed_by_provider`、`billing_subscription_not_resumable` 三個案例的 `ControlPlaneRequestError` 封裝，`test:unit` 入口的 service contract 現在能快速驗證 provider-managed、不可取消、不可恢復這類真實阻斷語義，讓 settings human-friendly 關鍵提示不會悄悄漂移。
  - `settings` 端對訂閱操作錯誤的文案也同步更新：新增 `formatSubscriptionActionError`，把 portal-managed、not_cancellable、not_resumable、subscription_missing、not_paid、plan_unavailable 等錯誤映射成穩定可理解的提示，並用 `settings-billing-action-error.page.test.ts` 與 `settings-billing-subscription-action-feedback.page.test.ts` 鎖死這層感知回饋與 Week 8/evidence handoff 語義。
  - provider return intent contract 塑造了 `billing-provider-return-intent.page.test.ts`：從 `src/app.ts` 的 `returnIntent`/`manage_plan_href` 開始，檢查 `settings` 的 `manage-plan` / `resolve-billing` intent card、portal follow-up copy 與 onboard/verification handoff 的交互路徑，確保 backend 決定的 `return_intent` 與前端的 evidence handoff 導流連在一起。
- `web/tests/e2e/billing-stripe-flow-smoke.e2e.test.ts` 已把 Stripe checkout create/fetch/complete lifecycle 的 upgrade->manage-plan 語義、Stripe-completed checkout 後的 active/manage-plan summary，以及 `past_due -> resolve-billing -> portal return` 的 return URL / intent 一起放進 non-browser smoke 裡核對，新增的 `checkout.session.completed` guard 也把 workspace/client_reference_id 與外部 refs 的 fallback 直接對照；這仍是非完整 upstream/browser e2e 的 smoke。
- 同一條 billing smoke 本輪也再往更像真實 Stripe upstream response 推了一小步：`web/tests/e2e/billing-stripe-flow-smoke.e2e.test.ts` 現在已補進 `createBillingPortalSession(...)` 的 execution，固定 `return_url` passthrough 與 Stripe-like portal response shape；`web/tests/page-level/billing-webhook-portal-contract.page.test.ts` 也把 `checkout.session.completed` 的 `metadata` object guard 一起鎖進 source contract，`web/tests/page-level/billing-provider-return-intent.page.test.ts` 進一步固定 settings 端會把 `window.location.href` 透過 `createBillingPortalSession(...)` 帶回 backend，讓 portal return intent 的 contract 更完整。
- billing smoke 已從單純 request/service 語義前推到 source-assisted flow，但驗收邊界仍停留在 non-browser smoke + page/source contract，尚未宣稱完整 upstream/browser e2e 或 billing GA。
- 最新一輪也把 `web/tests/e2e/billing-mainline-smoke.e2e.test.ts` 再補了一條 provider-managed resume error smoke：`resumeBillingSubscription` 現在會在 non-browser smoke 層顯式核對 `billing_subscription_managed_by_provider` 的 `status/code/details.billing_provider/details.manage_plan_href`，讓 cancel 與 resume 在 provider-managed 場景下都保有結構化錯誤語義護欄。
- 執行 plan 與環境配置文檔已同步把 `mock_checkout` 约束為 testing-only，強調 production self-serve 仍以 Stripe 為推薦配置，並提醒驗收仍以 page/source contract + non-browser smoke 為主，不等同完整 browser e2e 或 billing GA。
- 相關文檔與測試計劃已同步記錄上述 guard，提醒團隊 `mock_checkout` 只屬於 testing fallback、production self-serve 仍指向 Stripe，驗收也集中在 page/source contract + non-browser smoke 層級。

仍待完成：

- 跑通更接近真實 Stripe upstream response 的 checkout / webhook / portal smoke/contract，確保 self-serve 入口對應的 plan 變更穩定。
- 將 billing source-contract / smoke 範圍擴展到更多 UI 路徑，並逐步從目前的 request/service smoke 前進到更貼近真實 Stripe upstream response 的 flow smoke。
- 保持對新 page-level tests 的監控，讓 source/page contract 在未來的 `npm run web:test:page`/`web:test` 執行中持續驗證 `mock_checkout` test-only 的語義。

驗收：

- 能從 UI 發起升級並正確完成 plan 變更，且 `billing_self_serve_not_configured` 在無正式 provider 時依舊會被回傳。
- billing 異常流有明確狀態，同時驗收邊界保持在 page/source contract 與 non-browser smoke，還未宣稱完整 browser e2e 或 billing GA 已落地。

### 工作流 E：Onboarding Productization

範圍：

- `web/components/onboarding/*`
- `web/components/home/workspace-launchpad.tsx`
- `web/components/service-accounts/*`
- `web/components/api-keys/*`
- `web/components/playground/*`
- `src/app.ts` onboarding summary / bootstrap / delivery tracking logic

目標：

- 把 onboarding 從 script-assisted flow 推進到產品內 state machine

待辦：

- 讓 onboarding checklist 完全對應真實後端狀態
- 補一層更接近真實操作時序的 browser e2e；當前仍以 page/source contract + non-browser smoke 為主

本輪已完成補強：

- onboarding wizard 已顯式接入 `latest_demo_run`、`latest_demo_run_hint`、`blockers`、`recommended_next`、`delivery_guidance`
- `workspace-onboarding-wizard` 也進一步把 `recommended_next`、`blockers`、`latest_demo_run_hint` 與 `delivery_guidance` 收斂成統一的 guided recovery lane：優先導向 primary blocker，其次才是 demo run monitor/retry、verification evidence 與 go-live drill；對應的 `web/tests/page-level/onboarding-first-demo.page.test.ts` 與 `web/tests/page-level/onboarding-playground-usage-handoff.page.test.ts` 會固定 status badge、recommended-next CTA、delivery guidance copy 與 shared handoff continuity，避免 onboarding 又退回 wizard-only heuristics。
- `workspace-launchpad`、`playground` 與 `usage` 也開始直接消費同一組 onboarding signals：launchpad 新增 `Onboarding recovery lane`，會根據 `latest_demo_run_hint` / `delivery_guidance` / `recommended_next` 切換主要 CTA；`playground` 的 handoff guide 現在會在 demo attention、verification 與 go-live 之間切換 action surface，且 `verification/go-live` 明確維持 explicit `surface` query；`usage` 的 `Governed first demo signal` 也會把 `latest_demo_run_hint` 與 `delivery_guidance` 收進 callout/meta lines。對應的 `web/components/__tests__/handoff-query-surface-c.test.ts`、`web/components/__tests__/handoff-query-surface-d.test.ts`、`web/tests/page-level/onboarding-playground-usage-handoff.page.test.ts` 與 `web/tests/e2e/saas-mainline-smoke.e2e.test.ts` 已把這批 source/page/smoke contract 固定下來。
- onboarding create 成功路徑已抽出 `selectWorkspaceContext(...)` 共用 helper，workspace 建立後改走共享 workspace-context selection contract，不再在頁面內保留獨立 raw `fetch`；`web/tests/page-level/onboarding-workspace-context-contract.page.test.ts` 會固定 `create -> context-switch -> invalidate -> refresh` 的最小時序。
- `playground` / `verification` 已補 first-demo recovery/evidence lane，失敗、進行中、成功三態均有對應 CTA
- `verification/go-live` handoff 已統一維持 explicit `surface`：
  - `/verification?surface=verification`
  - `/go-live?surface=go_live`
- `service-accounts` / `invitations` / `api-keys` 的 create-action 表單已改為直接承接 service 層的結構化錯誤語義，避免前端保留另一套 generic copy；對應的 `web/tests/page-level/access-forms-error-contract.page.test.ts` 與 `web/tests/page-level/api-keys-form-error-contract.page.test.ts` 已納入 `test:page`。
- `service-accounts` disable 與 `invitations` revoke 的 detail-action 也已補齊結構化錯誤 copy，對應 `web/tests/page-level/service-accounts-panel-error-contract.page.test.ts` 與 `web/tests/page-level/invitations-panel-error-contract.page.test.ts` 已納入 `test:page`，讓 create-action / detail-action 的 error contract 維持一致。
- `tool-provider-list` 的 create/enable/disable 仍透過 service helper 與 `plan_limit_exceeded` metadata 對齊，並由 `web/tests/page-level/tool-provider-list-contract.page.test.ts` 固定 helper import 與 plan-limit copy。
- `workspace-context` route 在 fallback source 帶 warning 時，現也會同步寫出 `x-govrail-workspace-context-warning` header，並由 `web/tests/page-level/workspace-context-hardening.page.test.ts` 固定這層 source-contract，讓 topbar warning badge 與 route response contract 口徑保持一致。
- 對應 source/page contract 與 non-browser smoke 已同步更新，first-demo recovery lane 不再依賴舊的 wizard-only 語義

驗收：

- onboarding 頁可作為唯一主要入口
- 建立 workspace 後可持續追蹤整個初始化進度

### 工作流 F：Enterprise Surface

範圍：

- `web/components/settings/*`
- `web/app/api/control-plane/workspace/sso/route.ts`
- `web/app/api/control-plane/workspace/dedicated-environment/route.ts`
- `web/app/api/control-plane/workspace/audit-events/export/route.ts`
- `src/app.ts` 對應 SaaS workspace feature routes

目標：

- 至少將一個 enterprise surface 推到可用

待辦：

- 優先順序建議：`audit export` > `SSO` > `dedicated environment`
- 補 feature gating、權限、錯誤流與 UI 提示

驗收：

- 至少一個能力從 staged/readiness 變成真可交付功能

## 7. 並行開發建議

### 第一波可直接並行

- A：Workspace Session / Context
- B：Critical Route Hardening
- C：Mutation / Detail Proxy Consistency

理由：

- 三條線檔案集合基本可分離
- 都屬於 Week 1
- 完成後可直接降低後續 billing / onboarding 誤判

### 第二波適合在 Week 1 收尾後並行

- D：Billing Productionization
- E：Onboarding Productization

理由：

- 兩者都依賴更穩定的 session / proxy / route surface
- 但檔案層面仍可較好拆分

### 第三波

- F：Enterprise Surface
- e2e / observability / docs sync

### 下一波並行槽位建議

- 槽位 1：enterprise backend
  - 對齊 `src/app.ts` enterprise 相關 workspace feature routes，優先收口 `audit export`、`SSO`、`dedicated environment` 的後端可交付語義與錯誤流。
- 槽位 2：enterprise frontend
  - 對齊 `web/components/settings/*` 與 `web/services/control-plane.ts`，補齊 enterprise 能力的狀態呈現、引導文案、權限提示與操作閉環。
- 槽位 3：tests-observability
  - 補 SaaS 關鍵路徑 e2e、核心 mutation/route smoke 與 enterprise 能力監控口徑，確保「可用」有可回歸驗證與可觀測證據。
- 槽位 4：docs-runbook
  - 同步執行文檔、運維手冊與交付說明，沉澱 enterprise 能力的啟用條件、故障處理與回退流程。

### 下一輪實際排程（本輪拆解）

- 槽位 1：A2 page-level validation（Topbar / Members）
- 槽位 2：B2 enterprise backend contract deepen（SSO 多域名 + dedicated intake 回顯）
- 槽位 3：C2 enterprise settings/service 對齊
- 槽位 4：D2 docs/tests 同步

### 下一輪實際排程（預同步）

- 槽位 1：B3 enterprise dedicated structured round-trip（`requester_email` / `data_classification`）
  - 目標：補齊 dedicated intake 字段在 backend 的 `request -> normalize -> save -> parse -> readiness` 全鏈路。
  - 當前狀態：`completed`（`requester_email` / `data_classification` 已與 `requested_capacity` / `requested_sla` 一起完成 structured round-trip 與回顯收口）。
- 槽位 2：C3 enterprise contract tests 補強
  - 目標：新增 enterprise field round-trip 的最小 contract/source 護欄，覆蓋 SSO 與 dedicated 的新增字段語義。
  - 當前狀態：`completed`（已補齊 enterprise field round-trip 護欄並納入現有 `test:contract`）。
- 槽位 3：T3 e2e 後置
  - 目標：維持 `unit -> contract -> page` 作為當前主回歸網，`e2e` 在執行基座穩定後再引入。
  - 當前狀態：`in_progress`（`web:test:e2e` 與最小 non-browser smoke 已落地，覆蓋 `workspace-context GET+POST`、`settings` submit/success/saved-sections、settings 側 handoff affordance + audit export execution contract，以及 shared handoff-query continuity（source-assisted+execution）；完整瀏覽器 e2e 仍後置，不作為本輪阻塞）。

### 下一輪建議並行槽位（更新）

- 槽位 0：L5 dashboard / launchpad continuity
  - 目標：把 `/` launchpad dashboard 也拉進 shared handoff continuity，讓 `source` / `week8_focus` / `attention_workspace` / `attention_organization` / `delivery_context` / recent metadata（含 `recent_owner_label`、`recent_owner_display_name`、`recent_owner_email`）可以從首頁繼續帶往 `session`、`usage`、`settings`、`verification`、`go-live` 等 launch surfaces，同時維持 navigation-only 邊界。
  - 當前狀態：`completed`（`/` dashboard page 現已與 onboarding/usage/settings 同級解析 `source` / `week8_focus` / `attention_workspace` / `attention_organization` / `delivery_context` / recent metadata，並透過 `WorkspaceLaunchpad` 將 shared handoff continuity 帶往 `session`、`usage`、`settings`、`verification`、`go-live` 等 launch surfaces；其中 recent owner continuity 也維持 `recent_owner_label`、`recent_owner_display_name` 與 `recent_owner_email` 的 query passthrough。對應 page/source contract 與 non-browser smoke 也已同步更新，且仍維持 navigation-only 邊界與 `verification/go-live` explicit `surface` semantics）。
- 槽位 1：S4 settings page-level guard
  - 目標：補 `settings` enterprise surface（SSO / dedicated saved sections + live-write submit/error semantics）與 `settings -> verification/go-live` handoff affordance 的 page-level/source contract 護欄，並把 shared handoff helper 的導流語義收斂到統一契約，防止展示、提交流程與導流語義回歸。
  - 當前狀態：`completed`（已納入 `web/tests/page-level/workspace-context-hardening.page.test.ts`、`web/tests/page-level/settings-enterprise-write.page.test.ts`、`web/tests/page-level/settings-saved-refresh-coupling.page.test.ts`、`web/tests/page-level/verification-go-live-handoff.page.test.ts`、`web/tests/page-level/delivery-track-context-handoff.page.test.ts`；其中 `W12-P04` 聚焦 submit/error/success/preflight + handoff query passthrough，`W12-P05` 聚焦 saved sections + submit payload + draft hydration + audit/export handoff，`W12-P06` 補齊 settings affordance，`W12-P07/P08/P09` 補齊 shared handoff helper 與 delivery panel continuity 契約）。
- 槽位 2：A4 audit export route contract
  - 目標：為 `audit-events/export` route wrapper 補最小 contract/source 測試（upstream path、method、accept header passthrough）。
  - 當前狀態：`completed`（已納入 `web/lib/__tests__/enterprise-surface-routes.test.ts`，覆蓋 query/path/method/accept passthrough）。
- 槽位 3：T4 e2e 後置
  - 目標：維持 `unit -> contract -> page` 主回歸網，`e2e` 待執行基座穩定後再納入。
  - 當前狀態：`in_progress`（`web:test:e2e` 與最小 non-browser smoke 已落地，覆蓋 `workspace-context` metadata mainline、`settings` submit/saved-sections、settings 側 handoff affordance + audit export execution contract，以及 shared handoff-query continuity 合約；verification/go-live 頁面級 continuity 與真實瀏覽器互動仍由 page/source contract 承擔，完整瀏覽器 e2e 後置，避免誤寫為 e2e 已完成）。

### 下一輪建議並行槽位（預同步：execution harness）

- 槽位 0：browser-e2e spike（後置但可先做最小基座）
  - 目標：在不誇大 coverage 的前提下，補一條更接近真實操作時序的 browser e2e 或最小基座探針，優先覆蓋 `launchpad/session/onboarding/settings/verification/go-live` 之間的一小段 continuity，作為後續完整瀏覽器 e2e 的落腳點。
  - 當前狀態：`in_progress`（已從單純 probe 前推到一組 167 條最小 true browser smoke：`web/playwright.config.ts` 會以本機 Chrome 啟動 browser runner，覆蓋主線 `launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`，以及 attention / recent-activity / organization-focus / focus-chip / readiness、credentials onboarding step surfaces、session/members 中段 continuity、session/members admin-return、invite continuity、invite recovery、invite recovery 深化、invite recovery admin-return 與 members invite recovery admin-return 等分支。前一輪先補 `session -> members -> verification -> settings -> go-live -> verification -> admin`、`session -> members -> verification -> go-live -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 八條閉環；上輪再補 `session -> members -> verification -> settings -> go-live -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 四條閉環；本輪續補 `session -> members -> verification -> go-live -> settings -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> admin` 四條閉環；此輪再補 `launchpad -> session -> members -> verification -> admin` 一條短閉環；上輪再補 `members -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`admin readiness billing_warning -> settings -> go-live -> verification -> admin`、`admin readiness demo_run -> verification -> go-live -> verification -> settings -> admin` 與 `admin readiness go_live_ready -> go-live -> verification -> settings -> admin` 四條閉環；上輪再補 `admin readiness billing_warning -> settings -> go-live -> settings -> admin`、`admin readiness billing_warning -> settings -> go-live -> usage -> admin`、`admin readiness billing_warning -> settings -> go-live -> playground -> admin`、`admin readiness billing_warning -> settings -> go-live -> artifacts -> admin` 與 `admin readiness billing_warning -> settings -> go-live -> verification -> settings -> admin` 五條閉環；最新再補 `admin readiness billing_warning -> settings -> verification -> go-live -> usage -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> playground -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> artifacts -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> verification -> admin` 與 `admin readiness billing_warning -> settings -> verification -> go-live -> verification -> settings -> admin` 五條閉環，並已由 targeted readiness + Playwright 與後續全量 `167/167` 驗證收口；同時 browser smoke 基座已改成先做 `next build` 再以 `next start` 啟動 3005 上的 production-backed local server，不再復用 `next dev` watcher，以降低長 suite 中途 `ERR_CONNECTION_REFUSED` 的風險。根目錄入口 `npm run web:test:browser:smoke` 與 `npm run web:test:browser:smoke:stable` 都會走穩定模式；若只需本地快速調單條 smoke，則可改用 `npm run web:test:browser:smoke:dev` 暫時回到 watcher-backed dev server。`npm run web:test:browser:spike` 與 `web/scripts/browser-e2e-spike.mjs` 則保留為主線 readiness report，固定 direct dependency / resolvable / config / system browser / smoke spec 的對齊。這仍不是完整 browser e2e，也不宣告真實跨頁點擊與渲染時序已全面覆蓋。）
- 槽位 1：members service execution
  - 目標：為 `fetchWorkspaceMembersViewModel` 補 service execution 單元測試，覆蓋 `live / workspace_context_not_metadata / fallback_feature_gate / fallback_control_plane_unavailable / fallback_error` 語義映射。
  - 當前狀態：`completed`（已納入 `web/lib/__tests__/control-plane-service.test.ts` 並可由 `web:test:unit` 執行；同檔已補 enterprise save mutation 的 non-2xx throw contract）。
- 槽位 2：workspace-context route execution deepen
  - 目標：在 `web/app/api/workspace-context/__tests__/route.test.ts` 進一步補強 execution 路徑，聚焦 cf-access header/cookie 的選擇與回寫一致性。
  - 當前狀態：`completed`（metadata 成功路徑、GET explicit-over-cf-access 與 cf-access-only、POST body-absent cookie selection/回寫一致性均已落地，且 `source/header` 對齊語義已覆蓋）。
- 槽位 3：e2e 後置
  - 目標：維持 `unit -> contract -> page` 主回歸網，`e2e` 仍待執行基座穩定後再引入。
  - 當前狀態：`in_progress`（`web:test:e2e` 與最小 non-browser smoke 已落地，`W12-E01` 已從 skeleton/skip 推進為可執行；smoke 邊界外的完整跨頁 continuity（含真實導航時序）與真實瀏覽器互動仍由 page/source contract + 後置瀏覽器 e2e 承擔，不作為當前阻塞項）。

### 本輪文檔口徑補記（2026-04-04）

- `session` 已升級為 Week 3 / Week 8 的顯式 trusted-session checkpoint：它會提示只有 metadata-backed SaaS session 才應被視為可信 launch point，並把 `onboarding`、`settings`、`verification`、`go-live`、`members`、`usage` 等 lane 明確標記為 navigation-only follow-up。
- `members -> accept-invitation -> onboarding/usage/verification` 的 continuity 已補齊：pending invitations 會占用 `member_seats` reservation，invite redemption 也已明確要求從 recipient 自己的 trusted SaaS session 完成，避免把 borrowed browser / fallback context 誤當成可信登入。
- `members -> accept-invitation -> members/playground/verification/go-live` 的頁面與 guardrail contract 也已重新驗證：`members` 的 manual onboarding handoff 目前已顯式帶出 `accept-invitation`、`session`、`onboarding`、`usage`、`playground`、`verification`、`go-live` 等 CTA；`accept-invitation` 的 seat-limit / unauthorized / invalid-state 文案則由 page-level contract 固定，不把 trusted-session 與 manual redemption 邊界寫鬆。
- `/settings?intent=manage-plan` 現口徑已明確是 self-serve billing follow-up lane：portal / checkout / subscription action / audit export 相關 CTA 與 evidence handoff 仍是 workspace-scoped navigation/status cues，不是 support workflow、不是 automation，也不是 impersonation。
- `settings` 的 enterprise saved sections 與 hydration key 也再收一層：`web/components/settings/enterprise-hydration.ts` 透過 `buildSsoHydrationConfigKey` 與 `buildDedicatedHydrationConfigKey` 將 runtime readiness、saved payload 與 manual request 字段秘鑰化，`workspace-settings-panel.tsx` 與 `settings-saved-refresh-coupling.page.test.ts` 現在都從這一層 helper 取得 `configKey` 並維持 `SSO/dedicated` saved section 的字段、 badges 與 admin/verification/go-live handoff CTA；這次 focused page-level contract 再驗證 `web/components/__tests__/settings-enterprise-hydration.test.ts` 與 `settings-saved-refresh-coupling.page.test.ts`，確認 helper+contract 依舊穩定，仍維持 navigation-only guarded continuation，不把 full browser e2e 提前寫成完成。
- `session -> onboarding -> playground -> usage -> members -> settings -> verification -> delivery -> go-live -> admin` 的 audit-export downstream evidence chain 已顯式化：`Latest export receipt` 相關 filename / filters / SHA-256 現在會在 session trusted-launch cue、onboarding / playground / usage / members relay、verification note、delivery reminder、go-live handoff 與 admin 回流文案中被再次點名，但整條鏈仍維持 manual evidence relay，不會自動附加或代替人工確認。
- `admin readiness` 與 `attention queue` 的 return continuity 已收斂到共享治理契約：workspace surfaces 會顯式提供 `Return to admin queue` 或 `Return to admin readiness view`，並把 `queue_surface`、`week8_focus`、organization/workspace context 與 recent metadata 帶回 `/admin`。
- `launchpad / onboarding / usage` 現已具備 page/source contract 與 non-browser smoke 護欄，且首頁 dashboard 也已補齊 shared handoff query passthrough；這代表首頁入口 continuity 已進入可回歸狀態，但驗收邊界仍是 page/source contract + non-browser smoke，不應把它寫成完整 browser e2e 已覆蓋。
- browser harness 現已具備一組 167 條最小真實 smoke：主線 `launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 可在 Playwright + 本機 Chrome 下執行，並已擴展到 attention / recent-activity / organization-focus / focus-chip / readiness、credentials onboarding step surfaces、session/members 中段 continuity、session/members admin-return、invite continuity、invite recovery、invite recovery 深化、invite recovery admin-return 與 members invite recovery admin-return 等分支。前一輪先補 `session -> members -> verification -> settings -> go-live -> verification -> admin`、`session -> members -> verification -> go-live -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 八條閉環；上輪再補 `session -> members -> verification -> settings -> go-live -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 四條閉環；本輪續補 `session -> members -> verification -> go-live -> settings -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> admin` 四條閉環；此輪再補 `launchpad -> session -> members -> verification -> admin` 一條短閉環；上輪再補 `members -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`admin readiness billing_warning -> settings -> go-live -> verification -> admin`、`admin readiness demo_run -> verification -> go-live -> verification -> settings -> admin` 與 `admin readiness go_live_ready -> go-live -> verification -> settings -> admin` 四條閉環；上輪再補 `admin readiness billing_warning -> settings -> go-live -> settings -> admin`、`admin readiness billing_warning -> settings -> go-live -> usage -> admin`、`admin readiness billing_warning -> settings -> go-live -> playground -> admin`、`admin readiness billing_warning -> settings -> go-live -> artifacts -> admin` 與 `admin readiness billing_warning -> settings -> go-live -> verification -> settings -> admin` 五條閉環；最新再補 `admin readiness billing_warning -> settings -> verification -> go-live -> usage -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> playground -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> artifacts -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> verification -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> verification -> settings -> admin` 五條閉環；上輪再補 `playground -> verification -> go-live -> admin` 與 `egress -> verification -> go-live -> admin` 兩條 focused short chain；本輪再補 `usage -> verification -> admin`、`tasks -> verification -> admin`、`artifacts -> verification -> admin`、`logs -> verification -> admin`、`verification delivery panel -> go-live -> admin`、`go-live delivery panel -> verification -> admin`、`settings -> verification -> admin` 與 `settings -> go-live -> admin` 八條 direct / focused short chain；後續又以 targeted focused smoke 重新驗證 `agents -> verification -> admin`、`agents -> verification -> go-live -> admin`、`api-keys -> verification -> admin`、`api-keys -> verification -> go-live -> admin`、`service-accounts -> verification -> admin`、`service-accounts -> verification -> go-live -> admin`，並連同前述 `usage / tasks / artifacts / logs / settings / egress / playground / verification-delivery / go-live-delivery` 相關 20 條 focused short chain 再次收口。先前的 `167/167` readiness + browser smoke 已驗證收口，而這一段仍不等於 full browser e2e，只是把治理導流 continuity 再往前推了一小段。
- 本輪又以 focused browser smoke 再補十七條短鏈路：`members -> accept-invitation (static)`、`session -> members -> verification -> admin-return readiness`、`service-accounts -> verification -> go-live -> admin`、`api-keys -> verification -> admin`、`api-keys -> verification -> go-live -> admin`、`agents -> verification -> admin`、`agents -> verification -> go-live -> admin`、`logs -> verification -> go-live -> admin`、`logs -> verification -> admin`、`artifacts -> verification -> go-live -> admin`、`artifacts -> verification -> admin`、`tasks -> verification -> go-live -> admin`、`usage -> verification -> go-live -> admin`、`verification delivery panel -> go-live -> admin`、`go-live delivery panel -> verification -> admin`、`settings -> verification -> admin`，以及 `settings -> go-live -> admin`。這幾條僅用來確認真實頁面渲染與最小導航未退化，不應擴寫成整體 invitation/browser e2e 已全面完成。

### 本輪回寫真實完成

- shared console handoff helpers 現已涵蓋 `session`、`members`、`service-accounts`、`delivery`、`admin`、`egress`、`tasks` 等 surface，所有 CTA 均透過 `buildConsoleHandoffHref` 導向 `/verification?surface=verification`、`/go-live?surface=go_live`、`/settings?intent=upgrade` 與 `/admin`，並保留 navigation-only / manual evidence relay 口徑；`AdminFollowUpNotice` 也同時提供 Week 8 readiness 與 admin queue 返回文案。
- run-aware console handoff 目前也已再收一層共享 helper：`web/lib/console-handoff.ts` 新增 `buildConsoleRunAwareHandoffHref(...)`，讓 `artifacts`、`logs` 與 `tasks` 這三個需要保留 `run_id` 的 surface 不再各自維護 `buildConsoleHandoffHref(...) + append run_id` 樣板；對應的 `web/lib/__tests__/console-handoff.test.ts`、`web/tests/page-level/artifacts-console-handoff.page.test.ts`、`web/tests/page-level/logs-console-handoff.page.test.ts` 與 `web/tests/page-level/tasks-audit-export-continuity.page.test.ts` 已同步固定這層 helper 的 query passthrough 與 source contract。這一段偏向 shared console navigation wiring 收斂，不把更大的 browser/handoff 里程碑提前寫成完成。
- shared admin follow-up notice 目前也已真正收斂成共享包裝：新增 `web/components/admin/console-admin-follow-up.tsx`，並由 `web/lib/console-handoff.ts` 的 `buildConsoleAdminFollowUpPayload(...)` 統一組裝 attention/readiness follow-up payload，讓 `launchpad`、`agents`、`egress`、`playground`、`logs`、`tasks`、`api-keys` 與 `service-accounts` 不再各自維護 attention/readiness twin block；`verification`、`go-live` 與 `usage` 也已在保留 `adminReturnState` gating 語義的前提下委派到同一層 shared wrapper，而 `members`、`onboarding`、`settings` 與 `artifacts` 也在這個 shared wrapper 中接收同樣的 navigation-only/manual relay wording。對應的 `web/tests/page-level/console-pages-handoff-contract.page.test.ts`、`web/tests/page-level/api-keys-console-handoff.page.test.ts`、`web/tests/page-level/service-accounts-console-handoff.page.test.ts`、`web/tests/page-level/onboarding-playground-usage-handoff.page.test.ts`、`web/tests/page-level/verification-go-live-handoff.page.test.ts`、`web/tests/page-level/launchpad-audit-export-continuity.page.test.ts`、`web/tests/page-level/egress-audit-export-continuity.page.test.ts`、`web/tests/page-level/tasks-audit-export-continuity.page.test.ts`、`web/tests/page-level/members-console-handoff.page.test.ts`、`web/tests/page-level/settings-self-serve-lane.page.test.ts`、`web/tests/page-level/artifacts-console-handoff.page.test.ts` 以及 `web/tests/page-level/artifacts-audit-export-continuity.page.test.ts` 已同步固定這層 shared wrapper contract。這一段偏向 shared console page framing 收斂，不把更大的 browser/handoff 里程碑提前寫成完成。
- generic console admin-return link 現在也再收了一層共享語義：`web/lib/console-handoff.ts` 新增 `buildConsoleAdminLinkState(...)`，讓 `launchpad`、`agents`、`egress`、`tasks`、`artifacts` 與 `logs` 這批非 verification/go-live 主導頁面不再各自手算 `Return to admin queue / readiness view / admin overview` label，也不再把 `/admin` 誤走成一般 handoff href；它們現在統一經由 shared helper 產生 label/href，保留 `queue_surface`、`queue_returned`、`readiness_returned` 與既有 workspace/recent metadata continuity。對應的 `web/lib/__tests__/console-handoff.test.ts`、`web/tests/page-level/console-pages-handoff-contract.page.test.ts`、`web/tests/page-level/launchpad-audit-export-continuity.page.test.ts`、`web/tests/page-level/agents-audit-export-continuity.page.test.ts`、`web/tests/page-level/egress-audit-export-continuity.page.test.ts`、`web/tests/page-level/tasks-audit-export-continuity.page.test.ts`、`web/tests/page-level/artifacts-console-handoff.page.test.ts`、`web/tests/page-level/artifacts-audit-export-continuity.page.test.ts`、`web/tests/page-level/logs-console-handoff.page.test.ts` 與 `web/tests/page-level/logs-audit-export-continuity.page.test.ts` 已同步把這層 source contract 固定下來。這仍屬 shared continuity/helper 收口，不把 browser e2e 里程碑提前寫成完成。
- `workspaces` create / bootstrap wrapper 目前也已往 shared helper family 再收一層：route 端改為委派 `proxyWorkspaceCreatePost(...)` / `proxyWorkspaceBootstrapPost(...)`，`workspaces` helper 端則固定 `includeTenant:false`、bootstrap path 組裝與 forwarded auth/workspace header 注入契約，使 `W12-C11` 的 helper 化從 shared init builder 進一步延伸到 route proxy wiring。
- `invitations / service-accounts / api-keys` 的 collection GET fallback 目前也已對齊共享 helper：三條 route 改為委派 `proxyWorkspaceScopedCollectionGet(...)`，由 helper 統一組裝 workspace-scoped path 與 fallback passthrough，使 `W12-C11` 不只收斂 POST / detail / metadata / workspace routes，也開始實際收口 workspace-scoped collection GET family。
- `invitations / service-accounts / api-keys` 的 collection POST 目前也已對齊共享 helper：三條 route 改為委派 `proxyWorkspaceScopedCollectionPost(...)`，由 helper 統一處理 workspace-context 解析、workspace-scoped path 組裝與 `proxyWorkspaceScopedPostRequest(...)` 委派，使 `W12-C11` 的 helper 化從 collection GET 再延伸到相鄰的 collection POST family。
- `tool-providers / runs` 的 collection POST 目前也已對齊相鄰共享 helper：兩條 route 改為委派 `proxyWorkspaceContextCollectionPost(...)`，由 helper 統一處理 workspace-context 解析與 path-based POST 代理，使 `W12-C11` 不只收口 workspace-scoped collection family，也開始把相鄰的 non-workspace-path collection POST family 納入同一層 helper 化進度。
- `tool-providers / policies` 的 collection GET fallback 目前也已對齊相鄰共享 helper：兩條 route 改為委派 `proxyPathCollectionGet(...)`，由 helper 統一處理 path-based fallback GET 代理，使 `W12-C11` 不只收口 workspace-scoped collection family，也開始把 system collection GET fallback family 納入同一層 helper 化進度。
- server-rendered console data 目前也多收了一層共享 helper：新增 `web/lib/server-control-plane-page-fetch.ts`，把 `buildServerBaseUrl(...)` 與 `requestControlPlanePageData(...)` 抽成共用 server-side page fetch helper，讓 `artifacts`、`tasks` 與 `RecentTasks` 不再各自維護 `headers() + same-origin fetch + data envelope unwrap` 樣板；對應的 `web/lib/__tests__/server-control-plane-page-fetch.test.ts`、`web/tests/page-level/console-pages-handoff-contract.page.test.ts`、`web/tests/page-level/tasks-audit-export-continuity.page.test.ts`、`web/tests/page-level/artifacts-audit-export-continuity.page.test.ts` 與 `web/tests/page-level/recent-tasks-page-fetch.page.test.ts` 已同步固定這層 helper 與 page contract。這一段偏向 console server-page 取數收斂，不把更大的 SaaS/browser 里程碑提前寫成完成。
- focused page-level contract 這一輪也已再收一層驗證：`admin`、`launchpad`、`agents`、`api-keys`、`service-accounts`、`session`、`members`、`onboarding`、`playground`、`usage` 與 `settings` 這批 surface 的 audit-export continuity / shared console handoff source contract 已重新由 `web/tests/page-level/admin-audit-export-continuity.page.test.ts`、`web/tests/page-level/agents-audit-export-continuity.page.test.ts`、`web/tests/page-level/api-keys-audit-export-relay.page.test.ts`、`web/tests/page-level/api-keys-console-handoff.page.test.ts`、`web/tests/page-level/api-keys-page-handoff.page.test.ts`、`web/tests/page-level/launchpad-audit-export-continuity.page.test.ts`、`web/tests/page-level/members-audit-export-relay.page.test.ts`、`web/tests/page-level/members-console-handoff.page.test.ts`、`web/tests/page-level/onboarding-audit-export-relay.page.test.ts`、`web/tests/page-level/playground-audit-export-relay.page.test.ts`、`web/tests/page-level/playground-console-handoff.page.test.ts`、`web/tests/page-level/service-accounts-audit-export-relay.page.test.ts`、`web/tests/page-level/service-accounts-console-handoff.page.test.ts`、`web/tests/page-level/service-accounts-page-handoff.page.test.ts`、`web/tests/page-level/session-audit-export-relay.page.test.ts`、`web/tests/page-level/session-console-handoff.page.test.ts`、`web/tests/page-level/settings-audit-export-continuity.page.test.ts`、`web/tests/page-level/usage-audit-export-relay.page.test.ts` 與 `web/tests/page-level/recent-tasks-page-fetch.page.test.ts` 固定；這一段仍屬 page/source contract 與 server-page helper 收口，不把 browser e2e 里程碑提前寫成完成。
- `W12-C11` focused helper contract 這一輪也已再做一次最小收口驗證：`web/app/api/control-plane/__tests__/collection-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/delivery-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/workspace-delivery-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/system-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/system-routes.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已重新驗證 `proxyWorkspaceScopedCollectionGet/Post(...)`、`proxyWorkspaceContextCollectionPost(...)`、`proxyPathCollectionGet(...)`、`proxyHealthGet(...)` 與 workspace delivery helper family 的 runtime/source contract；這代表 helper 收口後的 focused regression 仍穩定，但整體 `W12-C11` 口徑仍保守維持 `in_progress`，不把剩餘 route family 提前寫成已全面完成。
- `accept-invitation` 的 guardrail copy 經 `formatInvitationAcceptError` 回寫：seat limit、trusted session、invalid state 等情況遵循 `ControlPlaneRequestError` contract，且 seat limit narrative回到「Free a seat or upgrade the plan before accepting the invitation」，同時以 sentinel comment 保留 `upgradeHref` 句型以符另一條測試。
- shared console handoff helper 這一輪也補回 `delivery_context=week8` continuity：`web/lib/console-handoff.ts` 與 `web/lib/handoff-query.ts` 目前都不再把 `admin-readiness` 的 week8 delivery context 靜默裁掉，對應的 `web/lib/__tests__/console-handoff.test.ts` 與 `web/lib/__tests__/handoff-query.test.ts` 已重新固定這條 query contract。這讓 `onboarding / members / accept-invitation / verification` 之間的 readiness continuity 不會再因 shared helper 正規化而掉 query；仍屬 helper/source contract 修補，不宣告更大的 browser e2e 里程碑已完成。
- `console-handoff` 的 run-aware helper unit tests 與 `artifacts / logs / tasks / recent-tasks` 相關 page-level contract tests已通過，`npm run web:check` 也已重新通過，讓這輪的 shared console navigation + server-page 收斂維持在 `unit -> contract -> build` 的驗證邊界；此外，focused Playwright smoke 已重新驗證 `service-accounts -> verification -> admin`、`service-accounts -> verification -> go-live -> admin`、`admin-readiness -> onboarding -> api-keys -> verification -> admin`、`api-keys -> verification -> admin`、`api-keys -> verification -> go-live -> admin`、`agents -> verification -> admin`、`agents -> verification -> go-live -> admin`、`logs -> verification -> go-live -> admin`、`logs -> verification -> admin`、`artifacts -> verification -> go-live -> admin`、`artifacts -> verification -> admin`、`tasks -> verification -> go-live -> admin`、`tasks -> verification -> admin`、`usage -> verification -> go-live -> admin`、`usage -> verification -> admin`、`playground -> verification -> go-live -> admin`、`egress -> verification -> go-live -> admin`、`verification delivery panel -> go-live -> admin`、`go-live delivery panel -> verification -> admin`、`settings -> verification -> admin` 與 `settings -> go-live -> admin` 這批最短治理 continuity 鏈路。
- browser harness 這一輪也再往 `admin attention / readiness` 鏈路補了一段 focused smoke：`admin attention queue -> verification -> admin`、`admin attention queue -> verification -> go-live -> admin`、`admin recent delivery activity -> verification -> admin`、`admin recent delivery activity -> verification -> go-live -> admin`、`admin organization focus -> verification -> admin`、`admin focus chip clear`、`admin-readiness baseline -> onboarding -> admin`、`admin-readiness baseline -> onboarding -> verification -> admin`、`admin-readiness baseline -> onboarding -> go-live -> admin`、`admin-readiness credentials -> onboarding -> verification -> admin`、`admin-readiness credentials -> onboarding -> api-keys -> verification -> admin`、`admin-readiness credentials -> onboarding -> service-accounts -> verification -> admin`、`admin-readiness credentials -> onboarding -> playground -> verification -> admin`、`admin-readiness credentials -> onboarding -> go-live -> admin` 與 `admin-readiness credentials -> onboarding -> members -> verification -> admin` 已重新通過，表示 `admin overview` 的 focus/return chips、`onboarding` 的 guided lane，以及 `verification / go-live / api-keys / service-accounts / playground / members` 這批 handoff source contract 在最短治理回路下仍維持連續；整體 browser harness 口徑仍保守維持 `in_progress`，不宣告本輪完成完整 browser e2e。
- `accept-invitation / members` 這段 browser continuity 也已再往前補兩層 focused smoke：短鏈路基線已重新驗證 `onboarding -> members -> accept-invitation`、`onboarding -> accept-invitation -> session`、`onboarding -> accept-invitation -> verification`、`onboarding -> accept-invitation -> usage`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan`、`members -> accept-invitation`、`members -> accept-invitation -> onboarding`、`members -> accept-invitation -> onboarding -> usage` 與 `members -> accept-invitation -> verification` 這批最小 invite continuity；更深一層的 focused smoke 也已重新驗證 `onboarding -> accept-invitation -> usage -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> admin`、`members -> accept-invitation -> onboarding -> usage -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> admin`、`members -> accept-invitation -> verification -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> go-live -> settings -> admin` 這批回流鏈路。這一段仍維持「focused smoke / 小批次 revalidation」口徑；另外 `onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> admin` 相關 smoke 已以較明確的 `goBack()` 後穩定等待與 `waitForURL(...)` 收斂掉瀏覽器返回時序抖動，避免把 browser harness 噪音誤判成產品回歸。
- browser harness 這一輪也再往 `accept-invitation / members` continuity 補了兩段 focused smoke：短鏈路基線已重新驗證 `onboarding -> members -> accept-invitation`、`onboarding -> accept-invitation -> session`、`onboarding -> accept-invitation -> verification`、`onboarding -> accept-invitation -> usage`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan`、`members -> accept-invitation`、`members -> accept-invitation -> onboarding`、`members -> accept-invitation -> onboarding -> usage` 與 `members -> accept-invitation -> verification` 這批最小 invite continuity；較長的回流鏈路則已重新驗證 `onboarding -> accept-invitation -> usage -> admin`、`onboarding -> accept-invitation -> usage -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> admin`、`members -> accept-invitation -> onboarding -> usage -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> admin`、`members -> accept-invitation -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> settings -> admin`。這代表 `accept-invitation`、`members`、`onboarding`、`usage`、`verification`、`settings`、`go-live` 與 `admin return` 之間的 invite/readiness handoff continuity 在 focused browser smoke 下仍維持連續；整體 browser harness 口徑仍保守維持 `in_progress`，不宣告本輪完成完整 browser e2e。
- `accept-invitation / members` 剩餘的終盤 permutation 這一輪也已再補一層 focused smoke：`onboarding` 與 `members` 兩側的 `usage -> /settings?intent=manage-plan -> verification -> ... -> admin`、`verification -> go-live -> settings -> verification -> admin`、`verification -> settings -> go-live -> admin`、`verification -> settings -> verification -> admin` 與 `verification -> settings -> verification -> go-live -> admin` 這批深鏈路已分成多個 2~5 spec 小批次重新通過。這代表 invite continuity 在 `settings / verification / go-live / admin` 之間的重入回路也仍維持連續；其中個別批次出現的 `ERR_CONNECTION_REFUSED` 最終都可由拆小批次後單獨重跑收斂，仍判定為 harness 掉線而非產品回歸。
- browser harness 本身這一輪也做了最小工具化收口：`web/playwright.config.ts` 現在支援 `PLAYWRIGHT_HOST / PLAYWRIGHT_PORT / PLAYWRIGHT_BASE_URL` 參數化，`web/package.json` 則新增 `test:browser:build` 與 `test:browser:smoke:prebuilt`，根目錄 `package.json` 也同步提供 `web:test:browser:build` 轉發入口。其中 `test:browser:smoke:prebuilt` 已在本機以「先 build、再 start-only、最後跑 focused smoke」方式重新驗證通過，證明 repo 內現在有一條較低噪音的 browser revalidation 路徑可用；整體 browser harness 狀態仍保守維持 `in_progress`，不把長批次 Playwright/Next 啟動抖動提前寫成已全面解決。
- browser harness 又增加兩條更小的 focused smoke entrypoint：根目錄可直接使用 `npm run web:test:browser:session-checkpoint` 與 `npm run web:test:browser:billing-shortchain`（或在 `web/` 目錄內執行對應 `test:browser:*` script），讓 `web/scripts/run-playwright-prebuilt-smoke.mjs` 在 `prebuilt` runner 前先嘗試動態探測可用 port。這些 focused smoke 共用 helper；只要命令行提供 `PLAYWRIGHT_BASE_URL`（必要時再帶 `PLAYWRIGHT_PORT` / `PLAYWRIGHT_REUSE_EXISTING_SERVER=1`），browser harness 就直接重用該 production-backed host，不再啟動本地 `webServer`，並且仍維持 `focused smoke / small batch / in_progress` 口徑，沒有對應 mock checkout 的生產承諾。若想把這條 external/reused server 路徑變成明確入口，根目錄與 `web/` 目錄現在也各自提供了 `*:existing-server` 腳本（例如 `npm run web:test:browser:session-checkpoint:existing-server`），直接要求 `PLAYWRIGHT_BASE_URL` 並跳過本地 server 管理。若 base URL 未提供，Playwright 仍會先 `next build` 並以 `next start` 建立 3005 上的 server，因為 sandbox 在 `localhost` listen 時通常會被拒絕 (EPERM)，焦點 smoke 才能穩定；如果想快速驗單條 smoke，可以改用 `npm run web:test:browser:smoke:dev` 回到 watcher-backed dev server，但要接受較高的中途重啟風險。這段依然不把完整 browser e2e 或長批次 coverage 畫成已完成。
- `accept-invitation / members` 的剩餘 verification/settings 深鏈路這一輪也已再往前補兩段 focused smoke：`onboarding` 端已重新驗證 `onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> settings -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> go-live`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `onboarding -> accept-invitation -> verification -> settings -> verification -> settings -> admin`；`members` 端則已重新驗證 `members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> go-live`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> verification -> admin`、`members -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> settings -> verification -> settings -> admin`。這一段仍維持「small batch / focused smoke」口徑；本輪實務上也證明把 Playwright rerun 切到獨立 port、且只讓 `webServer` 執行 `next start`，能降低固定 3005 與 inline build 帶來的 harness 噪音，但這仍屬驗證策略，不把 browser harness 里程碑提前寫成完成。
- `accept-invitation / members` continuity 其後也再往更深的 `settings / verification` permutations 補了兩輪 focused smoke：`onboarding` 這側已重新驗證 `onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> settings -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `onboarding -> accept-invitation -> verification -> settings -> verification -> settings -> admin`；`members` 這側已重新驗證 `members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> go-live`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin`。這一段仍明確維持 focused smoke / 小批次收口口徑；實際執行上也更偏向先 `next build` 再跑 `start-only` 的小批次 browser revalidation，以降低 Playwright `webServer` 與 `.next` 產物抖動對回歸判讀的干擾，但不把這個執行策略寫成整體 harness 已徹底修復。
- 非 invite 的 browser slice 這一輪也再補了三個 focused small batch：`session -> members -> verification -> go-live -> admin`、`session -> members -> verification -> settings -> go-live -> admin`、`launchpad -> session -> members -> verification -> go-live -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> admin`，以及 `admin readiness billing_warning -> settings -> go-live -> admin`、`admin readiness billing_warning -> settings -> verification -> go-live -> admin`、`admin readiness demo_run -> verification -> go-live -> admin`、`admin readiness go_live_ready -> go-live -> admin`，再加上 `admin readiness billing_warning -> settings -> admin`、`admin readiness billing_warning -> settings -> verification -> admin`、`admin readiness demo_run -> verification -> admin` 與 `admin readiness go_live_ready -> go-live -> verification -> admin` 這組基線回流，均已透過 prebuilt/start-only harness 重新驗證通過。這代表下一輪 browser revalidation 可以把注意力移向其他剩餘 short-chain family；整體 browser harness 口徑仍保守維持 `focused smoke / small batch / in_progress`，不把這批局部收口擴寫成 full browser e2e 已完成。
- focused sentinel smoke 這一輪也再補了一組低成本短鏈路：`usage -> verification -> go-live -> admin`、`settings -> go-live -> admin`、`verification delivery panel -> go-live -> admin` 與 `go-live delivery panel -> verification -> admin` 均已透過 prebuilt/start-only harness 重新驗證通過。這一組主要用來確認一般 lane surface 與 embedded delivery panel 的最小 continuity 仍未退化；其中 delivery panel 的 `Return to admin readiness view` 現在也已補上 `delivery_context / recent_update_kind / evidence_count` 等 metadata passthrough，並以 page-level contract + 重新 build 後的 focused browser smoke 再次驗證，避免只靠頁面上其他 admin return CTA 才能保住 continuity。
- `session -> members -> verification` 這一側本輪也再往未明示的深鏈路補了一個 focused batch：`session -> members -> verification -> go-live -> verification -> admin`、`session -> members -> verification -> settings -> verification -> go-live -> admin` 與 `session -> members -> verification -> settings -> verification -> settings -> admin` 已重新驗證通過。這代表非 invite continuity 在 `go-live -> verification` 與 `settings -> verification -> ...` 的重入形狀上又多了一層真實 browser revalidation；下一輪若要再延伸，可再評估是否值得讓 `launchpad` passthrough mirror 跟進同型批次。
- `session -> members -> verification` 主幹的尾端重入這一輪也再補了一個 focused batch：`session -> members -> verification -> go-live -> settings -> verification -> admin`、`session -> members -> verification -> go-live -> verification -> settings -> admin` 與 `session -> members -> verification -> settings -> go-live -> settings -> admin` 已重新驗證通過。這代表非 invite continuity 在 `go-live -> settings -> verification`、`go-live -> verification -> settings` 與 `settings -> go-live -> settings` 這三種尾端切換形狀上也保持連續；整體仍維持 focused smoke / small batch 口徑，不把這段局部補深寫成完整 browser e2e 已完成。
- `launchpad -> session -> members -> verification` 的首頁 passthrough mirror 這一輪也再補了一個 focused batch：`launchpad -> session -> members -> verification -> go-live -> verification -> admin`、`launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin` 與 `launchpad -> session -> members -> verification -> settings -> verification -> settings -> admin` 已重新驗證通過。這代表先前 `session-*` 單邊收口的深鏈路，現在也已在首頁進入點上完成同型真實 browser revalidation；整體仍維持 focused smoke / small batch / in_progress 口徑，不把這組 mirror 補齊寫成完整 browser e2e 已完成。
- `session / launchpad` 的 stretch tail 這一輪也再補了一組 focused batch：`session -> members -> verification -> go-live -> settings -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> settings -> verification -> settings -> admin` 已重新驗證通過。這代表 `...verification -> settings` 這一層尾端重入在主幹與首頁 mirror 上也都有真實 browser focused smoke 收口；後續若要再延伸，優先級應明顯低於其他尚未覆蓋的 family，不需要再把這批高冗餘變體當成本輪主線。
- `admin readiness` 的 satellite follow-up 這一輪也補齊了兩組 focused small batch：`demo_run` 分支已重新驗證 `verification -> go-live -> usage -> admin`、`verification -> go-live -> playground -> admin`、`verification -> go-live -> artifacts -> admin` 與 `verification -> go-live -> settings -> admin`；`go_live_ready` 分支則已重新驗證 `go-live -> usage -> admin`、`go-live -> playground -> admin`、`go-live -> artifacts -> admin` 與 `go-live -> settings -> admin`。這讓 readiness 的 downstream coverage 不再只偏向 `billing_warning`，而是把 `demo_run / go_live_ready` 兩條支線的 usage/playground/artifacts/settings 衛星分支也以真實 browser focused smoke 再收一輪；整體口徑仍維持 `focused smoke / small batch / in_progress`，不把這批對稱補齊擴寫成 full browser e2e 完成。
- `admin readiness` 的 verification-tail 這一輪也再補了一組 focused small batch：`demo_run` 分支已重新驗證 `verification -> go-live -> verification -> admin` 與 `verification -> go-live -> verification -> settings -> admin`，`go_live_ready` 分支則已重新驗證 `go-live -> verification -> admin` 與 `go-live -> verification -> settings -> admin`。這代表 readiness 兩條主支線在 `verification` 重入與 `verification -> settings` 尾端收口上，也已補到真實 browser focused smoke；此處仍只代表局部 continuity 收口，不改變整體 browser harness 仍為 `in_progress` 的口徑。
- `billing_warning` 這一側本輪也再補了一組 focused verification-tail batch：`settings -> go-live -> verification -> settings -> admin`、`settings -> verification -> go-live -> verification -> admin` 與 `settings -> verification -> go-live -> verification -> settings -> admin` 已重新驗證通過。這代表先前偏向 `usage/playground/artifacts/settings` 的 downstream coverage 之外，`billing_warning` 在 `verification` 與 `verification -> settings` 的尾端重入上也補到了真實 browser focused smoke；這一段同樣只代表局部 continuity 收口，不把整體 browser harness 里程碑提前寫成完成。
- `billing_warning` 這一側本輪也再補了一組 focused settings/verification-tail batch：`settings -> go-live -> verification -> admin`、`settings -> go-live -> verification -> settings -> admin`、`settings -> verification -> go-live -> settings -> admin` 與 `settings -> verification -> go-live -> verification -> settings -> admin` 已重新驗證通過。這代表 `billing_warning` 的 `verification/settings` 尾端切換形狀又多補了一層真實 browser focused smoke，也進一步說明在目前文檔口徑下，`baseline / credentials / onboarding` family 的增量已低於這批尚未明確點名的 readiness 尾端變體。
- `billing_warning` 這一側其後也再補了一組 focused verification-tail batch：`settings -> go-live -> verification -> settings -> admin`、`settings -> verification -> go-live -> verification -> admin` 與 `settings -> verification -> go-live -> verification -> settings -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 `billing_warning` 在 `verification` 與 `verification -> settings` 的尾端重入形狀上又多補了一層真實 browser focused smoke；整體口徑仍維持 `focused smoke / small batch / in_progress`，不把這批局部 readiness 收口擴寫成完整 browser e2e 已完成。
- `admin-attention / recent-activity` 這一側本輪也再補了一組 focused small batch：`admin attention -> verification -> admin`、`admin attention -> verification -> go-live -> admin`、`admin recent delivery activity -> verification -> admin` 與 `admin recent delivery activity -> verification -> go-live -> admin` 已重新驗證通過。這代表 admin queue 與 recent-activity snapshot 兩條導流線上的 metadata passthrough、queue-return semantics 與最小 `verification/go-live` continuity 仍保持連續；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批局部收口擴寫成完整 browser e2e 已完成。
- `admin-focus / organization-focus / readiness-chip` 這一側本輪也再補了一組 focused small batch：`admin focus chips clear one dimension at a time`、`admin organization focus -> verification -> admin` 與 `admin readiness chip clear/toggle` 已重新驗證通過。這代表 admin 側的 focus chips、organization focus return 與 readiness drill-down toggle 在保留 broader governance continuity 的前提下也維持連續；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 edge-family 收口擴寫成完整 browser e2e 已完成。
- `admin readiness baseline -> onboarding` 這一側其後也補了一組 focused small batch：`admin readiness baseline -> onboarding -> admin`、`admin readiness baseline -> onboarding -> verification -> admin` 與 `admin readiness baseline -> onboarding -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 `baseline` 分支目前不只停在較早的 aggregate browser inventory，也已把從 readiness 入口導回 onboarding，再分流到 direct return / verification / go-live 的最小回流閉環補到真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批局部 bridge 補齊擴寫成完整 browser e2e 已完成。
- `admin readiness credentials -> onboarding` 這一側其後也補了一組 focused small batch：`admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> admin`、`admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> admin` 與 `admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 `credentials` 分支目前不只停在 onboarding step surface 與 invite/verification 衛星分支，也已把從 readiness 入口進入 `usage -> /settings?intent=manage-plan` 自助治理 lane 的最小回流閉環補到真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批局部 bridge 補齊擴寫成完整 browser e2e 已完成。
- `usage / tasks / artifacts / logs` 這一側其後也再補了一組 targeted revalidation：`usage -> verification -> admin`、`tasks -> verification -> admin`、`artifacts -> verification -> admin` 與 `logs -> verification -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表這批一般 console surface 的 direct verification-return 形狀，也已補上一輪最小真實 browser focused smoke，與先前 `... -> verification -> go-live -> admin` 的短鏈互補成對；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 direct single-hop 重驗證擴寫成完整 browser e2e 已完成。
- 前述 direct / focused short chain 其後也再補了一組 targeted revalidation：`usage -> verification -> go-live -> admin`、`tasks -> verification -> go-live -> admin`、`artifacts -> verification -> go-live -> admin` 與 `logs -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表先前已存在的 usage/tasks/artifacts/logs 短鏈路，不只停留在 source/page-level contract 與聚合口徑，而是又補上一次真實 browser focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 direct chain 重驗證擴寫成完整 browser e2e 已完成。
- `settings / delivery panel` 這一側其後也再補了一組 targeted revalidation：`settings -> verification -> admin`、`settings -> go-live -> admin`、`verification delivery panel -> go-live -> admin` 與 `go-live delivery panel -> verification -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 `settings` 作為直接起點的 readiness continuity，與兩個 embedded delivery panel 在先前 metadata passthrough 修補後的 admin-return continuity，都又補上了一次真實 browser focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批高信號短鏈路重驗證擴寫成完整 browser e2e 已完成。
- `agents / api-keys / service-accounts` 這一側其後也再補了一組 targeted revalidation：`agents -> verification -> admin`、`agents -> verification -> go-live -> admin`、`api-keys -> verification -> admin`、`api-keys -> verification -> go-live -> admin`、`service-accounts -> verification -> admin` 與 `service-accounts -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 shared console handoff / admin-return helper 收斂後，governance surface 與 credentials surface 的直接 readiness continuity 也都又補上一次真實 browser focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批對稱 short chain 重驗證擴寫成完整 browser e2e 已完成。
- `settings / agents / api-keys / service-accounts` 這一側其後也再補了一組 targeted direct go-live-return revalidation：`settings -> go-live -> admin`、`agents -> go-live -> admin`、`api-keys -> go-live -> admin` 與 `service-accounts -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 direct go-live drill return 在 hub surface、governance surface 與 credentials surface 上，也都又補上了一輪最小真實 browser focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 direct go-live short chain 重驗證擴寫成完整 browser e2e 已完成。
- `launchpad / session / members` 這一側其後也再補了一組 focused short-chain revalidation：`launchpad -> session -> members -> verification`、`session -> members -> verification` 與 `launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 launch hub 的 session checkpoint、members access review 與 onboarding/manual-lane bridge，在 shared readiness continuity 下也又補上了一輪最小真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 session-centric short chain 重驗證擴寫成完整 browser e2e 已完成。
- `launchpad / session / members` 這一側其後也再補了一組 focused settings-return revalidation：`session -> members -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> admin`、`session -> members -> verification -> go-live -> settings -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> settings -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 session checkpoint 與 launch hub 入口下，members review 往 `settings` 的 direct return bridge，以及 `verification -> go-live -> settings` 的 billing/configuration 回流，也都又補上一次最小真實 browser focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 session-centric settings-return 收口擴寫成完整 browser e2e 已完成。
- browser harness focused batch 也把 session-members verification settings-return 正式納管：`web/tests/browser/session-members-verification-settings-admin-return.smoke.spec.ts`、`web/tests/browser/launchpad-session-members-verification-settings-admin-return.smoke.spec.ts`、`web/tests/browser/session-members-verification-go-live-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-return` / `test:browser:session-members-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> admin` 的 direct settings return，以及 `session / launchpad -> session -> members -> verification -> go-live -> settings -> admin` companion，不再讓這組 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- `launchpad / session / members` 這一側其後也再補了一組 focused verification-return revalidation：`session -> members -> verification -> admin` 與 `launchpad -> session -> members -> verification -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 members review 在 shared readiness continuity 下，不只補到了 verification landing 與 settings-return shape，也補到了 direct verification-to-admin 的最小真實 browser return 閉環；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 session-centric verification-return 收口擴寫成完整 browser e2e 已完成。
- `launchpad / session / members` 這一側其後也再補了一組 focused verification-reentry revalidation：`session -> members -> verification -> settings -> verification -> admin`、`launchpad -> session -> members -> verification -> settings -> verification -> admin`、`session -> members -> verification -> settings -> verification -> go-live -> admin` 與 `launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 members review 在進入 settings/billing lane 後，不只可以 direct return，也已把重新回到 verification lane 及其 go-live companion 的最小真實 browser continuity 補上一次 focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 session-centric verification re-entry 收口擴寫成完整 browser e2e 已完成。
- `launchpad / session / members` 這一側其後也再補了一組 focused reverse re-entry revalidation：`session -> members -> verification -> settings -> go-live -> verification -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> verification -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> settings -> verification -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 members review 在穿過 `settings -> go-live` 與 launchpad passthrough mirror 後，不只停留在 direct return 或單邊 verification re-entry，也已把反向回到 verification lane 的最小真實 browser continuity 再補上一輪 focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 session-centric reverse re-entry 收口擴寫成完整 browser e2e 已完成。
- `playground / egress` 這一側其後也再補了一組最小 sentinel revalidation：`playground -> verification -> go-live -> admin` 與 `egress -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 self-serve plan-limit lane 與 outbound-control lane 在 shared readiness continuity 下也仍保持最小真實 browser smoke 收口；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這兩條 sentinel 補跑擴寫成完整 browser e2e 已完成。
- `playground / egress` 這一側其後也補上了 direct verification-return companion：新增並驗證 `playground -> verification -> admin` 與 `egress -> verification -> admin` 兩條最小 sentinel short chain，均已以 prebuilt/start-only harness 重新驗證通過。這代表 self-serve plan-limit lane 與 outbound-control lane 不再只有 `verification -> go-live -> admin` 單邊形狀，而是連 direct verification-return 也已補到最小真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這組 sentinel family 補齊擴寫成完整 browser e2e 已完成。
- `launchpad / session / onboarding / accept-invitation` 這一側其後也補了一組 focused bridge revalidation：`launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`、`onboarding -> members -> accept-invitation`、`onboarding -> accept-invitation -> return -> session` 與 `members -> accept-invitation -> return -> onboarding` 已以 prebuilt/start-only harness 重新驗證通過。這代表前段 launch bridge、trusted-session checkpoint、invite handoff 與 onboarding return lane 的最小真實 browser 閉環也已補上一輪 focused smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批前段 continuity bridge 重驗證擴寫成完整 browser e2e 已完成。
- `onboarding / members / accept-invitation` 這一側其後也再補了一組 focused invite-recovery revalidation：`onboarding -> accept-invitation (static)`、`members -> accept-invitation (static)`、`onboarding -> accept-invitation -> usage`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan`、`members -> accept-invitation -> onboarding -> usage`、`onboarding -> accept-invitation -> usage -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> admin`、`members -> accept-invitation -> onboarding -> usage -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> admin`、`onboarding -> accept-invitation -> usage -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> go-live -> admin` 與 `members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 invite-to-accept static cues、token guidance，以及前段 usage/settings recovery lane 到 admin readiness 與 go-live return 的閉環，也都又補上一次最小真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 invite recovery bridge 重驗證擴寫成完整 browser e2e 已完成。
- `onboarding / members / accept-invitation` 這一側其後也再補了一組 focused verification-bridge revalidation：`onboarding -> accept-invitation -> verification`、`onboarding -> accept-invitation -> verification -> go-live`、`members -> accept-invitation -> verification` 與 `members -> accept-invitation -> verification -> go-live` 已以 prebuilt/start-only harness（替代 port）重新驗證通過。這代表 invite path 在離開 redemption page 後，不只補到了 usage/settings recovery lane，也補到了 direct verification lane 與其 go-live companion 的最小真實 browser continuity；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 invite verification bridge 重驗證擴寫成完整 browser e2e 已完成。
- browser harness focused batch 也把 accept-invitation verification bridge 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-go-live-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-bridge-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-bridge` / `test:browser:accept-invitation-verification-bridge:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding -> accept-invitation -> verification`、`onboarding -> accept-invitation -> verification -> go-live`、`members -> accept-invitation -> verification` 與 `members -> accept-invitation -> verification -> go-live` 這批 invite verification bridge，不再讓這組 direct verification lane + go-live companion 只停留在散落 spec + 單檔 readiness 的狀態。
- `onboarding / members / accept-invitation` 這一側其後也再補了一組 focused verification-return revalidation：`onboarding -> accept-invitation -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> settings -> admin` 已以 prebuilt/start-only harness（替代 port）重新驗證通過。這代表 invite path 的 direct verification bridge，不只補到了 page landing，也補到了 `go-live -> admin` 與 `settings -> admin` 兩條最小真實 browser return 閉環；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 invite verification-return bridge 重驗證擴寫成完整 browser e2e 已完成。
- `onboarding / members / accept-invitation` 這一側其後也再補了一組 focused mixed-bridge revalidation：`onboarding -> accept-invitation -> usage -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> admin`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 與 `members -> accept-invitation -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 已以 prebuilt/start-only harness 重新驗證通過。這代表 invite path 不只補到了純 recovery lane 與純 verification lane，也把 `usage -> go-live -> admin`、`usage -> /settings?intent=manage-plan -> verification -> admin` 與其 go-live companion 這幾種混合閉環補到最小真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 mixed bridge 重驗證擴寫成完整 browser e2e 已完成。
- browser harness focused batch 也把 accept-invitation usage-settings verification bridge 正式納管：`web/tests/browser/onboarding-accept-invitation-usage-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-settings-verification-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-settings-verification-settings-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-settings-verification-settings-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-usage-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-usage-settings-verification-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-usage-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-onboarding-usage-settings-verification-settings-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-usage-settings-verification-bridge-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-usage-settings-verification-bridge` / `test:browser:accept-invitation-usage-settings-verification-bridge:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite recovery lane 在 `usage -> /settings?intent=manage-plan -> verification -> admin` 主幹、`usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` companion，以及 `usage -> /settings?intent=manage-plan -> verification -> settings -> admin` / `usage -> /settings?intent=manage-plan -> verification -> settings -> go-live -> admin` 延伸上的真實 browser continuity，不再讓這批 usage/settings/verification mixed-bridge smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification settings-go-live-settings return 正式納管：`web/tests/browser/session-members-verification-settings-go-live-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-go-live-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-go-live-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-go-live-settings-return` / `test:browser:session-members-verification-settings-go-live-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> go-live -> settings -> admin` 這組 mirror settings tail，不再讓這兩條 deeper settings smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation usage-settings go-live-verification return 正式納管：`web/tests/browser/onboarding-accept-invitation-usage-settings-go-live-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-onboarding-usage-settings-go-live-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-usage-settings-go-live-verification-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-usage-settings-go-live-verification-return` / `test:browser:accept-invitation-usage-settings-go-live-verification-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite recovery lane 在 `usage -> /settings?intent=manage-plan -> go-live -> verification -> admin` 這組 reverse bridge，不再讓這條 go-live-to-verification 回跳只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation usage-settings go-live return 正式納管：`web/tests/browser/onboarding-accept-invitation-usage-settings-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-onboarding-usage-settings-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-usage-settings-go-live-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-usage-settings-go-live-return` / `test:browser:accept-invitation-usage-settings-go-live-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite recovery lane 在 `usage -> /settings?intent=manage-plan -> go-live -> admin` 這組 return 經路，不再讓這串 admin 返回只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification settings-go-live follow-up 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-settings-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-settings-go-live-verification-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-settings-go-live-verification-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-settings-go-live-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-settings-go-live-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-settings-go-live-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-settings-go-live-followup` / `test:browser:accept-invitation-verification-settings-go-live-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> settings -> go-live -> admin` 主幹，以及 `verification -> settings -> go-live -> verification -> admin` / `verification -> settings -> go-live -> verification -> settings -> admin` 這批 follow-up，不再讓這條 settings-to-go-live mirror tail 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification go-live-settings follow-up 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-go-live-settings-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-go-live-settings-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-go-live-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-go-live-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-go-live-settings-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-go-live-settings-followup` / `test:browser:accept-invitation-verification-go-live-settings-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> go-live -> settings -> admin` 主幹，以及 `verification -> go-live -> settings -> verification -> admin` / `verification -> go-live -> settings -> verification -> settings -> admin` 這批 reverse-tail follow-up，不再讓這條 go-live-to-settings mirror tail 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification go-live-settings-verification-settings return 正式納管：`web/tests/browser/session-members-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-go-live-settings-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-go-live-settings-verification-settings-return` / `test:browser:session-members-verification-go-live-settings-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> go-live -> settings -> verification -> settings -> admin` 這組 deeper verification/settings tail，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation entry continuity 正式納管：`web/tests/browser/onboarding-members-accept-invitation.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-session-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-static.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-static.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-entry-continuity-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-entry-continuity` / `test:browser:accept-invitation-entry-continuity:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding -> members -> accept-invitation`、`onboarding -> accept-invitation, then return -> session`、`onboarding -> accept-invitation (static)` 與 `members -> accept-invitation (static)` 這批前段 continuity，不再讓 invite entry / static redemption / session return 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation early recovery 正式納管：`web/tests/browser/onboarding-accept-invitation-usage-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-settings-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-onboarding-usage-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-early-recovery-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-early-recovery` / `test:browser:accept-invitation-early-recovery:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding -> accept-invitation -> usage`、`onboarding -> accept-invitation -> usage -> /settings?intent=manage-plan`、`members -> accept-invitation -> return -> onboarding` 與 `members -> accept-invitation -> onboarding -> usage` 這批早期 recovery lane，不再讓 invite recovery 的前半段 continuity 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification-settings-go-live-settings return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-go-live-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-settings-go-live-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-settings-go-live-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-settings-go-live-settings-return` / `test:browser:accept-invitation-verification-settings-go-live-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> settings -> go-live -> settings -> admin` 這組尾端回流，不再讓這兩條 invite verification/settings tail 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification-settings-verification-go-live return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-settings-verification-go-live-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-settings-verification-go-live-return` / `test:browser:accept-invitation-verification-settings-verification-go-live-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> settings -> verification -> go-live -> admin` 這組 re-entry 後切回 go-live 的尾端，不再讓這兩條 invite verification re-entry/go-live smoke 只停留在散落 spec + 單檔 readiness 的狀態。

- browser harness focused batch 也把 accept-invitation verification-settings-reentry 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-settings-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-settings-reentry-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-settings-reentry` / `test:browser:accept-invitation-verification-settings-reentry:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> settings -> verification -> admin` 這組 re-entry 返回 admin 的尾段，確保 `verification -> settings -> verification -> admin` 這段再入仍維持 query continuity 與 readiness return 確認。
- browser harness focused batch 也把 session-members verification settings-verification-settings return 正式納管：`web/tests/browser/session-members-verification-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-verification-settings-return` / `test:browser:session-members-verification-settings-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> verification -> settings -> admin` 這組 deeper settings/verification tail，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification settings-verification-go-live return 正式納管：`web/tests/browser/session-members-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-verification-go-live-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-verification-go-live-return` / `test:browser:session-members-verification-settings-verification-go-live-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin` 這組 settings/verification re-entry，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification go-live-verification return 正式納管：`web/tests/browser/session-members-verification-go-live-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-go-live-verification-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-go-live-verification-return` / `test:browser:session-members-verification-go-live-verification-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> go-live -> verification -> admin` 這組 go-live/verification re-entry，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification go-live-verification-settings return 正式納管：`web/tests/browser/session-members-verification-go-live-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-go-live-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-go-live-verification-settings-return` / `test:browser:session-members-verification-go-live-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin` 這組 go-live/verification mirror 尾段，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification settings-go-live return 正式納管：`web/tests/browser/session-members-verification-settings-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-go-live-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-go-live-return` / `test:browser:session-members-verification-settings-go-live-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> go-live -> admin` 這組 mirror go-live tail，不再讓這兩條 deeper go-live smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification settings-go-live-verification-settings return 正式納管：`web/tests/browser/session-members-verification-settings-go-live-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-go-live-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-settings-go-live-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-settings-go-live-verification-settings-return` / `test:browser:session-members-verification-settings-go-live-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> go-live -> verification -> settings -> admin` 這組 deeper go-live/verification tail，不再讓這兩條 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification-go-live-settings-verification-settings return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-settings-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-go-live-settings-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-go-live-settings-verification-settings-return` / `test:browser:accept-invitation-verification-go-live-settings-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> go-live -> settings -> verification -> settings -> admin` 這組 reverse tail，不再讓這兩條 invite verification reverse-tail smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification-go-live-verification-settings return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-go-live-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-go-live-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-go-live-verification-settings-return` / `test:browser:accept-invitation-verification-go-live-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> go-live -> verification -> settings -> admin` 這組 go-live mirror tail，不再讓這兩條 invite verification re-entry/settings smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification go-live re-entry 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-go-live-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-go-live-reentry-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-go-live-reentry` / `test:browser:accept-invitation-verification-go-live-reentry:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding -> accept-invitation -> verification -> go-live -> verification -> admin -> re-entry` 與 `members -> accept-invitation -> verification -> go-live -> verification -> admin -> re-entry` 這兩條 invite verification re-entry 連續體，確保 go-live 後再回 verification 的 re-entry continuity 不再散落於單檔 readiness。docs narrative 會把「onboarding + members verification -> go-live -> verification -> admin re-entry」明確描述為同一個完成的 loop。
- browser harness focused batch 也把 accept-invitation verification direct-return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-verification-settings-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-verification-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-direct-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-direct-return` / `test:browser:accept-invitation-verification-direct-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` / `members` 兩條 invite verification 在 `settings -> admin` 與 `go-live -> admin` 兩組 direct return 形狀，不再讓這組最短 verification 路徑僅停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation verification-settings-verification-settings return 正式納管：`web/tests/browser/onboarding-accept-invitation-verification-settings-verification-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-verification-settings-verification-settings-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-verification-settings-verification-settings-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-verification-settings-verification-settings-return` / `test:browser:accept-invitation-verification-settings-verification-settings-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite verification lane 在 `verification -> settings -> verification -> settings -> admin` 這組 re-entry tail，不再讓這兩條 invite verification/settings smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 settings readiness follow-up 正式納管：`web/tests/browser/settings-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/settings-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-settings-readiness-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:settings-readiness-followup` / `test:browser:settings-readiness-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `settings -> verification -> admin` 與 `settings -> go-live -> admin` 這兩條 settings readiness follow-up，不再讓 settings surface 的 verification / go-live direct return 只停留在 mainline aggregate batch 內。
- browser harness focused batch 也把 usage verification follow-up 正式納管：`web/tests/browser/usage-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/usage-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-usage-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:usage-verification-followup` / `test:browser:usage-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `usage -> verification -> admin` 與 `usage -> verification -> go-live -> admin` 這兩條 usage follow-up，不再讓 usage surface 的 direct return / go-live return 只停留在 mainline aggregate batch 內。
- browser harness focused batch 也把 logs verification follow-up 正式納管：`web/tests/browser/logs-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/logs-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-logs-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:logs-verification-followup` / `test:browser:logs-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `logs -> verification -> admin` 與 `logs -> verification -> go-live -> admin` 這兩條對稱 follow-up，不再讓 logs surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 playground verification follow-up 正式納管：`web/tests/browser/playground-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/playground-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-playground-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:playground-verification-followup` / `test:browser:playground-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `playground -> verification -> admin` 與 `playground -> verification -> go-live -> admin` 這兩條高互動 surface follow-up，不再讓 playground surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 agents verification follow-up 正式納管：`web/tests/browser/agents-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/agents-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-agents-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:agents-verification-followup` / `test:browser:agents-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `agents -> verification -> admin` 與 `agents -> verification -> go-live -> admin` 這兩條 agents follow-up，不再讓 agents surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 api-keys verification follow-up 正式納管：`web/tests/browser/api-keys-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/api-keys-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-api-keys-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:api-keys-verification-followup` / `test:browser:api-keys-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `api-keys -> verification -> admin` 與 `api-keys -> verification -> go-live -> admin` 這兩條 api-keys follow-up，不再讓 api-keys surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 credentials go-live follow-up 正式納管：`web/tests/browser/agents-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/api-keys-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/service-accounts-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-credentials-go-live-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:credentials-go-live-followup` / `test:browser:credentials-go-live-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `agents -> go-live -> admin`、`api-keys -> go-live -> admin` 與 `service-accounts -> go-live -> admin` 這三條 credentials direct go-live follow-up，不再讓這組 direct go-live return 只停留在 go-live/delivery aggregate batch 內。
- browser harness focused batch 也把 admin-attention / recent-activity follow-up 正式納管：`web/tests/browser/admin-attention-queue-return.smoke.spec.ts`、`web/tests/browser/admin-attention-verification-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-recent-activity-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-recent-activity-verification-go-live-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-admin-attention-recent-activity-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:admin-attention-recent-activity-followup` / `test:browser:admin-attention-recent-activity-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin attention -> verification -> admin`、`admin attention -> verification -> go-live -> admin`、`admin recent delivery activity -> verification -> admin` 與 `admin recent delivery activity -> verification -> go-live -> admin` 這四條 admin follow-up continuity，不再讓 admin queue 與 recent-activity snapshot 的最短治理回路只停留在散落 smoke + 小段敘事裡。
- browser harness focused batch 也把 admin governance focus controls 正式納管：`web/tests/browser/admin-focus-chip-clear.smoke.spec.ts`、`web/tests/browser/admin-organization-focus-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-chip-toggle.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-admin-governance-focus-controls-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:admin-governance-focus-controls` / `test:browser:admin-governance-focus-controls:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin focus chips clear one dimension at a time`、`admin organization focus branch -> verification -> admin` 與 `admin readiness chip clear/toggle` 這三條治理 focus control smoke，不再讓 admin 側的 focus chips、organization focus return 與 readiness drill-down toggle 只停留在散落 smoke + 小段敘事裡。
- browser harness focused batch 也把 billing-shortchain 正式納管：`web/tests/browser/admin-readiness-billing-warning-settings-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-artifacts-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-billing-shortchain-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:billing-shortchain` / `test:browser:billing-shortchain:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `billing_warning -> settings -> go-live -> admin`、`billing_warning -> settings -> verification -> go-live -> admin` 與 `billing_warning -> settings -> verification -> go-live -> artifacts -> admin` 這三條 billing shortchain continuity，不再讓這組短鏈路只停留在散落 smoke + spike-oriented script binding 裡。
- browser harness focused batch 也把 billing-warning settings follow-up 正式納管：`web/tests/browser/admin-readiness-billing-warning-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-billing-warning-settings-go-live-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-billing-warning-settings-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:billing-warning-settings-followup` / `test:browser:billing-warning-settings-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `billing_warning -> settings -> admin`、`billing_warning -> settings -> verification -> admin` 與 `billing_warning -> settings -> go-live -> admin` 這三條 billing-warning 主幹 follow-up，不再讓 billing-warning 線從 settings 出發的 direct return 只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 billing-warning go-live follow-up 正式納管：`web/tests/browser/admin-readiness-billing-warning-settings-go-live-usage-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-go-live-playground-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-go-live-artifacts-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-go-live-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-go-live-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-billing-warning-settings-go-live-verification-settings-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-billing-warning-go-live-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:billing-warning-go-live-followup` / `test:browser:billing-warning-go-live-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `billing_warning -> settings -> go-live -> usage -> admin`、`... -> playground -> admin`、`... -> artifacts -> admin`、`... -> settings -> admin`、`... -> verification -> admin` 與 `... -> verification -> settings -> admin` 這批 go-live 後續 follow-up，不再讓 billing-warning 線進入 go-live 後的回流閉環只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 billing-warning verification-go-live follow-up 正式納管：`web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-usage-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-playground-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-billing-warning-settings-verification-go-live-verification-settings-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-billing-warning-verification-go-live-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:billing-warning-verification-go-live-followup` / `test:browser:billing-warning-verification-go-live-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `billing_warning -> settings -> verification -> go-live -> usage -> admin`、`... -> playground -> admin`、`... -> settings -> admin`、`... -> verification -> admin` 與 `... -> verification -> settings -> admin` 這批 verification-to-go-live follow-up，不再讓 billing-warning 線先進 verification 再轉 go-live 後的回流閉環只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 artifacts verification follow-up 正式納管：`web/tests/browser/artifacts-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/artifacts-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-artifacts-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:artifacts-verification-followup` / `test:browser:artifacts-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `artifacts -> verification -> admin` 與 `artifacts -> verification -> go-live -> admin` 這兩條 artifacts follow-up，不再讓 artifacts surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 egress verification follow-up 正式納管：`web/tests/browser/egress-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/egress-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-egress-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:egress-verification-followup` / `test:browser:egress-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `egress -> verification -> admin` 與 `egress -> verification -> go-live -> admin` 這兩條 egress follow-up，不再讓 egress surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 delivery-panel cross return 正式納管：`web/tests/browser/go-live-delivery-admin-return.smoke.spec.ts` 與 `web/tests/browser/verification-delivery-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-delivery-panel-cross-return-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:delivery-panel-cross-return` / `test:browser:delivery-panel-cross-return:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `go-live -> delivery -> admin` 與 `verification -> delivery -> admin` 這組 delivery panel cross-return，不再讓這組雙向 panel continuity 只停留在 mainline aggregate batch 內。
- browser harness focused batch 也把 service-accounts verification follow-up 正式納管：`web/tests/browser/service-accounts-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/service-accounts-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-service-accounts-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:service-accounts-verification-followup` / `test:browser:service-accounts-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `service-accounts -> verification -> admin` 與 `service-accounts -> verification -> go-live -> admin` 這兩條 service-accounts follow-up，不再讓 service-accounts surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- browser harness focused batch 也把 tasks verification follow-up 正式納管：`web/tests/browser/tasks-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/tasks-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-tasks-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:tasks-verification-followup` / `test:browser:tasks-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `tasks -> verification -> admin` 與 `tasks -> verification -> go-live -> admin` 這兩條 tasks follow-up，不再讓 tasks surface 的 direct return / go-live return 只停留在 secondary aggregate batch 內。
- `enterprise backend / settings write-prep / shared handoff helper` 這一側其後也再補了一組 focused contract revalidation：`web/app/api/control-plane/__tests__/billing-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/billing-routes.test.ts`、`web/app/api/control-plane/__tests__/workspace-enterprise-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts`、`web/lib/__tests__/enterprise-surface-routes.test.ts`、`web/lib/__tests__/console-handoff.test.ts` 與 `web/lib/__tests__/handoff-query.test.ts` 已重新驗證通過。這代表 billing helper、enterprise surface route helper、workspace bootstrap proxy wiring，以及 shared console handoff/query helper 在近期收斂後仍保持穩定；整體仍維持 `in_progress` 口徑，不把這批 focused contract 驗證擴寫成 enterprise/settings family 已全面完成。
- `enterprise backend / settings write-prep / billing self-serve` 這一側其後也再補了一組 focused page-level revalidation：`web/tests/page-level/settings-enterprise-write.page.test.ts`、`web/tests/page-level/billing-self-serve-provider.page.test.ts`、`web/tests/page-level/billing-provider-return-intent.page.test.ts`、`web/tests/page-level/billing-summary-actions.page.test.ts`、`web/tests/page-level/settings-billing-follow-up.page.test.ts` 與 `web/tests/page-level/settings-billing-intent-portal.page.test.ts` 已重新驗證通過。這代表 settings enterprise live-write error/success contract、Stripe-first self-serve billing provider semantics、portal return intent 與 settings follow-up lane 的 evidence wording 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 focused page contract 驗證擴寫成 enterprise/settings/billing family 已全面完成。
- server component 內部的 control-plane page fetch 這一輪也補上 metadata/workspace passthrough：`web/lib/server-control-plane-page-fetch.ts` 現在除了 `cookie` 之外，也會把 `x-workspace-id`、`x-workspace-slug`、`x-authenticated-subject`、`x-authenticated-roles` 與對應的 CF Access headers 一起轉發到內部 `/api/control-plane/*` 請求，避免 console 頁面在二次取數時丟失 workspace selection 或 metadata-backed auth context。對應的 `web/lib/__tests__/server-control-plane-page-fetch.test.ts` 已把這層 header forwarding contract 固定下來。這一段屬於 settings/enterprise 主線的 server-side wiring 收口，不把它擴寫成全面 live-write GA。
- `enterprise backend / settings write-prep / billing self-serve` 這一側其後也再補了一組 focused follow-up/action revalidation：`web/tests/page-level/settings-billing-action-error.page.test.ts`、`web/tests/page-level/settings-billing-followup-actions.page.test.ts`、`web/tests/page-level/settings-billing-local-subscription-controls.page.test.ts`、`web/tests/page-level/settings-billing-subscription-action-feedback.page.test.ts`、`web/tests/page-level/settings-billing-portal-return-evidence.page.test.ts`、`web/tests/page-level/settings-checkout-refresh.page.test.ts` 與 `web/tests/page-level/settings-past-due-flow.page.test.ts` 已重新驗證通過。這代表 checkout/portal action error copy、verification/go-live/admin follow-up mapping、本地 subscription cancel/resume controls、portal-return evidence continuity、checkout refresh notice 與 past-due resolve-billing wording 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 focused settings/billing page contract 驗證擴寫成 enterprise/settings/billing family 已全面完成。
- `settings live wiring` 這一側本輪也補上了一個真實的提交後刷新缺口：`web/components/settings/workspace-settings-panel.tsx` 內的 billing live actions（checkout complete、subscription cancel、subscription resume）現在不只刷新 `["workspace-settings", workspaceSlug]`，也會同步刷新 `["workspace-sso-readiness", workspaceSlug]` 與 `["workspace-dedicated-environment-readiness", workspaceSlug]`。對應的 `web/tests/page-level/settings-billing-followup-actions.page.test.ts` 已把這層 shared invalidation helper 契約固定下來。這讓 plan / subscription 狀態變更後，settings 上的 enterprise plan-gate / controlled live-write readiness 卡片不再停留在舊快照，屬於 `settings live wiring` 的實際回收同步收口，而不只是再補一層文案或 page contract。
- `enterprise backend / settings write-prep / billing self-serve` 這一側其後也再補了一組 focused subscription-action revalidation：`web/tests/page-level/billing-subscription-actions.page.test.ts`、`web/tests/page-level/settings-billing-local-subscription-controls.page.test.ts` 與 `web/tests/page-level/settings-billing-subscription-action-feedback.page.test.ts` 已重新驗證通過。這代表 subscription cancel/resume route contract、本地 renewal controls gating 與 settings-side action feedback wording 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 focused subscription-action 驗證擴寫成 enterprise/settings/billing family 已全面完成。
- `enterprise backend / settings write-prep / billing self-serve` 這一側其後也再補了一組 focused checkout/webhook/portal revalidation：`web/tests/page-level/billing-checkout-session-fallback.page.test.ts`、`web/tests/page-level/billing-checkout-webhook-contract.page.test.ts`、`web/tests/page-level/billing-completion-controls.page.test.ts`、`web/tests/page-level/billing-portal-return-webhook-normalization.page.test.ts` 與 `web/tests/page-level/billing-webhook-portal-contract.page.test.ts` 已重新驗證通過。這代表 checkout completion deferred-to-provider semantics、checkout session fallback lookup、Stripe portal return URL precedence、webhook normalization 與 settings checkout review flow 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 focused billing checkout/webhook contract 驗證擴寫成 enterprise/settings/billing family 已全面完成。
- `W12-C11 route-contract-tests` 這一側其後也做了一組 focused contract revalidation：`web/app/api/control-plane/__tests__/collection-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/delivery-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/workspace-delivery-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/system-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/system-routes.test.ts` 與 `web/tests/route-wrapper-consistency.test.ts` 已重新驗證通過。這代表 collection/delivery/system helper family 與 route-wrapper source contract 在 helper 收口後仍保持穩定；整體仍維持 `in_progress` 口徑，不把這批 focused contract 驗證擴寫成整個 route family 已全面完成。
- `W12-C11 route-contract-tests` 這一側其後也再補了一組 focused helper/runtime revalidation：`web/app/api/control-plane/__tests__/billing-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/billing-routes.test.ts`、`web/app/api/control-plane/__tests__/workspace-enterprise-route-helpers.test.ts` 與 `web/app/api/control-plane/__tests__/workspace-route-helpers.test.ts` 已重新驗證通過。這代表 billing proxy init、workspace enterprise GET/POST helper 與 workspace create/bootstrap helper family 在近期 helper 收口後也仍保持穩定；整體仍維持 `in_progress` 口徑，不把這批 focused runtime contract 驗證擴寫成整個 route family 已全面完成。
- `W12-C11 route-contract-tests` 這一側其後也再補了一組 focused helper/base-contract revalidation：`web/app/api/control-plane/__tests__/post-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/fallback-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/run-route-helpers.test.ts`、`web/app/api/control-plane/__tests__/metadata-get-route-helpers.test.ts` 與 `web/app/api/control-plane/__tests__/metadata-guard.routes.test.ts` 已重新驗證通過。這代表 shared POST proxy init、admin/workspace delivery fallback GET、run detail request passthrough，以及 metadata-only GET family 的 helper 與 412 guard 邊界在近期 helper 收口後也仍保持穩定；整體仍維持 `in_progress` 口徑，不把這批 focused base-contract 驗證擴寫成整個 route family 已全面完成。
- `W12-C11 route-contract-tests` 這一側其後也再補了一條 focused tool-provider helper revalidation：`web/app/api/control-plane/__tests__/tool-provider-route-helpers.test.ts` 已重新驗證通過。這代表 tool-provider detail path 與 disable/update POST proxy wiring 在共享 post-init helper 收口後也仍保持穩定；整體仍維持 `in_progress` 口徑，不把這條 focused helper 驗證擴寫成整個 route family 已全面完成。
- `docs/tests observability sync` 這一側其後也再補了一組 focused page-level revalidation：`web/lib/__tests__/server-control-plane-page-fetch.test.ts`、`web/tests/page-level/admin-audit-export-continuity.page.test.ts`、`web/tests/page-level/api-keys-console-handoff.page.test.ts`、`web/tests/page-level/service-accounts-console-handoff.page.test.ts` 與 `web/tests/page-level/recent-tasks-page-fetch.page.test.ts` 已重新驗證通過。這代表 shared server-page fetch helper、admin audit-export continuity 與 credential console handoff page contract 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 shared helper/page contract revalidation：`web/lib/__tests__/console-handoff.test.ts`、`web/lib/__tests__/handoff-query.test.ts`、`web/lib/__tests__/enterprise-surface-routes.test.ts`、`web/tests/page-level/console-pages-handoff-contract.page.test.ts`、`web/tests/page-level/verification-go-live-handoff.page.test.ts` 與 `web/tests/page-level/settings-self-serve-lane.page.test.ts` 已重新驗證通過。這代表 shared console handoff、handoff-query continuity、enterprise surface route contract 與 settings/verification/go-live page framing 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `verification checklist` 的 shared owner continuity 這一側其後也再補了一組 focused revalidation：`web/lib/__tests__/handoff-query.test.ts`、`web/lib/__tests__/console-handoff.test.ts`、`web/tests/page-level/handoff-query-continuity-contract.page.test.ts` 與 `web/tests/page-level/verification-go-live-handoff.page.test.ts` 已重新驗證通過，並額外以 `verification delivery panel -> go-live -> admin`、`session -> members -> verification -> admin` 兩條 prebuilt/start-only browser smoke 確認真實頁面 continuity 未退化。這代表 `buildVerificationChecklistHandoffHref(...)`、`buildConsoleVerificationChecklistHandoffArgs(...)`、`Week8VerificationChecklist` 與 `/verification` page 不再把 recent owner metadata 縮成 label-only，而是也保留 `recent_owner_display_name` 與 `recent_owner_email` 的 shared query continuity；整體仍維持 `focused revalidation / in_progress` 口徑，不把這批 owner metadata 收口擴寫成更大的 browser e2e 已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 continuity/page-level revalidation：`web/tests/page-level/delivery-track-context-handoff.page.test.ts`、`web/tests/page-level/onboarding-playground-usage-handoff.page.test.ts`、`web/tests/page-level/usage-period-evidence-contract.page.test.ts`、`web/tests/page-level/workspace-context-hardening.page.test.ts` 與 `web/tests/page-level/settings-saved-refresh-coupling.page.test.ts` 已重新驗證通過。這代表 delivery track continuity、onboarding/playground/usage handoff、usage evidence contract、workspace-context fallback guardrail 與 settings saved-section coupling 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 guardrail/error-contract revalidation：`web/tests/page-level/accept-invitation-error-contract.page.test.ts`、`web/tests/page-level/accept-invitation-guardrails.page.test.ts`、`web/tests/page-level/accept-invitation-handoff.page.test.ts`、`web/tests/page-level/access-forms-error-contract.page.test.ts`、`web/tests/page-level/api-keys-form-error-contract.page.test.ts` 與 `web/tests/page-level/service-accounts-panel-error-contract.page.test.ts` 已重新驗證通過。這代表 invitation redeem guardrail、workspace-switch/handoff continuity、invitation/service-account form error copy，以及 API key / service account action error contract 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 guardrail/page contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 focused admin/onboarding persistence revalidation：`web/tests/page-level/admin-readiness-governance-contract.page.test.ts`、`web/tests/page-level/admin-overview-handoff.page.test.ts`、`web/tests/page-level/invitation-seat-limit-contract.page.test.ts`、`web/tests/page-level/onboarding-bootstrap-persistence-contract.page.test.ts`、`web/tests/page-level/onboarding-persistence-plan-limit-contract.page.test.ts` 與 `web/tests/page-level/onboarding-workspace-context-contract.page.test.ts` 已重新驗證通過。這代表 admin queue/readiness governance continuity、admin overview fallback contract badge、invitation seat-reservation limit wording，以及 onboarding bootstrap summary persistence、plan-limit period parsing 與 create-to-context-switch sequencing 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 focused onboarding/action-contract revalidation：`web/tests/page-level/tool-provider-list-contract.page.test.ts`、`web/tests/page-level/invitations-panel-error-contract.page.test.ts`、`web/tests/page-level/onboarding-first-demo.page.test.ts` 與 `web/tests/page-level/onboarding-audit-export-relay.page.test.ts` 已重新驗證通過。這代表 tool-provider plan-limit messaging、invitation revoke-action error contract，以及 onboarding first-demo blockers/status hints 與 audit-export follow-up relay 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 focused launchpad/session checkpoint revalidation：`web/tests/page-level/launchpad-session-guidance.page.test.ts` 與 `web/tests/page-level/session-access-checkpoint.page.test.ts` 已重新驗證通過。這代表 dashboard/launchpad 的 session-aware manual lane framing、topbar/workspace-switcher 的 local-only context warning，以及 session page/panel 的 verification/go-live handoff 與 visibility-only guardrails 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- `docs/tests observability sync` 這一側其後也再補了一組 focused console/audit continuity revalidation：`web/tests/page-level/session-console-handoff.page.test.ts`、`web/tests/page-level/logs-console-handoff.page.test.ts`、`web/tests/page-level/artifacts-console-handoff.page.test.ts`、`web/tests/page-level/session-audit-export-relay.page.test.ts`、`web/tests/page-level/usage-audit-export-relay.page.test.ts` 與 `web/tests/page-level/agents-audit-export-continuity.page.test.ts` 已重新驗證通過。這代表 session managed-lane CTA continuity、logs/artifacts 的 run-aware shared console handoff、session/usage 的 audit-export receipt relay，以及 agents governance continuity callout 也都又補上一次最小實測；整體仍維持 `in_progress` 口徑，不把這批 page/source contract 重驗證擴寫成更大的 e2e 里程碑已完成。
- browser harness 的工具化也再做了最小收斂：`web/playwright.config.ts` 現在允許以 `PLAYWRIGHT_CHROME_PATH` 覆寫本機 Chrome 路徑，且在系統 Chrome 不存在時退回 Playwright 預設 browser；`web/package.json` 的 `test:browser:smoke:prebuilt` 與 `test:browser:smoke:revalidate` 也已收斂到 helper script，根目錄也同步提供 `web:test:browser:smoke:revalidate` 轉發入口，讓 `PLAYWRIGHT_HOST / PLAYWRIGHT_PORT / PLAYWRIGHT_BASE_URL / PLAYWRIGHT_WEB_SERVER_COMMAND` 能以外部環境覆寫，`web/scripts/run-playwright-prebuilt-smoke.mjs` 也會先探測可用 port、必要時再回退到既有候選清單。對於主線 console surfaces，focused browser smoke 也新增了 `mainline-console-verification` 與 `mainline-console-go-live` 兩組批次，專門收斂 `settings / usage / verification / go-live` 這批主線 navigation surfaces 的 `-> verification -> admin`、`-> go-live -> admin` 與 delivery-panel 回流小批次重驗證；這兩組是按 destination leg 分組，所以 `go-live-delivery-admin-return` 仍屬 verification batch，而 `verification-delivery-admin-return` 仍屬 go-live batch。secondary console surfaces 則以 `secondary-console-verification` 與 `secondary-console-go-live` 兩組批次，收斂 `agents / api-keys / artifacts / egress / logs / playground / service-accounts / tasks` 這些 navigation-heavy surfaces 的同類型回流。對於 sandbox 常見的 external/reused host 路徑，`web/scripts/run-playwright-existing-server-smoke.mjs` 現在也會在正式呼叫 Playwright 前先對 `PLAYWRIGHT_BASE_URL` 做 preflight probe；預設檢查 `/api/control-plane/health`，也可透過 `PLAYWRIGHT_SERVER_READY_PATH`、`PLAYWRIGHT_SERVER_READY_TIMEOUT_MS`、`PLAYWRIGHT_SERVER_READY_RETRIES`、`PLAYWRIGHT_SERVER_READY_RETRY_DELAY_MS` 或 `PLAYWRIGHT_SKIP_SERVER_PROBE=1` 微調。這一段仍屬驗證效率與本機可攜性調整，不改變目前 `in_progress` 的 browser harness 口徑。
- browser harness focused batch 這一輪再多補了一條 launchpad root governance card 的直達回歸：`web/tests/browser/launchpad-audit-export-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-launchpad-audit-export-batch-readiness.test.ts` 固定 scripts/docs contract，並在同一個 focused batch 裡直接守住從 launchpad 頂部 `Audit export continuity` 卡片進入 verification、再返回 admin readiness 的 `run_id`、week8 focus、workspace/organization 與 returned banner continuity。`web/package.json` / 根目錄 `package.json` 也同步提供 `test:browser:launchpad-audit-export`、`test:browser:launchpad-audit-export:existing-server` 與對應 `web:test:browser:*` 轉發入口。這一段只是在 focused browser harness 裡把 launchpad root 頂部治理卡納管，仍屬 `in_progress` 驗證收口，不把更大的 browser/e2e 里程碑提前寫成完成。
- browser harness focused batch 也把最後一條未納管的主線 single-spec smoke 正式收口：`web/tests/browser/launchpad-session-onboarding.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-launchpad-session-onboarding-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:launchpad-session-onboarding` / `test:browser:launchpad-session-onboarding:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 這條最小真實主線，不再讓它只停留在 spike readiness 與單檔 smoke 引用裡；根目錄 `package.json` 也同步提供 `web:test:browser:launchpad-session-onboarding` 與 `web:test:browser:launchpad-session-onboarding:existing-server` 轉發入口，讓主線 launchpad onboarding journey 可以被獨立 targeted 重跑。
- browser harness focused batch 也再補了一條 credentials follow-up 收斂：`web/tests/browser/admin-readiness-credentials-onboarding-usage-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-credentials-onboarding-usage-settings-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-credentials-onboarding-usage-settings-go-live-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-credentials-settings-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:credentials-settings-followup` / `test:browser:credentials-settings-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> admin`、`admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> admin` 與 `admin readiness credentials -> onboarding -> usage -> /settings?intent=manage-plan -> go-live -> admin` 這三條 continuity，不再讓這三條 spec 停留在未納管狀態。
- browser harness focused batch 也把 baseline onboarding follow-up 正式納管：`web/tests/browser/admin-readiness-baseline-onboarding-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-baseline-onboarding-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-baseline-onboarding-go-live-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-baseline-onboarding-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:baseline-onboarding-followup` / `test:browser:baseline-onboarding-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin readiness baseline -> onboarding` 之後的 direct admin return、verification return 與 go-live return 三條最小閉環，不再讓 baseline onboarding family 只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 credentials onboarding follow-up 正式納管：`web/tests/browser/admin-readiness-credentials-onboarding-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-credentials-onboarding-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-credentials-onboarding-members-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-credentials-onboarding-service-accounts-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-credentials-onboarding-api-keys-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-credentials-onboarding-playground-verification-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-credentials-onboarding-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:credentials-onboarding-followup` / `test:browser:credentials-onboarding-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin readiness credentials -> onboarding` 之後的 verification、go-live、members、service-accounts、api-keys 與 playground follow-up，不再讓 credentials onboarding family 只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 demo-run follow-up 正式納管：`web/tests/browser/admin-readiness-demo-run-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-usage-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-artifacts-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-playground-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-verification-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-demo-run-verification-go-live-verification-settings-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-demo-run-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:demo-run-followup` / `test:browser:demo-run-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 admin readiness demo-run branch 在 `verification -> admin`、`verification -> go-live -> admin`，以及轉入 go-live 後再分流到 verification、verification -> settings、usage、playground 與 artifacts 的 follow-up，不再讓這條主幹分支只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 demo-run verification follow-up 正式納管：`web/tests/browser/admin-readiness-demo-run-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-verification-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-usage-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-demo-run-verification-go-live-playground-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-demo-run-verification-go-live-artifacts-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-demo-run-verification-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:demo-run-verification-followup` / `test:browser:demo-run-verification-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin readiness demo_run -> verification` 之後的 direct admin return，以及轉入 go-live 後再分流到 settings、verification、usage、playground 與 artifacts 的 follow-up，不再讓 demo-run family 只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 go-live-ready follow-up 正式納管：`web/tests/browser/admin-readiness-go-live-ready-go-live-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-go-live-ready-go-live-verification-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-go-live-ready-go-live-verification-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-go-live-ready-go-live-settings-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-go-live-ready-go-live-usage-return.smoke.spec.ts`、`web/tests/browser/admin-readiness-go-live-ready-go-live-playground-return.smoke.spec.ts` 與 `web/tests/browser/admin-readiness-go-live-ready-go-live-artifacts-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-go-live-ready-followup-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:go-live-ready-followup` / `test:browser:go-live-ready-followup:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `admin readiness go_live_ready -> go-live` 之後的 direct admin return，以及轉向 verification、settings、usage、playground 與 artifacts 的 follow-up，不再讓 go-live-ready family 只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members mainline 正式納管：`web/tests/browser/session-members-verification.smoke.spec.ts`、`web/tests/browser/launchpad-session-members-verification.smoke.spec.ts`、`web/tests/browser/session-members-verification-admin-return.smoke.spec.ts`、`web/tests/browser/launchpad-session-members-verification-admin-return.smoke.spec.ts`、`web/tests/browser/session-members-verification-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-mainline-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-mainline` / `test:browser:session-members-mainline:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 session checkpoint 與 launchpad 兩個入口在進入 members 之後的最小 continuity、direct admin return 與 direct go-live return，不再讓這批最常用主線只停留在散落 smoke + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification reentry 正式納管：`web/tests/browser/session-members-verification-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/launchpad-session-members-verification-settings-verification-admin-return.smoke.spec.ts`、`web/tests/browser/session-members-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-settings-verification-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-reentry-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-reentry` / `test:browser:session-members-verification-reentry:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session / launchpad -> session -> members -> verification -> settings -> verification -> admin` 主幹，以及 `session / launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin` companion，不再讓這組 verification reentry smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 session-members verification reverse re-entry 正式納管：`web/tests/browser/session-members-verification-settings-go-live-verification-admin-return.smoke.spec.ts`、`web/tests/browser/launchpad-session-members-verification-settings-go-live-verification-admin-return.smoke.spec.ts`、`web/tests/browser/session-members-verification-go-live-settings-verification-admin-return.smoke.spec.ts` 與 `web/tests/browser/launchpad-session-members-verification-go-live-settings-verification-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-session-members-verification-reverse-reentry-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:session-members-verification-reverse-reentry` / `test:browser:session-members-verification-reverse-reentry:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `session` 與 `launchpad` 兩條 mirror 入口在 `settings -> go-live -> verification` 與 `go-live -> settings -> verification` 這兩種 reverse re-entry 形狀上的最小真實 browser 閉環，不再讓這組 mirror smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness focused batch 也把 accept-invitation usage recovery 正式納管：`web/tests/browser/onboarding-accept-invitation-usage-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-settings-admin-return.smoke.spec.ts`、`web/tests/browser/onboarding-accept-invitation-usage-go-live-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-usage-admin-return.smoke.spec.ts`、`web/tests/browser/members-accept-invitation-onboarding-usage-settings-admin-return.smoke.spec.ts` 與 `web/tests/browser/members-accept-invitation-onboarding-usage-go-live-admin-return.smoke.spec.ts` 現在由 `web/tests/e2e/browser-e2e-accept-invitation-usage-recovery-readiness.test.ts` 固定 scripts/docs contract，並透過 `test:browser:accept-invitation-usage-recovery` / `test:browser:accept-invitation-usage-recovery:existing-server` 收斂成一個 focused batch。這條 batch 專門守住 `onboarding` 與 `members` 兩條 invite recovery lane 在 `usage -> admin`、`usage -> /settings?intent=manage-plan -> admin` 與 `usage -> go-live -> admin` 這三種最小閉環上的真實 browser continuity，不再讓這批 usage/settings recovery smoke 只停留在散落 spec + 單檔 readiness 的狀態。
- browser harness 的 prebuilt/start-only runner 其後也再補了一組 focused smoke revalidation：`settings -> verification -> admin`、`verification delivery panel -> go-live -> admin` 與 `go-live delivery panel -> verification -> admin` 已透過 `test:browser:smoke:prebuilt` 重新驗證通過。這代表新的 runner script 與可覆寫 host/port/base-url 的 browser harness，不只停留在配置層，現在也已實際支撐 settings 直鏈與 delivery-panel continuity 的最小真實 browser smoke；整體仍維持 `focused smoke / small batch / in_progress` 口徑，不把這批 runner-level revalidation 擴寫成完整 browser e2e 已完成。

## 8. 建議任務卡格式

每張任務卡至少包含：

- 背景
- 目標
- 檔案範圍
- 不可修改的檔案
- 驗收標準
- 測試命令
- 風險與回退方式

推薦命名：

- `SAAS-W1-A workspace-context-hardening`
- `SAAS-W1-B critical-route-fallback-cleanup`
- `SAAS-W1-C mutation-proxy-consistency`
- `SAAS-W2-D billing-self-serve-path`
- `SAAS-W3-E onboarding-productization`
- `SAAS-W4-F enterprise-surface`

## 9. 本輪推薦驗證命令

倉庫級：

- `npm run check`
- `npm run web:check`

必要時補充：

- 針對變更的 route / component 補最小手動驗證
- 若 billing 或 onboarding 流改動較大，至少做一輪頁面跳轉與 mutation smoke

## 10. 文檔更新規則

當以下事項完成時，需要同步更新本文件：

- 工作流狀態從 `planned` 進入 `in_progress`
- 工作流完成定義改變
- 真正文件 ownership 或依賴關係發生變化
- fallback / billing / onboarding 的策略與本文不一致

---

目前建議先啟動的並行槽位如下：

- 槽位 1：A `workspace/session`
- 槽位 2：B `critical route hardening`
- 槽位 3：C `mutation/detail proxy consistency`

待第一波合併與驗證後，再啟動：

- 槽位 4：D `billing productionization`
- 槽位 5：E `onboarding productization`
