# Govrail Docs Index

這個目錄存放 Govrail MVP 的交付文檔、運維手冊與配置範例。

如果你是第一次接手這個倉庫，建議閱讀順序如下。

## 1. 先看哪份

### 想快速了解這個專案現在做到哪裡

- [../README.md](/Users/zh/Documents/codeX/agent_control_plane/README.md)
- [../agent_control_plane_dev_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/agent_control_plane_dev_spec_zh.md)
- [saas_plan_zh.md](./saas_plan_zh.md)
- [saas_v1_execution_plan_zh.md](./saas_v1_execution_plan_zh.md)
- [saas_phase1_data_model_zh.md](./saas_phase1_data_model_zh.md)
- [saas_week8_verification_checklist_zh.md](./saas_week8_verification_checklist_zh.md)（新增 delivery tracking panel、attention queue、返回 admin queue、organization drill-down、Week 8 readiness summary 以及 readiness drill-down 使用說明）
  - 最新檢查項已同步 SaaS trusted session boundary：metadata-backed session 只接受 `x-authenticated-subject` / `cf-access-authenticated-user-email`，不再把 `x-subject-id` 視為可信身份來源。
  - 最新檢查項也同步了 onboarding persisted summary 與 period-aware plan-limit evidence，方便驗收 bootstrap summary 回顯與 subscription billing window 對齊。
  - 最新檢查項也把 `accept-invitation` 的 guardrail 驗收點補進來：trusted session、seat-limit、revoked/expired token、disabled workspace / organization 都要有明確且誠實的頁面文案。
  - 最新檢查項也把 `/session`、`members`、`launchpad` 與 `usage -> /settings?intent=manage-plan` 的 continuity 補進來：session 現在是顯式 trusted-session checkpoint，members 會誠實提示 pending invitation 先占 `member_seats` reservation，settings 則明確標成 self-serve billing follow-up lane。
  - `/admin` 的 recent delivery activity 卡片現在也可以直接把 `attention_workspace`、`surface`（以及選擇性的 `attention_organization`）帶回治理 jump，這仍然是 navigation-only 的 follow-up contract，沒有 impersonation 或支援自動化的承諾。
  - `/admin` 的 action/attention queue 預設只顯示目前 focus 下的前幾筆 workspace，並提供 `Show more` 以在相同 snapshot 內展開更多項目，同樣只做導航、沒有 impersonation 或自動化支援。上方新增的 focus-state control bar 會把 surface、organization、workspace 與 returned follow-up 狀態用 chip 呈現，chip 上會顯示 filter 標籤、當前值，以及一個 per-chip 的「Clear」連結（存在時），同時當任一 focus 有值時也會顯示「Clear all focus」的 action，讓平台維運可以透過清除單一層級或回到更大治理視角的方式重置 focus，所有操作仍然只是導航提示，沒有實際 impersonation 或 automation 的行為。
  - `/admin` 的 Week 8 readiness summary 卡片不但展示 onboarding baseline / credentials / demo run / billing warning / mock go-live-ready 五個 counts，還可以讓 operators 點擊該指標，把 Week 8 readiness follow-up 列表過濾到對應 workspace，並跳轉到 onboarding、settings、verification 或 go-live surface 繼續觀察。所有的 drill-down 均是 navigation-only 的 governance cues，沒有 impersonation 或 support automation。
  - `/admin` 的 Week 8 readiness follow-up flows carry `source=admin-readiness`, `week8_focus`, and the organization/workspace context so the onboarding, settings, verification, or go-live pages can remind operators where they came from, surface a “Return to admin readiness view” link, and keep the same governance focus when returning to `/admin`. This keeps the whole loop shareable while remaining purely navigation context.
  - 當前驗收邊界仍以 `unit + contract + page + non-browser smoke` 為主。對於 web non-browser smoke，根目錄入口 `npm run web:test:e2e` 會跑完整 `web/tests/e2e/*.test.ts`，而 `npm run web:test:e2e:file -- tests/e2e/saas-mainline-smoke.e2e.test.ts` 適合 targeted 單檔重跑；不要期待 `npm run web:test:e2e -- <file>` 只執行一個 smoke。browser 側則已新增 167 條最小 true browser smoke，可透過根目錄入口 `npm run web:test:browser:smoke` 執行；這個入口目前等同於顯式穩定模式 `npm run web:test:browser:smoke:stable`。若只需先重建 browser harness，再接著跑 prebuilt smoke，根目錄也已提供 `npm run web:test:browser:build`、`npm run web:test:browser:smoke:revalidate` 與 `npm run web:test:browser:smoke:prebuilt`。若只需跑 focused batch，根目錄也已對齊提供 `npm run web:test:browser:session-checkpoint`、`npm run web:test:browser:session-members-mainline`、`npm run web:test:browser:billing-shortchain`、`npm run web:test:browser:billing-warning-settings-followup`、`npm run web:test:browser:billing-warning-go-live-followup`、`npm run web:test:browser:billing-warning-verification-go-live-followup`、`npm run web:test:browser:mainline-console-verification`、`npm run web:test:browser:mainline-console-go-live`、`npm run web:test:browser:launchpad-audit-export`、`npm run web:test:browser:launchpad-session-onboarding`、`npm run web:test:browser:credentials-settings-followup`、`npm run web:test:browser:baseline-onboarding-followup`、`npm run web:test:browser:credentials-onboarding-followup`、`npm run web:test:browser:demo-run-followup`、`npm run web:test:browser:demo-run-verification-followup`、`npm run web:test:browser:go-live-ready-followup`、`npm run web:test:browser:session-members-verification-reentry`、`npm run web:test:browser:session-members-verification-reverse-reentry`、`npm run web:test:browser:session-members-verification-settings-return`、`npm run web:test:browser:session-members-verification-settings-go-live-return`、`npm run web:test:browser:session-members-verification-go-live-verification-return`、`npm run web:test:browser:session-members-verification-settings-go-live-verification-settings-return`、`npm run web:test:browser:accept-invitation-verification-bridge`、`npm run web:test:browser:accept-invitation-usage-recovery`、`npm run web:test:browser:accept-invitation-usage-settings-verification-bridge`、`npm run web:test:browser:secondary-console-verification`、`npm run web:test:browser:secondary-console-go-live`、`npm run web:test:browser:smoke:revalidate`、`npm run web:test:browser:smoke:prebuilt`，以及明確要求外部 server 的 `npm run web:test:browser:smoke:existing-server`、`npm run web:test:browser:session-checkpoint:existing-server`、`npm run web:test:browser:session-members-mainline:existing-server`、`npm run web:test:browser:billing-shortchain:existing-server`、`npm run web:test:browser:billing-warning-settings-followup:existing-server`、`npm run web:test:browser:billing-warning-go-live-followup:existing-server`、`npm run web:test:browser:billing-warning-verification-go-live-followup:existing-server`、`npm run web:test:browser:mainline-console-verification:existing-server`、`npm run web:test:browser:mainline-console-go-live:existing-server`、`npm run web:test:browser:launchpad-audit-export:existing-server`、`npm run web:test:browser:launchpad-session-onboarding:existing-server`、`npm run web:test:browser:credentials-settings-followup:existing-server`、`npm run web:test:browser:baseline-onboarding-followup:existing-server`、`npm run web:test:browser:credentials-onboarding-followup:existing-server`、`npm run web:test:browser:demo-run-followup:existing-server`、`npm run web:test:browser:demo-run-verification-followup:existing-server`、`npm run web:test:browser:go-live-ready-followup:existing-server`、`npm run web:test:browser:session-members-verification-reentry:existing-server`、`npm run web:test:browser:session-members-verification-reverse-reentry:existing-server`、`npm run web:test:browser:session-members-verification-settings-return:existing-server`、`npm run web:test:browser:session-members-verification-settings-go-live-return:existing-server`、`npm run web:test:browser:session-members-verification-go-live-verification-return:existing-server`、`npm run web:test:browser:session-members-verification-settings-go-live-verification-settings-return:existing-server`、`npm run web:test:browser:accept-invitation-verification-bridge:existing-server`、`npm run web:test:browser:accept-invitation-usage-recovery:existing-server`、`npm run web:test:browser:accept-invitation-usage-settings-verification-bridge:existing-server`、`npm run web:test:browser:secondary-console-verification:existing-server`、`npm run web:test:browser:secondary-console-go-live:existing-server`。新增的 session-members-mainline focused batch 則把 `session -> members -> verification` 與 `launchpad -> session -> members -> verification` 這兩個入口的最小中段 continuity、direct admin return、以及 direct go-live return 收斂成同一組 batch。其中 mainline-console 兩組 focused batch 會把 `settings / usage / verification / go-live` 這批主線 navigation surfaces 分別收斂成 `-> verification -> admin` 與 `-> go-live -> admin` 小批次；兩組都是按 destination leg 分組，所以 `go-live-delivery-admin-return` 會落在 verification batch，而 `verification-delivery-admin-return` 會落在 go-live batch。新增的 launchpad audit-export focused batch 則專門守住 launchpad 根頁頂部治理卡的 `launchpad audit-export -> verification -> admin` run-aware continuity；新增的 launchpad-session-onboarding focused batch 則把 `launchpad -> session -> onboarding -> usage -> settings -> verification -> go-live -> admin` 這條主線 single-spec continuity 納入獨立 targeted 重跑入口。新增的 billing-warning-settings-followup focused batch 則把 `billing_warning -> settings -> admin`、`billing_warning -> settings -> verification -> admin` 與 `billing_warning -> settings -> go-live -> admin` 這三條 billing-warning 主幹 follow-up 收斂成同一組 batch。新增的 billing-warning-go-live-followup focused batch 則把 `billing_warning -> settings -> go-live -> usage -> admin`、`... -> playground -> admin`、`... -> artifacts -> admin`、`... -> settings -> admin`、`... -> verification -> admin` 與 `... -> verification -> settings -> admin` 這批 go-live 後續 follow-up 收斂成同一組 batch。新增的 billing-warning-verification-go-live-followup focused batch 則把 `billing_warning -> settings -> verification -> go-live -> usage -> admin`、`... -> playground -> admin`、`... -> settings -> admin`、`... -> verification -> admin` 與 `... -> verification -> settings -> admin` 這批 verification-to-go-live follow-up 收斂成同一組 batch。新增的 credentials-settings-followup focused batch 則收斂 `admin readiness credentials -> onboarding -> usage -> settings` 這條鏈上的直接 admin return、verification return 與 go-live return。新增的 baseline-onboarding-followup focused batch 則把 `admin readiness baseline -> onboarding` 這條線收斂成 direct admin return、verification return 與 go-live return 三條閉環。新增的 credentials-onboarding-followup focused batch 則把 `admin readiness credentials -> onboarding` 這條線上的 verification、go-live、members、service-accounts、api-keys 與 playground follow-up 一次收斂成同一組 focused batch。新增的 demo-run-verification-followup focused batch 則把 `admin readiness demo_run -> verification` 之後的 direct return 與 `go-live -> settings / verification / usage / playground / artifacts` follow-up 一次收斂。新增的 go-live-ready-followup focused batch 則把 `admin readiness go_live_ready -> go-live` 之後的 direct return 與 `verification / settings / usage / playground / artifacts` follow-up 一次收斂。新增的 session-members-verification-reentry focused batch 則把 `session / launchpad -> session -> members -> verification -> settings -> verification` 這個 shared re-entry 形狀收斂成一個 mirror batch。新增的 session-members-verification-reverse-reentry focused batch 則把 `settings -> go-live -> verification` 與 `go-live -> settings -> verification` 這兩種 reverse re-entry 形狀收斂成同一組 mirror batch。新增的 session-members-verification-settings-return focused batch 則把 `session / launchpad -> session -> members -> verification -> settings -> admin` 與 `session / launchpad -> session -> members -> verification -> go-live -> settings -> admin` 這兩條 settings-return 尾段收斂成同一組 mirror batch。新增的 accept-invitation-verification-bridge focused batch 則把 `onboarding / members -> accept-invitation -> verification` 與其 `go-live` companion 收斂成同一組 bridge batch。新增的 accept-invitation-usage-recovery focused batch 則把 `onboarding / members -> accept-invitation` 之後的 `usage -> admin`、`usage -> settings -> admin` 與 `usage -> go-live -> admin` recovery lane 收斂成同一組 batch。新增的 accept-invitation-usage-settings-verification-bridge focused batch 則把 `onboarding -> accept-invitation -> usage -> settings -> verification -> go-live -> admin` 與 `members -> accept-invitation -> onboarding -> usage -> settings -> verification -> go-live -> admin` 這兩條代表性 mixed bridge，加上 `usage -> settings -> verification -> admin`、`usage -> settings -> verification -> settings -> admin` 與 `usage -> settings -> verification -> settings -> go-live -> admin` 等延伸閉環，一起收斂成同一組 mirror batch。secondary-console 兩組 focused batch 則對齊 `agents / api-keys / artifacts / egress / logs / playground / service-accounts / tasks` 這批 navigation-heavy surfaces 的相同回流結構。`*:existing-server` 入口共用 `run-playwright-existing-server-smoke.mjs`，會要求 `PLAYWRIGHT_BASE_URL`，並先對既有 host 做一次 preflight probe；預設 probe `/api/control-plane/health`，必要時可用 `PLAYWRIGHT_SERVER_READY_PATH`、`PLAYWRIGHT_SERVER_READY_TIMEOUT_MS`、`PLAYWRIGHT_SERVER_READY_RETRIES`、`PLAYWRIGHT_SERVER_READY_RETRY_DELAY_MS` 調整，若外部環境已自行保證存活也可帶 `PLAYWRIGHT_SKIP_SERVER_PROBE=1` 略過。`*:prebuilt` 入口則共用 `run-playwright-prebuilt-smoke.mjs`，會先檢查是否已提供 `PLAYWRIGHT_BASE_URL` / `PLAYWRIGHT_REUSE_EXISTING_SERVER=1`；`*:revalidate` 則會先強制重建，再接續走同一條 prebuilt runner。只要有外部 server 指定，browser harness 就不再啟動本地 `webServer`，而是假設命令已重用這個 production-backed host；若沒有 base URL，Playwright 仍會先 `next build` 並以 `next start` 建立 3005 上的 server，避免長測試被 `next dev` watcher 中途重啟打斷。當前 sandbox 仍會在 `localhost` listen 階段吐出 `EPERM`，所以 focused smoke 通常要提供 `PLAYWRIGHT_BASE_URL`（必要時再帶 `PLAYWRIGHT_PORT`）才能成功；若只是在本地快速調單條 smoke，可改用 `npm run web:test:browser:smoke:dev` 臨時切回 watcher-backed dev server，代價是更高的重啟風險；而 `npm run web:test:browser:spike` 則保留作為 browser harness readiness report：
    - 主線：`launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin`，會在 `/admin` 顯示 `Returned from Week 8 readiness` / `Focus restored` 的 return-state banner。
    - admin-attention 分支：`admin -> verification -> admin` 會驗證 `Open verification checklist`、workspace surface 上的 `Return to admin queue`，以及 `/admin` 的 `Admin queue focus restored` queue-return banner；`admin -> verification -> go-live -> admin` 則在此基礎上再補 `Continue to go-live drill` 與 go-live surface 上的相同 queue-return continuity。
    - admin recent-delivery 分支：`admin recent delivery activity -> verification -> admin` 與 `admin recent delivery activity -> verification -> go-live -> admin`，前者會顯式保留 `delivery_context=recent_activity` 並驗證 workspace surface 上的 recent-activity context copy 與返回 `/admin` 後的 queue-return continuity；後者則在此基礎上再補 `Continue to go-live drill` 與 go-live surface 上的相同 recent-context continuity。
    - admin organization-focus 分支：`admin organization focus -> verification -> admin`，會顯式保留 `attention_organization`，並驗證 Governance focus 的 Organization chip、`Focused organization` cue、queue-return banner，以及 `Clear all focus` 回到 broader admin 視角的連續性。
    - admin focus-chip 分支：`admin organization + workspace + return focus -> per-chip clear`，會驗證 `Workspace`、`Follow-up return`、`Organization` 三個 chip 的 `Clear` 會逐層放寬治理視角，而不會一次把較高層 focus 一起丟掉。
    - admin readiness 分支：`admin readiness baseline -> onboarding -> admin`，會驗證 `week8_focus=baseline` 的 Governance focus 與 `Drill-down active: Baseline gaps`、`Open onboarding flow`、`Finish onboarding`、workspace surface 上的 `Return to admin readiness view`，以及返回 `/admin` 後的 `Returned from Week 8 readiness` / `Clear readiness focus`。
    - admin readiness onboarding follow-up 分支：`baseline -> onboarding -> verification -> admin` 與 `baseline -> onboarding -> go-live -> admin`，會驗證 onboarding 內的 `Step 6: Capture verification evidence` / `Step 7: Rehearse go-live` CTA 仍保留 `source=admin-readiness`、`week8_focus=baseline` 與 readiness return continuity。
    - admin readiness credentials onboarding 分支：`credentials -> onboarding -> verification -> admin`、`credentials -> onboarding -> go-live -> admin`、`credentials -> onboarding -> members -> verification -> admin`、`credentials -> onboarding -> service-accounts -> verification -> admin`、`credentials -> onboarding -> api-keys -> verification -> admin` 與 `credentials -> onboarding -> playground -> verification -> admin`，會驗證 `week8_focus=credentials` 的 summary drill-down 進入 onboarding 後，仍可沿著 `Step 1: Invite first members`、`Step 2: Create service account`、`Step 3: Issue API key`、`Step 4: Run playground demo`、`Step 6: Capture verification evidence` / `Step 7: Rehearse go-live` 保留 admin-readiness continuity。
    - session / members 中段 continuity 分支：`session -> members -> verification` 與 `launchpad -> session -> members -> verification`，會驗證 `Review members and access`、`Manual onboarding handoff`、`Capture verification evidence` 這條治理導流在真實瀏覽器下仍保留 `source=admin-readiness`、workspace / organization focus 與 `surface=verification`。
    - session / members admin-return 分支：`session -> members -> verification -> admin`、`session -> members -> verification -> settings -> admin`、`session -> members -> verification -> go-live -> admin`、`session -> members -> verification -> settings -> go-live -> admin`、`session -> members -> verification -> settings -> go-live -> settings -> admin`、`session -> members -> verification -> go-live -> settings -> admin`、`session -> members -> verification -> go-live -> settings -> verification -> admin`、`session -> members -> verification -> go-live -> settings -> verification -> settings -> admin`、`session -> members -> verification -> go-live -> verification -> admin`、`session -> members -> verification -> go-live -> verification -> settings -> admin`、`session -> members -> verification -> settings -> verification -> admin`、`session -> members -> verification -> settings -> verification -> go-live -> admin`、`session -> members -> verification -> settings -> go-live -> verification -> admin`、`session -> members -> verification -> settings -> go-live -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> admin`、`launchpad -> session -> members -> verification -> go-live -> settings -> admin`、`launchpad -> session -> members -> verification -> go-live -> settings -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> settings -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> go-live -> verification -> admin`、`launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> settings -> admin`、`launchpad -> session -> members -> verification -> settings -> verification -> admin`、`launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin`、`launchpad -> session -> members -> verification -> settings -> go-live -> verification -> admin` 與 `launchpad -> session -> members -> verification -> settings -> go-live -> verification -> settings -> admin`，會驗證 members 中段治理導流已可重新接回 `/admin`，並保留 `Return to admin readiness view`、`Returned from Week 8 readiness`、`Focus restored` 與 `Clear readiness focus` 這組閉環提示。
    - invite continuity 分支：`onboarding -> accept-invitation`、`members -> accept-invitation`、`onboarding -> members -> accept-invitation`，以及 `onboarding -> accept-invitation` 後手動返回再進 `session`，會驗證 `Invite-to-accept path`、`Manual onboarding handoff`、`Accept workspace invitation`、`Token guidance`、`Accept invitation`、`/session` 提示與 handoff query continuity，但不會虛構 token 成功後的 role-lane 導流。
    - invite recovery 分支：`members -> accept-invitation -> 返回 members -> onboarding`、`members -> accept-invitation -> 返回 members -> verification`、`onboarding -> accept-invitation -> 返回 onboarding -> usage`、`onboarding -> accept-invitation -> 返回 onboarding -> verification`，會驗證未提交 token 時的手動恢復路徑仍保留 continuity query，且能誠實回到 onboarding/usage/verification 後續 lane。
    - invite recovery 深化分支：`members -> accept-invitation -> 返回 members -> onboarding -> usage`、`members -> accept-invitation -> 返回 members -> verification -> go-live`、`onboarding -> accept-invitation -> 返回 onboarding -> usage -> /settings?intent=manage-plan`、`onboarding -> accept-invitation -> 返回 onboarding -> verification -> go-live`，會驗證恢復後可以再往 usage、`/settings?intent=manage-plan`、go-live 繼續深入，同時保留 query continuity 與 `surface=verification|go_live`、`intent=manage-plan` 這類關鍵語義。
    - invite recovery admin-return 分支：`onboarding -> accept-invitation -> usage -> admin`、`onboarding -> accept-invitation -> usage -> settings -> admin`、`onboarding -> accept-invitation -> usage -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> settings -> admin`、`onboarding -> accept-invitation -> usage -> settings -> verification -> admin`、`onboarding -> accept-invitation -> usage -> settings -> verification -> settings -> admin`、`onboarding -> accept-invitation -> usage -> settings -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> settings -> verification -> go-live -> admin`、`onboarding -> accept-invitation -> usage -> settings -> go-live -> verification -> admin` 與 `onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`，會驗證 invite recovery 之後已可沿著 usage/settings/verification/go-live 多跳閉環再回到 `/admin`，並恢復 `readiness_returned=1`、`Returned from Week 8 readiness`、`Focus restored` 與 `Clear readiness focus` 這組治理閉環提示；其中 `usage -> /settings?intent=manage-plan` 後續路徑仍誠實沿用現有 handoff builder 對 `delivery_context` 的正規化邊界，而 `go-live -> verification` 回跳也要接受 owner metadata 可能落在 `recent_owner_display_name` 的既有序列化現實。
    - members invite recovery admin-return 分支：`members -> accept-invitation -> onboarding -> usage -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> go-live -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> go-live -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> verification -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> verification -> settings -> admin`、`members -> accept-invitation -> onboarding -> usage -> settings -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> go-live -> admin`、`members -> accept-invitation -> verification -> go-live -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> verification -> admin` 與 `members -> accept-invitation -> verification -> settings -> verification -> go-live -> admin`，會驗證 members 入口的 invite recovery 也已可重新接回 `/admin`，同時維持靜態 accept-invitation 邊界、手動恢復敘事，以及真實可保留的 query continuity。
    - admin readiness chip-toggle 分支：`admin readiness baseline -> clear readiness focus -> credentials toggle -> clear`，只在 `/admin` 內驗證 `week8_focus` 的 clear/toggle continuity，並確認 `attention_organization` / `attention_workspace` 這類較高層 governance focus 不會在切換 readiness drill-down 時被一起清掉。
    - admin readiness action-variant 分支：`billing_warning -> settings -> admin` 與 `demo_run -> verification -> admin`，會驗證 summary card 的 primary action 能保留 `week8_focus`、workspace/organization context 與 `Return to admin readiness view`，並在返回 `/admin` 後恢復 readiness banner。
    - admin readiness go-live 分支：`go_live_ready -> go-live -> admin` 與 `demo_run -> verification -> go-live -> admin`，會驗證 readiness 線從 summary/verification 進入 `go-live` 仍保留 `source=admin-readiness`、`week8_focus` 與 return banner continuity。
  - 新增的 `web:test:browser:demo-run-followup` focused batch 則把 `demo_run -> verification -> admin`、`demo_run -> verification -> go-live -> admin`、`demo_run -> verification -> go-live -> verification -> admin`、`demo_run -> verification -> go-live -> verification -> settings -> admin`、`demo_run -> verification -> go-live -> usage -> admin`、`demo_run -> verification -> go-live -> playground -> admin` 與 `demo_run -> verification -> go-live -> artifacts -> admin` 這批 admin readiness demo-run branch 的主幹 follow-up 收斂成同一組 batch。
    - admin readiness demo-run go-live follow-up 分支：`demo_run -> verification -> go-live -> settings -> admin`、`demo_run -> verification -> go-live -> verification -> admin`、`demo_run -> verification -> go-live -> verification -> settings -> admin`、`demo_run -> verification -> go-live -> usage -> admin`、`demo_run -> verification -> go-live -> playground -> admin` 與 `demo_run -> verification -> go-live -> artifacts -> admin`，會驗證 go-live 內的 `Review billing + settings` / `Reopen verification evidence` / `Confirm usage posture` / `Revisit playground run` / `Inspect artifacts evidence` CTA 仍保留 `source=admin-readiness`、`week8_focus=demo_run` 與 readiness return continuity。
    - admin readiness go-live follow-up 分支：`go_live_ready -> go-live -> verification -> admin`、`go_live_ready -> go-live -> verification -> settings -> admin`、`go_live_ready -> go-live -> settings -> admin`、`go_live_ready -> go-live -> usage -> admin`、`go_live_ready -> go-live -> playground -> admin` 與 `go_live_ready -> go-live -> artifacts -> admin`，會驗證 go-live 內的 `Reopen verification evidence` / `Review billing + settings` / `Confirm usage posture` / `Revisit playground run` / `Inspect artifacts evidence` CTA 仍保留 admin-readiness continuity。
    - admin readiness settings follow-up 分支：`billing_warning -> settings -> verification -> admin`、`billing_warning -> settings -> go-live -> admin` 與 `billing_warning -> settings -> go-live -> verification -> admin`，會驗證 settings 內的 `Capture verification evidence` / `Rehearse go-live readiness` CTA 仍保留 admin-readiness continuity。
    - admin readiness billing-warning go-live follow-up 分支：`billing_warning -> settings -> go-live -> settings -> admin`、`billing_warning -> settings -> go-live -> usage -> admin`、`billing_warning -> settings -> go-live -> playground -> admin`、`billing_warning -> settings -> go-live -> artifacts -> admin` 與 `billing_warning -> settings -> go-live -> verification -> settings -> admin`，會驗證 billing-warning 線進入 go-live 後，`Review billing + settings` / `Confirm usage posture` / `Revisit playground run` / `Inspect artifacts evidence` / `Reopen verification evidence` 的 follow-up CTA 也都保留 `source=admin-readiness`、`week8_focus=billing_warning` 與 readiness return continuity。
    - admin readiness billing-warning verification-to-go-live follow-up 分支：`billing_warning -> settings -> verification -> go-live -> usage -> admin`、`billing_warning -> settings -> verification -> go-live -> playground -> admin`、`billing_warning -> settings -> verification -> go-live -> artifacts -> admin`、`billing_warning -> settings -> verification -> go-live -> verification -> admin` 與 `billing_warning -> settings -> verification -> go-live -> verification -> settings -> admin`，會驗證 billing-warning 線先進 verification 再轉 go-live 之後，對應的 `Confirm usage posture` / `Revisit playground run` / `Inspect artifacts evidence` / `Reopen verification evidence` / `Review settings + billing` CTA 也都保留 `source=admin-readiness`、`week8_focus=billing_warning` 與 readiness return continuity。
  - 新增的 `web:test:browser:session-members-verification-settings-go-live-return` focused batch 則把 `session -> members -> verification -> settings -> go-live -> admin` 與 `launchpad -> session -> members -> verification -> settings -> go-live -> admin` 這兩條 mirror go-live tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-settings-go-live-settings-return` focused batch 則把 `session -> members -> verification -> settings -> go-live -> settings -> admin` 與 `launchpad -> session -> members -> verification -> settings -> go-live -> settings -> admin` 這兩條 mirror settings tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-settings-go-live-verification-settings-return` focused batch 則把 `session -> members -> verification -> settings -> go-live -> verification -> settings -> admin` 與 `launchpad -> session -> members -> verification -> settings -> go-live -> verification -> settings -> admin` 這兩條 deeper go-live/verification tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-settings-verification-settings-return` focused batch 則把 `session -> members -> verification -> settings -> verification -> settings -> admin` 與 `launchpad -> session -> members -> verification -> settings -> verification -> settings -> admin` 這兩條 deeper settings/verification tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-settings-verification-go-live-return` focused batch 則把 `session -> members -> verification -> settings -> verification -> go-live -> admin` 與 `launchpad -> session -> members -> verification -> settings -> verification -> go-live -> admin` 這兩條 settings/verification re-entry 收斂成同一組 batch。
  - 新增的 `web:test:browser:launchpad-session-onboarding` focused batch 會把 `launchpad -> session -> onboarding -> usage -> /settings?intent=manage-plan -> verification -> go-live -> admin` 這條主線 minimal browser continuity 單獨收斂成一組 single-spec focused batch，讓首頁 launchpad 入口一路回到 `/admin` 的真實渲染與 return-state contract 可以被 targeted 重跑。
  - 新增的 `web:test:browser:session-members-verification-go-live-verification-return` focused batch 則把 `session -> members -> verification -> go-live -> verification -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> verification -> admin` 這兩條 go-live/verification re-entry 收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-go-live-verification-settings-return` focused batch 則把 `session -> members -> verification -> go-live -> verification -> settings -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> verification -> settings -> admin` 這兩條 go-live/verification mirror 尾段收斂成同一組 batch。
  - 新增的 `web:test:browser:session-members-verification-go-live-settings-verification-settings-return` focused batch 則把 `session -> members -> verification -> go-live -> settings -> verification -> settings -> admin` 與 `launchpad -> session -> members -> verification -> go-live -> settings -> verification -> settings -> admin` 這兩條 deeper verification/settings tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-usage-settings-go-live-return` focused batch 則把 `onboarding -> accept-invitation -> usage -> settings -> go-live -> admin` 與 `members -> accept-invitation -> onboarding -> usage -> settings -> go-live -> admin` 這兩條 invite recovery continuity 收斂成同一組 batch，強化 invite + members usage -> settings -> go-live -> admin 這串 return。
  - 新增的 `web:test:browser:accept-invitation-usage-settings-go-live-verification-return` focused batch 則把 `onboarding -> accept-invitation -> usage -> settings -> go-live -> verification -> admin` 與 `members -> accept-invitation -> onboarding -> usage -> settings -> go-live -> verification -> admin` 這兩條 reverse bridge 收斂成同一組 batch。
- 新增的 `web:test:browser:accept-invitation-verification-settings-go-live-settings-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> go-live -> settings -> admin` 與 `members -> accept-invitation -> verification -> settings -> go-live -> settings -> admin` 這兩條 invite verification/settings tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-go-live-settings-followup` focused batch 則把 `onboarding -> accept-invitation -> verification -> go-live -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 以及 `members -> accept-invitation -> verification -> go-live -> settings -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 這六條 go-live -> settings / verification follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-settings-go-live-followup` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> go-live -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`onboarding -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 以及 `members -> accept-invitation -> verification -> settings -> go-live -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> admin`、`members -> accept-invitation -> verification -> settings -> go-live -> verification -> settings -> admin` 這六條 settings -> go-live / verification follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-settings-verification-go-live-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> settings -> verification -> go-live -> admin` 這兩條 invite verification re-entry 後再切回 go-live 的 tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-settings-reentry` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> verification -> admin` 與 `members -> accept-invitation -> verification -> settings -> verification -> admin` 這兩條 invite verification re-entry 返回 admin 的尾段收斂成同一組 batch，確保 `verification -> settings -> verification -> admin` 的交錯再入仍保留 query continuity。
  - 新增的 `web:test:browser:accept-invitation-verification-settings-verification-settings-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> settings -> verification -> settings -> admin` 這兩條 invite verification/settings re-entry 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-go-live-settings-verification-settings-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> go-live -> settings -> verification -> settings -> admin` 這兩條 invite verification reverse tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-go-live-verification-settings-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 與 `members -> accept-invitation -> verification -> go-live -> verification -> settings -> admin` 這兩條 invite verification 的 go-live mirror tail 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-go-live-reentry` focused batch 則把 `onboarding -> accept-invitation -> verification -> go-live -> verification -> admin -> re-entry` 與 `members -> accept-invitation -> verification -> go-live -> verification -> admin -> re-entry` 這兩條 go-live verification re-entry 形狀收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-verification-direct-return` focused batch 則把 `onboarding -> accept-invitation -> verification -> settings -> admin`、`members -> accept-invitation -> verification -> settings -> admin`、`onboarding -> accept-invitation -> verification -> go-live -> admin` 與 `members -> accept-invitation -> verification -> go-live -> admin` 這四條 invite verification direct return 尾段收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-entry-continuity` focused batch 則把 `onboarding -> members -> accept-invitation`、`onboarding -> accept-invitation, then return -> session`、`onboarding -> accept-invitation (static)` 與 `members -> accept-invitation (static)` 這批前段 continuity 收斂成同一組 batch。
  - 新增的 `web:test:browser:accept-invitation-early-recovery` focused batch 則把 `onboarding -> accept-invitation -> usage`、`onboarding -> accept-invitation -> usage -> settings`、`members -> accept-invitation -> return -> onboarding` 與 `members -> accept-invitation -> onboarding -> usage` 這批早期 recovery lane 收斂成同一組 batch。
  - 新增的 `web:test:browser:settings-readiness-followup` focused batch 則把 `settings -> verification -> admin` 與 `settings -> go-live -> admin` 這兩條 settings readiness follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:usage-verification-followup` focused batch 則把 `usage -> verification -> admin` 與 `usage -> verification -> go-live -> admin` 這兩條 usage follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:delivery-panel-cross-return` focused batch 則把 `go-live -> delivery -> admin` 與 `verification -> delivery -> admin` 這兩條 delivery panel cross-return 收斂成同一組 batch。
  - 新增的 `web:test:browser:credentials-go-live-followup` focused batch 則把 `agents -> go-live -> admin`、`api-keys -> go-live -> admin` 與 `service-accounts -> go-live -> admin` 這三條 credentials direct go-live follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:admin-attention-recent-activity-followup` focused batch 則把 `admin attention -> verification -> admin`、`admin attention -> verification -> go-live -> admin`、`admin recent delivery activity -> verification -> admin` 與 `admin recent delivery activity -> verification -> go-live -> admin` 這四條 admin queue / recent-activity follow-up continuity 收斂成同一組 batch。
  - 新增的 `web:test:browser:admin-governance-focus-controls` focused batch 則把 `admin focus chips clear one dimension at a time`、`admin organization focus branch -> verification -> admin` 與 `admin readiness chip clear/toggle` 這三條 admin governance focus control smoke 收斂成同一組 batch。
  - 新增的 `web:test:browser:billing-warning-settings-followup` focused batch 則把 `billing_warning -> settings -> admin`、`billing_warning -> settings -> verification -> admin` 與 `billing_warning -> settings -> go-live -> admin` 這三條 billing-warning 主幹 follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:billing-warning-go-live-followup` focused batch 則把 `billing_warning -> settings -> go-live -> usage -> admin`、`billing_warning -> settings -> go-live -> playground -> admin`、`billing_warning -> settings -> go-live -> artifacts -> admin`、`billing_warning -> settings -> go-live -> settings -> admin`、`billing_warning -> settings -> go-live -> verification -> admin` 與 `billing_warning -> settings -> go-live -> verification -> settings -> admin` 這批 go-live 後續 follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:billing-warning-verification-go-live-followup` focused batch 則把 `billing_warning -> settings -> verification -> go-live -> usage -> admin`、`billing_warning -> settings -> verification -> go-live -> playground -> admin`、`billing_warning -> settings -> verification -> go-live -> settings -> admin`、`billing_warning -> settings -> verification -> go-live -> verification -> admin` 與 `billing_warning -> settings -> verification -> go-live -> verification -> settings -> admin` 這批 verification-to-go-live follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:logs-verification-followup` focused batch 則把 `logs -> verification -> admin` 與 `logs -> verification -> go-live -> admin` 這兩條 logs follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:playground-verification-followup` focused batch 則把 `playground -> verification -> admin` 與 `playground -> verification -> go-live -> admin` 這兩條 playground follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:agents-verification-followup` focused batch 則把 `agents -> verification -> admin` 與 `agents -> verification -> go-live -> admin` 這兩條 agents follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:api-keys-verification-followup` focused batch 則把 `api-keys -> verification -> admin` 與 `api-keys -> verification -> go-live -> admin` 這兩條 api-keys follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:artifacts-verification-followup` focused batch 則把 `artifacts -> verification -> admin` 與 `artifacts -> verification -> go-live -> admin` 這兩條 artifacts follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:egress-verification-followup` focused batch 則把 `egress -> verification -> admin` 與 `egress -> verification -> go-live -> admin` 這兩條 egress follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:service-accounts-verification-followup` focused batch 則把 `service-accounts -> verification -> admin` 與 `service-accounts -> verification -> go-live -> admin` 這兩條 service-accounts follow-up 收斂成同一組 batch。
  - 新增的 `web:test:browser:tasks-verification-followup` focused batch 則把 `tasks -> verification -> admin` 與 `tasks -> verification -> go-live -> admin` 這兩條 tasks follow-up 收斂成同一組 batch。
  - 這一輪也順手修正了 checklist handoff helper 在 server/client 邊界上的引用方式；完整 browser e2e 仍後置，不應把目前覆蓋寫成真實跨頁點擊與渲染時序已 fully covered。現況是 167 條最小真實 browser smoke 已覆蓋主線、attention、recent-activity、organization-focus、focus-chip，以及 onboarding/settings/go-live/usage/playground/artifacts、session/members 中段 continuity、session/members admin-return、invite continuity、invite recovery continuity、invite recovery 深化 continuity、invite recovery admin-return continuity、members invite recovery admin-return continuity 與 credentials onboarding step surfaces 內的多條 readiness action variants，但仍不是 verification/go-live/admin 的 full browser e2e。

### 想對接 API 或補後端行為

- [api_contract_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/api_contract_spec_zh.md)
- [data_model_state_machine_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/data_model_state_machine_spec_zh.md)
- [policy_approval_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/policy_approval_spec_zh.md)

### 想部署、驗收或排障

- [deployment_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/deployment_runbook_zh.md)
- [github_actions_runtime_inventory_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/github_actions_runtime_inventory_zh.md)
- [environment_config_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/environment_config_runbook_zh.md)
- 其 billing portal 區段說明 customer portal 返回邏輯：API `return_url` > `STRIPE_CUSTOMER_PORTAL_RETURN_URL` > `BILLING_RETURN_BASE_URL`，這個返回路徑只在 portal 完成後觸發，與 webhook 或 checkout success 無關。
- [access_ingress_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/access_ingress_runbook_zh.md)
- [access_ingress_plan.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/access_ingress_plan.example.json)
- [observability_alerting_baseline_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/observability_alerting_baseline_zh.md)
- [monitoring_dashboard_template.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/monitoring_dashboard_template.example.json)
- [observability_integration_manifest.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/observability_integration_manifest.example.json)
- [incident_response_checklist_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/incident_response_checklist_zh.md)
- [secret_rotation_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/secret_rotation_runbook_zh.md)
- [ops_handoff_summary_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/ops_handoff_summary_zh.md)
- [enterprise_surface_runbook_zh.md](./enterprise_surface_runbook_zh.md)
- [saas_mock_go_live_drill_zh.md](./saas_mock_go_live_drill_zh.md)
- [tests_observability_wave1_wave2_min_plan_zh.md](./tests_observability_wave1_wave2_min_plan_zh.md)
- [final_delivery_summary_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/final_delivery_summary_zh.md)
- [flow_failure_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/flow_failure_runbook_zh.md)
- [saas_week8_verification_checklist_zh.md](./saas_week8_verification_checklist_zh.md)
- 如果是要交接給下一位工程師，優先把 `post-deploy:verify` 當成驗收出口來看
- 如果是新 tenant 接入，優先把 onboarding bundle 內的 `provision.sh`、`apply-request.sh`、`verify.sh` 和 `complete-handoff.sh` 當成最小接入與驗收出口來看
- 如果是新 tenant 接入，先用 onboarding bundle 內的 `status.sh` 快速看摘要，再用 `submit-request.sh`、`apply-request.sh`、`verify.sh` 完成接入與驗收

## 2. 各文件用途

| 文件 | 用途 |
|---|---|
| [api_contract_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/api_contract_spec_zh.md) | API 端點、錯誤碼、冪等、A2A、MCP 契約 |
| [data_model_state_machine_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/data_model_state_machine_spec_zh.md) | D1 表結構、狀態機、artifact / audit / queue 模型 |
| [policy_approval_spec_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/policy_approval_spec_zh.md) | policy 匹配、approval 規則與治理約束 |
| [flow_failure_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/flow_failure_runbook_zh.md) | 執行路徑、失敗語義、證據來源與排障方向 |
| [deployment_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/deployment_runbook_zh.md) | 部署步驟、驗收流程、D1 SQL 查詢、常見故障 |
| [github_actions_runtime_inventory_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/github_actions_runtime_inventory_zh.md) | GitHub Actions 所需 repo variables / secrets / inputs 對照清單 |
| [release_checklist_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/release_checklist_zh.md) | staging / production 發版前後的可勾選清單 |
| [environment_config_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/environment_config_runbook_zh.md) | staging / production 配置、secrets、graph / replay / SSE 驗證節奏 |
| [access_ingress_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/access_ingress_runbook_zh.md) | Access / service token / trusted-edge 部署方式與入口治理檢查點 |
| [access_ingress_plan.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/access_ingress_plan.example.json) | Access / service token 部署計劃模板，供生成 checklist 與驗證命令 |
| [access:ingress:plan](/Users/zh/Documents/codeX/agent_control_plane/scripts/render_access_ingress_plan.mjs) | 根據 ingress plan 模板生成 access-ingress-plan 與 checklist |
| [observability_alerting_baseline_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/observability_alerting_baseline_zh.md) | 可觀測性、SLI、告警門檻與 oncall 排障順序 |
| [monitoring_dashboard_template.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/monitoring_dashboard_template.example.json) | 可直接對接監控系統的 dashboard 模板 |
| [observability_integration_manifest.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/observability_integration_manifest.example.json) | synthetic checks、alert routes、evidence contract 的跨平台接入契約 |
| [incident_response_checklist_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/incident_response_checklist_zh.md) | oncall 事故處置與升級清單 |
| [secret_rotation_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/secret_rotation_runbook_zh.md) | `auth_ref` 對應 Worker secret 的輪替、回滾與交接流程 |
| [secret:rotation:bundle](/Users/zh/Documents/codeX/agent_control_plane/scripts/render_secret_rotation_bundle.mjs) | 根據 rotation plan 生成 rotation-plan、checklist 與 rotate.sh |
| [ops_handoff_summary_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/ops_handoff_summary_zh.md) | 目前已落地環境、verify 證據與接手入口摘要 |
| [final_delivery_summary_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/final_delivery_summary_zh.md) | 本輪交付的完成項、部署結果、驗收證據與下一步建議 |
| [enterprise_surface_runbook_zh.md](./enterprise_surface_runbook_zh.md) | enterprise 能力（audit export / SSO / dedicated environment）啟用前提、入口、失敗回退與驗收手冊 |
| [tests_observability_wave1_wave2_min_plan_zh.md](./tests_observability_wave1_wave2_min_plan_zh.md) | Wave 1/2 hardening 最小測試集（單元 / 契約 / 頁面級）與優先級落地計劃 |
| [saas_plan_zh.md](./saas_plan_zh.md) | SaaS 化產品定位、資料模型、技術演進與 8 週里程碑 |
| [saas_v1_execution_plan_zh.md](./saas_v1_execution_plan_zh.md) | SaaS v1 的 4 週執行計劃、並行開發工作流、檔案 ownership 與驗收標準 |
| [saas_phase1_data_model_zh.md](./saas_phase1_data_model_zh.md) | SaaS 第一階段資料模型、workspace 與 tenant 映射，以及 migration 0004 說明 |
| [tenant_onboarding_runbook_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/tenant_onboarding_runbook_zh.md) | 新 tenant 接入步驟、tenant onboarding bundle 生成、驗收出口、回滾與交接資訊 |
| [tenant_provisioning_request.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/tenant_provisioning_request.example.json) | 外部 provisioning / ticket / CMDB 可直接對接的 request manifest 範例 |
| [implementation_status_matrix_zh.md](/Users/zh/Documents/codeX/agent_control_plane/docs/implementation_status_matrix_zh.md) | 一頁查看已實作 / 佔位 / 保留欄位 / 驗證方式 |
| [wrangler.multi-env.example.jsonc](/Users/zh/Documents/codeX/agent_control_plane/docs/wrangler.multi-env.example.jsonc) | Wrangler 多環境配置範例 |
| [secrets.bulk.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/secrets.bulk.example.json) | `wrangler secret bulk` 匯入範例 |
| [secret_rotation_plan.example.json](/Users/zh/Documents/codeX/agent_control_plane/docs/secret_rotation_plan.example.json) | secret rotation 計劃模板，包含舊新 binding、驗證命令與 rollback 提示 |

## 3. 建議操作順序

### 本地改完代碼後

- `npm run verify:local`
- `npm run verify:build`
- web non-browser e2e 入口：
  - `npm run web:test:e2e` 會跑完整的 `web/tests/e2e/*.test.ts`
  - `npm run web:test:e2e:file -- tests/e2e/saas-mainline-smoke.e2e.test.ts` 只跑指定檔案，適合 targeted smoke
  - 不要期待 `npm run web:test:e2e -- tests/e2e/saas-mainline-smoke.e2e.test.ts` 只跑單檔；root script 仍會轉發到整包 `test:e2e`

### staging 部署後

- `BASE_URL="https://<worker>" TENANT_ID="tenant_verify" npm run post-deploy:verify`
- 會驗證 health、agent card、graph、replay metadata、A2A SSE snapshot 與 MCP SSE ready stream
- 另外會建立一筆暫時的 provider / policy，最後確認兩者已停用並可在 `status=disabled` 清單中查到
- 若是要驗 web console staging，而不是 root API Worker，先從 repo root 執行 `npm run web:deploy:staging:dry-run` 或 `npm run web:deploy:staging`
- web staging 上線後，可用 `PLAYWRIGHT_BASE_URL="https://<web-staging>.workers.dev" npm run web:test:browser:session-checkpoint:existing-server` 或 `npm run web:test:browser:mainline-console-verification:existing-server` 做 focused browser smoke

### production 部署後

- `BASE_URL="https://<worker>" TENANT_ID="tenant_prod" RUN_ID="<existing_run_id>" npm run post-deploy:verify:readonly`
- 會驗證既有 run、graph、artifact 與可用的 SSE ready stream
- 這是較適合 handoff 的 production gate，因為它不會建立新的 run、provider 或 policy

## 4. 目前最值得注意的限制

- 目前仍是 MVP，還不是完整 production 服務
- 已有 baseline tenant onboarding bundle 與 apply/submit/handoff helper，但 Access / service token、最終 provisioning、secret 輪替與告警體系仍待補強
- staging 建議使用寫入式驗收
- production 建議優先使用唯讀驗收
- 交接時至少要留下 `base_url`、`tenant_id`、`trace_id`、`run_id`、`tool_provider_id`、`policy_id`
