# Admin Console Surface

對應入口：`web/app/(console)/admin/page.tsx`

## 1. 這個 surface 目前負責什麼

`/admin` 是 SaaS console 的平台級總覽入口，不是 workspace 內部操作頁。它負責承接兩條主鏈路：

- `admin-readiness`：Week 8 readiness follow-up
- `admin-attention`：recent delivery / attention queue follow-up

目前這頁的核心角色是「快照 + 導航 + 回跳狀態恢復」，不是代辦自動執行器。

## 2. 真實頁面結構

頁面當前由三層組成：

1. `WorkspaceContextSurfaceNotice`
   - 顯示目前 workspace slug 與 context source
   - 明確提示 metadata-backed / fallback / local-only 差異
2. `PageHeader`
   - 固定描述 admin overview 的平台級 snapshot 目的
3. `AdminOverviewPanel`
   - 真正承載 queue filter、readiness focus、attention workspace / organization、return-state governance

## 3. handoff / query / session 結構

這頁已經對齊共用 helper，而不是各頁自己重寫 query parsing。

- workspace context：`resolveWorkspaceContextForServer()`
- handoff query：`parseConsoleHandoffState(searchParams)`
- queue surface normalization：
  - `queue_surface=verification|go-live|all`
  - 透過 `resolveAdminQueueSurface(...)` 收斂成 `verification | go_live | all`
- return flags：
  - `queue_returned=1`
  - `readiness_returned=1`
- readiness focus：
  - 透過 `resolveConsoleWeek8Focus(...)` 收斂成受控枚舉

這代表 `/admin` 現在不是只會「打開一張總表」，而是能夠知道使用者是不是剛從 `settings / usage / verification / go-live` 回來，以及應該恢復哪一個 queue/readiness 視角。

## 4. 目前已落地的測試契約

- `web/tests/page-level/admin-overview-handoff.page.test.ts`
  - 固定 readiness follow-up link 與 admin-attention query naming
  - 固定 direct go-live queue entry 與 return cues
  - 固定 admin contract source / fallback guidance
- `web/tests/page-level/console-pages-handoff-contract.page.test.ts`
  - 固定 console pages 的 shared query continuity 契約
- `web/tests/e2e/saas-mainline-smoke.e2e.test.ts`
  - 固定 surface -> verification/go-live -> admin return 的 non-browser smoke
- `web/tests/e2e/browser-e2e-mainline-console-readiness.test.ts`
  - 固定 mainline console focused browser batch 與 admin return wiring
- `web/tests/e2e/browser-e2e-console-surface-verification-readiness.test.ts`
  - 固定 `settings/usage -> verification -> admin` 這組 readiness wrapper 映射
- `web/tests/e2e/browser-e2e-console-surface-go-live-delivery-readiness.test.ts`
  - 固定 `settings/verification/go-live -> admin` 這組 go-live/delivery wrapper 映射
- 若只需快速重跑這條主線 smoke，請從倉庫根目錄執行 `npm run web:test:e2e:file -- tests/e2e/saas-mainline-smoke.e2e.test.ts`；`npm run web:test:e2e` 仍保留給整包 non-browser smoke。
- 若只需 focused browser 重跑，主線 console 可從倉庫根目錄執行 `npm run web:test:browser:mainline-console-verification` 或 `npm run web:test:browser:mainline-console-go-live`；admin return lane 會透過這兩組 batch 與上面的 readiness wrapper 一起覆蓋。

## 5. 已完成範圍與保守邊界

已完成：

- admin queue / readiness 的 query continuity
- verification / go-live 顯式 surface 導流
- fallback / feature-gate / 404 / 503 的快照敘事

保守邊界：

- 不做自動 remediation
- 不做跨角色 impersonation
- 不在這頁直接執行 enterprise/support workflow

如果後續要繼續開發，應該優先延續 `AdminOverviewPanel` 內的受控 follow-up action，而不是把邏輯重新分散回 page 層。
